<%# Logowanie — krok 1 (e-mail + hasło). Cały wygląd (układ, logo, hero, stopka)
    obsługuje wspólna skorupa `_login_shell`; tu tylko wskazujemy formularz. %>
<%- include('_login_shell', { innerPartial: '_login_form', email: email, error: error }) %>
