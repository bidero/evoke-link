<%# Publiczna strona oferty /o/:token. Treść karty w partialu `_offer_card` (reużywanym w streamie po
    decyzji — Etap 2, bez reloadu). Stabilny kontener `#offer-card` = cel Turbo Stream `update`. %>
<div class="w-full max-w-2xl">
  <div id="offer-card"><%- include('_offer_card', { offer: offer, state: state, totals: totals, done: done, error: error, turbo: true }) %></div>
</div>
