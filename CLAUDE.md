# Evoke LINK — przewodnik projektu (dla Claude i dewelopera)

Prywatny system wymiany plików agencja ↔ klient (uproszczony, „agencyjny" WeTransfer).
Działa w dwóch kierunkach i grupuje wszystko w **Projektach**. Nazwa aplikacji: **Evoke LINK**
(katalog/repo: `evoke-link`; w historii bywała nazywana „Evoke Transfer").

Domena produkcyjna: **link.evoke.pl** (na razie dev lokalny; produkcja to finalny etap).

## Stack (i dlaczego taki)
- **Node.js + Express** — prosty, znajomy (deweloper ma tło PHP/WordPress).
- **SQLite + Prisma** — zero serwera bazy; backup = kopia pliku. Łatwa migracja na PostgreSQL (zmiana providera).
- **EJS + Tailwind (v3, CLI) + Alpine.js** — szablony serwerowe zamiast SPA: mniej kodu, łatwe utrzymanie, brak builda JS. Alpine z CDN (uwaga: strona klienta wymaga internetu — przed produkcją wrzucić lokalnie).
- **CommonJS** (require), nie ESM.
- Biblioteki świadomie „shared-hosting-safe" (bez kompilacji natywnej): **bcryptjs** (nie bcrypt), **cookie-session** (nie session-file-store — ten rzucał EPERM na Windows; cookie-session przeżywa restart i działa wszędzie), tokeny przez `crypto`, ZIP przez `archiver` (strumieniowo), mail przez `nodemailer`, upload przez `multer`.

## Uruchomienie lokalne
```bash
npm install
npm run prisma:deploy     # zastosuj migracje (dev: prisma migrate dev)
npm run build:css         # Tailwind → public/css/app.css (po każdej zmianie klas!)
npm start                 # http://localhost:3000 → /admin/login (dane z .env)
```
Login admina: `.env` → `ADMIN_EMAIL` / `ADMIN_PASSWORD` (lub `ADMIN_PASSWORD_HASH`, `npm run hash -- "haslo"`).

## Hosting / deploy (SeoHost.pl, DirectAdmin + Passenger)
- Plik startowy: `app.js` (root). Restart: `touch tmp/restart.txt`.
- Deploy: `git pull && npm install && npm run prisma:deploy && touch tmp/restart.txt`. (CSS jest w repo — NIE budujemy Tailwinda na produkcji; `prisma` jest w dependencies, więc migrate deploy działa po produkcyjnym npm install.)
- Sprzątanie wygasłych: cron w DirectAdmin → `node src/jobs/cleanup.job.js` (NIE node-cron — Passenger usypia proces).
- Duże pliki: upload na razie zwykły multipart; przed produkcją dołożyć **chunked upload** (limit rozmiaru requestu na shared hostingu).

## Struktura
```
app.js                    entry (Passenger)
src/app.js                konfiguracja Express
src/config/               wczytanie .env
src/db/client.js          singleton Prisma
src/routes/               admin / public / auth
src/controllers/          obsługa requestów (cienka warstwa)
src/services/             logika: transfer, project, storage, zip, mail, event, auth
src/middleware/           auth, upload (multer), error
src/utils/                format (rozmiary/daty), icons (helper SVG Lucide)
src/views/                EJS (layouts / admin / public / errors)
src/jobs/cleanup.job.js   cron
prisma/schema.prisma      model danych
storage/                  pliki użytkowników + evoke.db (poza repo, .gitignore)
```

## Model danych (Prisma)
- **User** — konto agencji (w MVP logowanie z `.env`, tabela pod przyszłość).
- **Client** — klient agencji (CRM). Pola: `name` (pełna/wyświetlana nazwa), `firstName?`/`lastName?` (personalizacja maili — placeholdery `{imie}`/`{nazwisko}`), `email?`, `company?`, `phone?`, `nip?`, `address?`, `status` lead|active|inactive, `tags?`, `token` (portal klienta `/c/:token`), `note?`. Onboarding: `onboardingToken?` (unikalny, formularz `/onboard/:token`), `onboardingExpiresAt?` (7 dni), `onboardingCompletedAt?` (null = niewypełniony; token zostaje po wypełnieniu — konsumpcję wyznacza `completedAt`, ponowna wizyta = karta „Dziękujemy"). Ma wiele projektów i pozycji rozliczeniowych.
- **Project** — pojemnik. `clientId?` (relacja do Client) / `clientName?` (legacy etykieta), `clientToken` (unikalny, link panelu `/p/:token`), `clientPasswordHash?`, `position` (kolejność drag&drop), `status` active|archived.
- **Charge** — pozycja rozliczeniowa, kwota NETTO w GROSZACH (`amount`). Należy do `projectId?` **lub** `clientId?` (dokładnie jedno; klient pozycji = `clientId ?? project.clientId`). Pola: `label?`, `amount` (netto), `vatRate?` (% VAT 23/8/5/0; null = bez VAT → netto=brutto; brutto = `amount + round(amount*vatRate/100)` przez `chargeService.grossOf`), `date?`, `dueDate?` (wiekowanie należności + przypomnienia), `paidAt?`, `remindedAt?`, `note?`. `chargeService.totals()` zwraca `{net,vat,gross,total(=brutto),paid,outstanding}` — wszystkie sumy/kafelki liczą BRUTTO (agregaty przez `findMany`+`grossOf`, bo SQL `_sum` nie liczy VAT).
- **Transfer** — JEDEN model dla obu kierunków: `direction` = `outgoing` (agencja→klient, `/t/:token`) lub `incoming` (klient→agencja, `/upload/:token`). Pola: `token`, `passwordHash?`, `expiresAt?`, `maxDownloads?`, `downloadCount`, `clientVisible` (czy w panelu klienta), `notifyOnDownload` (e-mail przy 1. pobraniu), `projectId?`, `status` active|expired|deleted.
- **File** — `originalName`, `storedName`, `storedPath` (względna do storage), `size` (BigInt!), `mimeType`. onDelete Cascade.
- **Event** — historia + powiadomienia + dane widżetów. `type` (created|downloaded|uploaded|viewed|email_sent|note|updated|expired|error|onboarded), `projectId?`, `transferId?`, `clientId?` (oś czasu klienta), `isRead`, `dismissed`, `meta` (JSON string). Powiadomienia (dzwonek) = tylko `NOTIFY_TYPES` (uploaded|downloaded|error|approved|changes|onboarded|paid_declared|offer_accepted|offer_rejected); `viewed` jest w osi czasu, ale NIE dzwoni. Klik w powiadomienie: transfer → projekt → klient (fallbacki w `notification.controller.open`).
- **Message** — wiadomość klient↔agencja. `direction` in|out, kontekst `clientId?`/`projectId?`/`transferId?` (definiuje wątek), `senderName?`/`senderEmail?`, `isRead` (dla agencji). Skrzynka panelu grupuje po kontekście (wątki); klient widzi wątek w popupie + badge nowej odpowiedzi (sesja `msgSeen`).
- **Retainer** — cykliczna pozycja rozliczeniowa klienta (abonament). `clientId` (Cascade), `label`, `amount` (NETTO, grosze), `vatRate?`, `dayOfMonth` (1–28), `dueDays` (termin +X dni), `active`, `lastPeriod?` ('YYYY-MM' — anty-duplikat), `note?`. Cron (`reminders.job`) generuje z aktywnych zwykłe `Charge`; MRR = suma brutto aktywnych.
- **Reminder** — zadanie/przypomnienie kalendarza (menedżer zadań). `title`, `note?`, `dueAt`, `done`/`doneAt?`, `priority` low|normal|high, opcjonalnie `clientId?`/`projectId?`. `calendar.service` agreguje je z terminami płatności (`Charge.dueDate`) i wygasaniem transferów (`Transfer.expiresAt`) w jeden strumień wydarzeń.
- **Settings** — branding/customizacja (1 rekord, `id=1`). Kolumny: `appName`, `logoPath`, `faviconPath`, `ogImagePath` (obraz podglądu linku OG), `customCss`, `panel` (JSON `{menu:[{key,hidden,label}], dashboard:[{key,hidden}]}` — układ panelu admina; listy kanoniczne + scalanie w `utils/panelUi.js`: zapis trzyma delty, nieznane klucze odrzucane, nowe pozycje doklejane, „Ustawienia" zawsze widoczne) + JSON-y: `colors` (`primary`, `adminAccent`, `adminText`, `adminSidebar`, `adminHeader` (pasek nagłówka, puste=jak sidebar), `adminBg`, `darkBg/darkSurface/darkText`), `texts` (`heroTitle`, `heroSubtitle`, `footer`), `background` (`type` gradient|custom|image|solid, `preset`, `color`, `custom`, `imagePath`/`images[]`/`rotate`/`rotateSec`, `overlay`, `imageGradient`+`imageGrad`, `grain`/`grainType`/`grainStrength`, `scroll`), `logo` (`size`, `align`, `darkPath`), `layout` (`style`,`card`,`cardSide`,`radius`,`button`,`stickyHeader`,`font`,`hideName`,`heroOnBg`,`applyToLogin`), `emails` (loga/tematy/wstępy + przypomnienia), `pdf` (szablon + dane sprzedawcy).
- **User** — w MVP login z `.env`, ale `auth.service.setAdminPassword` zapisuje hash admina do tej tabeli (po zmianie hasła w panelu baza ma pierwszeństwo nad `.env`).

## Konwencje i pułapki (WAŻNE)
- **GOTCHA EJS: NIE nazywać locala `client` w `res.render` widoku, który używa `include()`** — `ejs.renderFile` kopiuje z locals opcje kompilatora (`_OPTS_PASSABLE_WITH_EXPRESS`: m.in. `client`, `cache`, `views`, `async`, `strict`, `delimiter`); truthy `client` przełącza szablon w tryb client-side i `include` przestaje istnieć („include is not a function"). Dlatego widok onboardingu dostaje `onbClient`. (`client-portal.ejs` dostaje `client` i działa tylko dlatego, że nie ma include'a.)
- **Warstwa storage** (`storage.service.js`) — wszystkie operacje na plikach przez nią (łatwa zmiana na S3). Pliki w `storage/transfers/<token>/`.
- **Ikony**: `<%- icon('nazwa') %>` / `<%- eventIcon(type) %>` (helper `src/utils/icons.js`) — kontur w stylu Lucide, kolor `brand-600` (#6e00a5). BEZ emoji.
- **Kolor przewodni**: `#6e00a5` = `brand-600` (paleta `brand-*` w `tailwind.config.js`). Używać klas `brand-*`, nie `indigo-*`. Tailwind skanuje `src/views`, `src/assets/js`, `src/utils`.
- **Sesja**: cookie-session. Wylogowanie = `req.session = null` (NIE `.destroy()`).
- **Hasła** (transfer/projekt): bcryptjs; odblokowanie zapamiętane w sesji (`unlocked` / `portalUnlocked`).
- **Formularze uploadu (`new.ejs`, `upload.ejs`, `portal.ejs`)** wysyłają pliki przez **XHR** i ręcznie doklejają pola do FormData — przy dodaniu nowego pola PAMIĘTAJ dopisać je w skrypcie (był bug: `projectId`/`clientVisible` nie były wysyłane).
- **Prisma generate** na Windows rzuca `EPERM` na pliku silnika, gdy działa lokalny serwer → **zatrzymaj serwer przed `prisma generate`/`npm install`**.
- **Migracje**: `prisma migrate dev` bywa nieinteraktywne i odmawia przy ostrzeżeniach (np. nowy UNIQUE index). Wtedy: napisz `prisma/migrations/<ts>_nazwa/migration.sql` ręcznie i `prisma migrate deploy`.
- **BigInt**: `File.size` to BigInt — formatować przez `fmt.bytes()`, nie serializować wprost do JSON.
- **Po zmianie klas Tailwind**: `npm run build:css`. **Po zmianie tylko widoków EJS**: wystarczy refresh (dev nie cache'uje).
- **Customizacja (Etap 5–6)** — kluczowe miejsca:
  - Kolory bez rebuildu: paleta `brand-*` to zmienne CSS (`--brand-*`); middleware w `app.js` wstrzykuje `brandStyleTag` (z `primary`, dla stron klienta/logowania) i `adminStyleTag` (z `adminAccent` + `--admin-bg/-text/-sidebar/-sidebar-text/-header/-header-text`, dla panelu). `utils/color.js` generuje paletę + `readableText` (auto-kontrast).
  - Tło stron klienta: `utils/background.js` (`bodyStyle`/`overlayHtml`/`isDark`, presety + własny gradient `custom`, ziarno z `grainStrength`). W layoutach: `bgStyle` na `<body>`, `bgOverlay` jako nakładki (`z-0`), treść w `z-10`.
  - **Tekst na tle**: `<body>` stron klienta/logowania jest ZAWSZE ciemny (`text-slate-800`) — bo treść siedzi w białych kartach. Jasny kolor (gdy `bgDark`) dostaje tylko „chrome" na gradiencie: nagłówek (logo/nazwa) i stopka. NIE ustawiać jasnego tekstu na całym `body` (regresja: niewidoczne `h1` na kartach).
  - **x-data w `settings.ejs`**: stan wstrzykiwany przez `x-data="settingsForm(<%= JSON.stringify(formState) %>)"` — MUSI być `<%=` (HTML-escape), NIE `<%-`. Przy `<%-` cudzysłowy JSON-a urywają atrybut → Alpine pada → color-inputy czernieją (był bug „drugi zapis = czarne").
  - Upload brandingu: `middleware/brandingUpload.js` (pola `logo`/`favicon`/`bg`, do 5 MB, też SVG). SVG czyszczone przez `utils/svgSanitize.js`. Własny CSS czyszczony przez `utils/css.js` (usuwa `<`), wstrzykiwany jako `customStyleTag` we WSZYSTKICH layoutach.
  - Zmiana hasła: strona `/admin/account` (`account.controller`), `auth.service.verifyCredentials` jest **async** (sprawdza DB, potem `.env`). Reset z CLI: `npm run set-password -- "haslo"` / `-- --clear`.
  - Hero/podpis: renderowane w `layouts/public.ejs` (blok nad kartą, gdy `texts.heroTitle` ustawiony), kolor adaptacyjny do jasnego/ciemnego tła.
- **Chunked upload (dzielenie dużych plików)** — omija limit rozmiaru requestu hostingu. Architektura drop-in:
  - Klient: `public/js/chunked-upload.js` → `window.chunkedUpload(files, '<endpoint>/chunk', onProgress)` dzieli pliki na 5 MB i wysyła sekwencyjnie, zwraca `uploadId`. Potem widok wysyła żądanie tworzące (urlencoded) z nagłówkiem `X-Upload-Id`.
  - Serwer: `chunk.service.js` skleja kawałki w `storage/tmp/chunks/<uploadId>/`; `middleware/chunkUpload.js` → `receiveChunk` (endpoint `/chunk`, surowe bajty) + `receiveUpload(field)` zastępuje `multer.array` (gdy jest `X-Upload-Id` → składa pliki w `req.files` w kształcie multera, inaczej multipart fallback). **Kontrolery i serwisy bez zmian** — `req.files` ma ten sam kształt `{ originalname, path, size, mimetype }`.
  - Endpointy `/chunk` MUSZĄ być przed trasami `:id` (np. `/transfers/chunk` przed `/transfers/:id`). Porzucone sesje sprząta `chunk.sweepOld()` (start aplikacji, >24h).
  - **Równoległość**: klient wysyła kawałki współbieżnie (pula 3). Dlatego serwer zapisuje KAŻDY kawałek do osobnego pliku `<fi>_<ci>.part` i skleja je po indeksach przy `assembleFiles` (NIE dopisuje sekwencyjnie — kolejność przyjścia jest dowolna).
  - **Sklejanie jest async + strumieniowe** (`assembleFiles` zwraca Promise; `receiveUpload` robi `await`) — nie blokuje pętli zdarzeń przy dużych plikach. Klient po wysłaniu kawałków pokazuje stan „Finalizowanie…" (animowany pasek), bo na localhoście sam upload bywa zbyt szybki, by pasek był widoczny, a odczuwalne „zawieszenie" to właśnie sklejanie po stronie serwera.
- **Układ stron klienta (warianty wyglądu)** — `Settings.layout` JSON `{ style: classic|centered|split, card: solid|glass|elevated, cardSide: left|right (split), hideName, heroOnBg, applyToLogin, radius, button: rounded|pill }`. `centered` = landing z DUŻYM hero (text-5xl); `split` używa `cardSide` (kolejność przez `md:order-*`). `hideName` chowa nazwę aplikacji (placeholder w nagłówku bez logo + fallback w kolumnie hero). Tło-obraz ma opcję `background.imageGradient` (nakładka gradientu brandowego) i mocniejszy szum (mix-blend `overlay`). Render w `layouts/public.ejs` (3 warianty kompozycji). Styl karty/rogi/przycisk przez zmienne CSS (`--card-*`, `--btn-radius`) wstrzykiwane jako `surfaceStyleTag` (app.js `surfaceVars`) + wspólne klasy `.evoke-card`/`.evoke-btn` (input.css) używane przez 5 widoków publicznych i logowanie. Hero pokazywany gdy `heroTitle && heroOnBg` (na wszystkich układach). **GOTCHA (nawracający, GPU-zależny): artefakt „gradientu” przy tytule karty „szkło”.** Warstwa tła (`position:fixed`, `background.js overlayHtml`) odsłania się `opacity 0→1` po `onload` zdjęcia, JUŻ PO pierwszym malowaniu karty z `backdrop-filter` → część GPU trzyma nieświeżą próbkę backdropu (ciemny/gradientowy pas za tytułem, znika przy scroll/resize). Instant-reveal (v0.83.2, bez `transition`) łagodzi, ale nie usuwa. Trwały fix: `public/js/bg-rotate.js` po odsłonięciu wymusza re-sample kart (`.evoke-card`/`.evoke-panel`) — na 1 klatkę `blur(22px)`, potem powrót do CSS. NIE „naprawiać” tego w CSS karty ani przez powrót do animowanego reveal.
  - **GOTCHA nazwa `uiLayout`**: ustawienia układu w res.locals są pod `uiLayout`, NIE `layout` — `layout` koliduje z express-ejs-layouts (nazwa pliku układu z kontrolera; w widoku to string).
  - **GOTCHA kontrolki**: radia/checkboxy układu w `settings.ejs` są NATYWNE (EJS `checked`, bez Alpine `x-model`/`x-for`) — wzorzec `<template x-for>`+`x-model` na radiach był zawodny w przeglądarce (nie dało się wybrać). Podświetlenie działa przez `peer-checked` natywnie. Reaktywne kontrolki (kolory/tło) dalej na Alpine, bo mają podgląd na żywo.

## Testy
`npm test` = `node --test --test-concurrency=1` na katalogu `test/` (smoke + przepływ wiadomości + ostrzeżenie o wygasaniu i in.). Każdy plik startuje `app.listen(0)`, woła endpointy globalnym `fetch`-em, sprząta utworzone dane i kończy się sam. Uwagi:
- **`--test-concurrency=1` jest OBOWIĄZKOWE** — pliki testowe współdzielą wiersz Settings (id=1); przy równoległych procesach snapshot/restore przeplatały się i wskrzeszały stany „ze środka" innych testów (użytkownikowi zmieniał się wygląd po `npm test`). Testy piszące Settings: snapshot + restore w `finally` ORAZ jawnie przypinać zależne pola (np. `layout.style`), nie dziedziczyć ich z ustawień użytkownika.
- działa na **dev-DB** (`storage/evoke.db`) — testy tworzą i kasują własne rekordy; uruchamiaj lokalnie.
- login admina wymaga `ADMIN_PASSWORD` w `.env` (test wiadomości robi `t.skip` bez niego).
- cookie-session ustawia 2 ciasteczka (`evoke` + `evoke.sig`) — brać oba przez `res.headers.getSetCookie()`.
- joby (np. `reminders.job.js`) mają guard `require.main === module`, więc `require` w teście nie odpala `run()` — można wołać wyeksportowane funkcje (`runExpiryWarnings`).

## Status / roadmapa
- [x] Etap 0 — szkielet, logowanie, layout, model danych
- [x] Etap 1 — transfery wychodzące (`/t/:token`, hasło, wygasanie, limit, ZIP, edycja)
- [x] Etap 2 — uploady przychodzące (`/upload/:token`, e-mail)
- [x] Etap 3 — projekty + historia
- [x] Etap 4 — dashboard (realne dane) + powiadomienia (dzwonek, licznik)
- [x] Panel klienta — portal projektu `/p/:token` (hasło, widoczność per transfer `clientVisible`, pobieranie + upload)
- [x] Etap 5 — customizacja (logo/kolory/treści z panelu; model Settings; kolor bez rebuildu)
- [x] Etap 6 — rozszerzona customizacja: tła stron klienta (presety/własny gradient/obraz/ziarno z mocą), osobne kolory panelu (akcent/sidebar/tło/czcionka, auto-kontrast), logo (rozmiar+pozycja, SVG sanityzowane), brandowane logowanie, zmiana hasła admina (DB), pole na własny CSS
- [x] Chunked upload — dzielenie dużych plików na 5 MB (panel + /upload + portal), drop-in z fallbackiem multipart, sprawdzone E2E (integralność md5)
- [x] Hero/podpis renderowane na stronach klienta; reset hasła z CLI (`npm run set-password`)
- [x] Warianty układu stron klienta (klasyczny/karta-na-tle/hero+karta) + styl karty (biel/szkło/uniesiona) + rogi/przycisk + branding logowania; równoległy chunked upload (pula 3)
- [x] Bezpieczeństwo/ops: backup automatyczny (`src/jobs/backup.job.js`), multer 2.x, rate-limit (login + hasła klienta), Alpine/Sortable lokalnie + CSP (helmet), nodemailer 9 (audit: 0 vuln)
- [x] Rozliczenia: `Charge` (projekt/klient, grosze, `dueDate`), kafelek przeterminowanych, CSV + PDF (pdfmake) + wysyłka mailem, przypomnienia cronem (`jobs/reminders.job.js`)
- [x] CRM: baza klientów + strona 360° (Przegląd/Projekty/Rozliczenia/Transfery/Oś czasu), portal klienta `/c/:token`, `firstName`/`lastName` + personalizacja maili
- [x] Globalna wyszukiwarka (`services/search.service.js`, `/admin/search`, pole w nagłówku) — klienci/projekty/transfery (też po imieniu/nazwisku)
- [x] Więcej układów stron klienta (showcase/panel/panel-bg/sidebar/corner/hero-card/minimal/banner) + sticky header + typografia + gotowe motywy; dark mode (klasa, auto-kontrast)
- [x] Uczciwe placeholdery e-mail per-pole (`mail.PLACEHOLDER_SUPPORT`); osobny kolor paska nagłówka panelu (`colors.adminHeader`)
- [x] Podgląd linku OG/meta + własny obraz OG (`Settings.ogImagePath`); „klient otworzył link" (`Event(type:viewed)` na `/t` i `/p`); kod QR linku (inline SVG, `utils/qr.js`)
- [x] Miniatury obrazów + Quick Look (lightbox) w panelu transferu (`/admin/transfers/:id/preview/:fileId`); fix migania gradientu tła za kartą „szkło" (warstwa odsłaniana po `onload`)
- [x] Wiadomości klient↔agencja (dwukierunkowo): koperta + wątek (popup) na `/p`/`/t`/`/c` + badge nowej odpowiedzi; panel = widok wątków (grupowanie po kontekście) + odpowiedź + mail; model `Message`
- [x] Ostrzeżenie o wygasaniu transferu — mail do agencji o wychodzących wygasających <24 h, niepobranych (cron `reminders`, toggle `emails.expiryWarn`, anty-powtórka `Transfer.expiryWarnedAt`)
- [x] Testy `node:test` (`npm test`): smoke + przepływ wiadomości + ostrzeżenie o wygasaniu (`test/*.test.js`)
- [x] Kalendarz / menedżer zadań — siatka miesiąca + „Nadchodzące", przypomnienia (status/priorytet, powiązanie z klientem/projektem) + agregacja terminów płatności i wygasania; badge „do zrobienia" w menu (`calendar`/`reminder` service, `/admin/calendar`)
- [x] Automatyzacja: „otworzył link" też na `/upload` i `/c`; „Przedłuż transfer" (reset ostrzeżenia + reaktywacja); dzienny digest do agencji = także maile o przypomnieniach na dziś (toggle `emails.dailyDigest`, cron `reminders`)
- [x] Puls agencji (`/admin/pulse`, `stats.service.pulse()`): przychód mies. + delta, do zapłaty/przeterminowane, wykres 6 mies. (inline SVG), skuteczność pobrań (30 dni), aktywni klienci, top klienci wg przychodu — bez nowego modelu
- [x] Fala 3 (drag&drop): kanban projektów `/admin/projects/board` (`Project.stage` lead|active|delivered|paid — niezależny od `status`; przeciąganie kart = POST `:id/stage`) + D&D w kalendarzu (przeciągnięcie przypomnienia na inny dzień = `reminders/:id/move`, godzina zostaje)
- [x] Proofing: akceptacja/poprawki plików per transfer wychodzący (opt-in checkbox `Transfer.proofing`; klient na `/t` i w portalu `/p` klika Zatwierdzam / Poprawki+komentarz → `approvalStatus/Comment/By/At`, event `approved|changes` w NOTIFY_TYPES + mail do agencji; status w panelu transferu i chip w portalu)
- [x] Lista braków (file requests): model `FileRequest` (per projekt, Cascade); sekcja na stronie projektu (dodaj/odhacz/usuń); checklista w portalu `/p` + select „Czego dotyczą te pliki?" przy uploadzie → auto-odhaczenie punktu i link do transferu (GOTCHA: finalize w portal.ejs dokleja `fileRequestId` ręcznie — XHR)
- [x] Osobne logo per powierzchnia: `Settings.logo.{adminPath,adminDarkPath,loginPath,loginDarkPath}` (JSON — bez migracji); `logoTag(settings,{surface})` + `hasLogo` (utils/logo.js). Reguła: własne jasne logo powierzchni → ciemne tylko z JEJ pary (bez mieszania z globalnym ciemnym); puste = dziedziczy bazowe. GOTCHA: logoCfg w settings.controller MUSI przenosić te pola przy zapisie
- [x] Własne tło strony logowania: kolumna `Settings.loginBackground` (JSON jak `background`; null = dziedziczy tło klienta); locals `loginBg*` w app.js (fallback do `bg*`); auth.ejs używa loginBg*; UI w Ustawienia → Wygląd („Strona logowania — tło": preset/własny gradient/kolor/obraz+przyciemnienie, upload `loginBgImage`)
- [x] Kwoty netto + opcjonalny VAT (`Charge.vatRate`, migracja `20260702200000`): `amount` = netto, select VAT (bez/23/8/5/0%) w formularzach projektu i klienta; rozbicie netto/VAT/brutto w pozycjach, podsumowaniach, PDF (kolumny warunkowe) i CSV; wszystkie sumy „do zapłaty" liczą brutto
- [x] Archiwizacja projektu jednym kliknięciem: `POST :id/archive` (toggle active↔archived) — przycisk na kafelku listy (stretched-link, `redirect=list`) i na stronie projektu; `projectService.setStatus`
- [x] Zmiana daty rozliczenia po oznaczeniu: edytowalne pole daty przy pozycji (`chargeService.setPaidDate`; projekt: `POST :id/charges/:cid/paid-date`; klient: przez `updateCharge` z polem `paidAt`, auto-submit)
- [x] Sortowanie klientów po polsku (`localeCompare('pl')` w `client.service` `list()`+`options()` — SQLite ORDER BY jest BINARY); tablica kanban 4 kolumny od `lg` (było `xl` → dwie pod sobą)
- [x] Wybór sortowania na liście klientów: select `sort` (nazwa/firma/nazwisko × rosnąco/malejąco) w tym samym formularzu co szukajka+status (zachowują się nawzajem); `client.service.SORTS` + `sortClients` (puste wartości zawsze na końcu, remis rozstrzyga nazwa)
- [x] Dopasowanie do brutto po VAT (v0.85.2): Puls (`stats.service` — przychód mies./wykres/top klienci/do zapłaty/przeterminowane), kalendarz (tytuł terminu płatności), maile (przypomnienie o płatności, digest), logi zdarzeń przy dodaniu/rozliczeniu pozycji, lista wyboru pozycji przy wydruku — wszystko przez `grossOf`
- [x] Link onboardingowy: jednorazowy formularz `/onboard/:token` (7 dni, regeneracja wymienia token), klient sam uzupełnia firmę/NIP/adres/kontakt (e-mail tylko gdy pusty w kartotece); sekcja w kartotece 360° (utwórz/kopiuj/QR/wyślij mailem/wygeneruj nowy + chip stanu), event `onboarded` (dzwonek) + mail do agencji, szablon maila w Ustawienia → E-mail (`emails.onboardSubject/onboardIntro`); test `test/onboarding.test.js`
- [x] Konfigurowalny panel: pulpit z widżetami (rejestr `utils/panelUi.js`, partiale `views/admin/_widgets/`; 6 kafelków + akcje + aktywność + NOWE: zadania z kalendarza, przychód+top klienci z Pulsu, nieprzeczytane wiadomości) — edycja na pulpicie (ikonka ołówka przy tytule w nagłówku layoutu → `CustomEvent('dash-edit')` łapany przez `@dash-edit.window`; drag&drop Sortable, oko ukrywa, przełącznik szerokości ¼/⅓/½/⅔/1 = `span` 3/4/6/8/12 na siatce `lg:grid-cols-12`; kontrolki Zapisz/Anuluj w pływającym pasku na dole — nic nie przesuwa treści; POST `/admin/dashboard/layout`; dane pobierane tylko dla widocznych, ukryte = placeholder) + menu boczne z ikonami (kolejność/ukrywanie/własne nazwy w Ustawienia → Panel; scalone `res.locals.panelMenu` w admin.routes); `Settings.panel`, migracja `20260703120000`; test `test/panel.test.js` (snapshot+restore Settings!)
- [x] Dane do przelewu w portalu /c: sekcja „Do zapłaty" (nierozliczone pozycje BRUTTO wprost + z projektów — `chargeService.unpaidForClient`, zaległe na czerwono, suma) + blok danych przelewu z `Settings.pdf.seller` (odbiorca/konto/tytuł „Rozliczenie — {klient}") + **QR przelewu wg rekomendacji ZBP** (`utils/payment.js`: `bankDigits` → 26 cyfr NRB albo brak QR, `zbpPayload` `NIP|PL|konto|kwota gr|odbiorca|tytuł|||`); blok konta tylko gdy `seller.bank` ustawiony; przełącznik `pdf.portalBilling` (Ustawienia → Rozliczenia (PDF), domyślnie włączony) chowa całą sekcję. **GOTCHA QR**: `qrcode-generator` domyślnie obcina znaki do 1 bajta — `utils/qr.js` ustawia `stringToBytesFuncs['UTF-8']` (inaczej polskie znaki w payloadzie wychodzą zmielone po skanie)
- [x] Widżet pulpitu „Do odezwania się" (`followup`): klienci bez żadnej aktywności ≥30 dni (`clientService.staleClients` — max z Event/Message wprost i przez projekty, fallback createdAt, bez statusu inactive; 60+ dni na czerwono); przycisk „Przypomnij" = Reminder „Odezwij się: X" na jutro 9:00 (`POST /clients/:id/followup`), otwarte przypomnienie → chip „zaplanowane" (bez dublowania)
- [x] Fix: transfer przypisany do projektu klienta podstawia jego e-mail w polu „Wyślij link e-mailem" (`transfer.getById` dołącza `project.client`)
- [x] Retainery (cykliczność): model `Retainer` (per klient: `label`, `amount` NETTO gr, `vatRate?`, `dayOfMonth` 1–28, `dueDays`, `active`, `lastPeriod` 'YYYY-MM' = anty-duplikat; migracja `20260704090000`); cron `reminders.job → runRetainers()` (odpalany PRZED bramką SMTP — nie wymaga maili; bez generowania wstecz) tworzy z aktywnych zwykłe `Charge` (label „X — lipiec 2026", termin +dueDays) → działają przypomnienia/sumy/PDF; sekcja „Cykliczne (retainery)" w 360°→Rozliczenia (dodaj/wstrzymaj/usuń/„Generuj teraz" z anty-duplikatem, alerty `ret-*`); **MRR** (`retainer.service.mrrActive` brutto) = kafelek + lista w Pulsie (`stats.pulse().retainers`) i dopisek w widżecie przychodu; test `test/retainer.test.js`
- [x] Mail do klienta o nowej pozycji cyklicznej: toggle+szablon `emails.retainerNotify/retainerSubject/retainerIntro` (Ustawienia → E-mail, domyślnie WYŁĄCZONY); `retainer.service.notifyClient` po każdej generacji (cron i „Generuj teraz"; kwota brutto, termin, dane przelewu, link do portalu /c; loguje event `email_sent`, błąd nie blokuje generacji)
- [x] „Zgłoś wykonanie przelewu" w portalu /c: przycisk pod „Do zapłaty" (`POST /c/:token/paid`, messageLimiter, bramka `pdf.portalBilling`) → event `paid_declared` (NOTIFY_TYPES — dzwoni; ikona banknote, bursztyn) + mail do agencji (`sendPaymentDeclared`); chip „Zgłoszono wpłatę {data} — czekamy na potwierdzenie" przez 7 dni = anty-duplikat (jedna deklaracja/7 dni); NIE oznacza pozycji jako opłaconych — potwierdza agencja
- [x] Ikony widżetów pulpitu: `icon` w rejestrze `panelUi.WIDGETS` (nowe PATHS: trendingUp, phone) — kafelki mają ikonę przy etykiecie (prawy róg, slate-300), widżety treściowe w nagłówku h2 (partiale dostają `wicon` przez include)
- [x] Szablony projektów: model `ProjectTemplate` (JSON `items` — migracja `20260704120000`), `template.service` (definicja TEKSTEM: braki `Etykieta | notatka`, przypomnienia `Tytuł | +dni | priorytet` — linia = pozycja, zero JS w formularzu); strona `/admin/projects/templates` (lista+dodaj+usuń, przycisk „Szablony" na liście projektów), select w „Nowy projekt" → `applyToProject` rozstawia FileRequests + Remindery (termin = utworzenie + offsetDays, 9:00, projectId+clientId); test `test/templates.test.js`. GOTCHA: trasa `/projects/templates` PRZED `/projects/:id`
- [x] Interakcje 360°: formularz notatki na osi czasu z typem (notatka/telefon/spotkanie/e-mail — `NOTE_KINDS` w client.controller, prefiks w message + `meta.kind`) i polem „follow-up za X dni" (Reminder „Follow-up: {klient}" 9:00)
- [x] Wskaźniki na 360° (zakładka Przegląd): karta „Statystyki klienta" — LTV (suma zapłaconego brutto), śr. czas płatności (dni od `Charge.date`/createdAt do `paidAt`), wykres przychodu 12 mies. (zapłacone brutto per miesiąc, inline SVG). Liczone przez `clientService.clientMetrics(charges)` z już wczytanych pozycji w `overview` — zero dodatkowych zapytań; test w `test/crm.test.js`
- [x] Oferty/wyceny z akceptacją w portalu: modele `Offer`+`OfferItem` (migracja `20260704160000`; pozycje netto/szt.+ilość+VAT), `offer.service` (definicja pozycji TEKSTEM „Etykieta | netto | VAT | ilość"; `totals`/`state` open|accepted|rejected|expired; `decide` — akceptacja tworzy Charge z pozycji [projektowe albo wprost na kliencie], rusza projekt `lead→active` na kanbanie, log `offer_accepted`; odrzucenie z komentarzem → `offer_rejected`). Publiczna strona `/o/:token` (`public/offer.ejs`: pozycje, suma, Akceptuję/Odrzucam — odrzucenie WYMAGA komentarza, `:disabled` na Alpine + walidacja serwera). Admin: zakładka „Oferty" w 360° (`offer.controller` admin+public jak onboarding; utwórz/kopiuj link/wyślij mailem/usuń, chipy stanu). Lista ofert w portalu `/c` (wygasłe ukryte). Maile: `sendOfferLink` (klient) + `sendOfferDecision` (agencja). Nowe NOTIFY_TYPES `offer_accepted`/`offer_rejected` (dzwonek). Test `test/offers.test.js`
- [x] Załączniki w wiadomościach: pojedynczy plik na Message (`attachmentPath/Name/Size/Mime`; migracja `20260704180000`). Formularz w popoverze `/p /t /c` = `multipart/form-data` + input „Dołącz plik" (`middleware/messageUpload.js` → multer do tmp; `message.service.create` przenosi do `storage/transfers/_messages/` przez `storage.saveMessageFile`, sprząta tmp gdy treść pusta). Bąbelki: badge z nazwą u klienta (public.ejs), link pobierania w skrzynce (`GET /admin/messages/:id/attachment`, requireAuth, `Content-Disposition: attachment`). `deleteThread` kasuje pliki z dysku. Backup obejmuje je automatycznie (są pod `transfers/`). Test `test/attachments.test.js`
- [x] Lejek sprzedaży (widok „Sprzedaż", `/admin/sales`): `offer.service.pipeline()` agreguje oferty ponad klientem — otwarte (sort po najbliższym terminie ważności) + wartość lejka, wygasające w 7 dni, wygasłe (do odświeżenia), skuteczność (win rate = accepted/(accepted+rejected)) i wartość wygranych. Widok `admin/sales.ejs` (kafelki + lista z „Ponaglij" [reuse `/offers/:oid/send`] i „Otwórz"). Pozycja menu `sales` w `panelUi.MENU` (ikona `funnel`; dla zapisanej konfiguracji menu dokleja się na końcu). Test w `test/offers.test.js`
- [x] Dokumenty klienta: model `Document` (migracja `20260704200000`; plik w `storage/transfers/_documents/`, `visibleToClient`). `document.service` (list/listVisible/create[multer→`storage.saveDocumentFile`]/toggleVisible/remove[kasuje plik]). Zakładka „Dokumenty" na 360° (`document.controller` admin: wgraj/toggle/pobierz/usuń; `middleware/documentUpload.js`, 50 MB). Pobieranie: admin `GET /admin/clients/:id/documents/:docId` (requireAuth), klient `GET /c/:token/documents/:docId` (tylko `visibleToClient` + własność). Sekcja dokumentów w portalu `/c` (widoczne). Backup obejmuje pliki (`transfers/`). Test `test/documents.test.js`
- [x] Upload całych katalogów + odporność sieci (v0.97.0): drag&drop folderu (`collectDataTransferFiles` → `webkitGetAsEntry`, rekurencyjnie) i input `webkitdirectory` na wszystkich dropzone'ach (`/upload`, `/p`, panel `new`); ścieżka względna (`katalog/plik.pdf`) jedzie jako `File.originalName` → ZIP odtwarza strukturę, a `Content-Disposition` przy pobraniu bierze samą nazwę (basename). Kawałki ponawiane z backoffem (0,5 s / 2 s, timeout 120 s) — koniec z „Błąd sieci podczas przesyłania" przy chwilowym zerwaniu łącza (typowo przy PDF-ach). `chunk.service`: sanityzacja ścieżek (wycina `..`, dyski, znaki sterujące), `MAX_FILES` 500→5000, wspólny `manifest.json` zastąpiony plikami `<fi>.meta` (bez read-modify-write per kawałek — koniec z 400 „Nieprawidłowy indeks pliku" przy dużej liczbie plików i wyścigu między procesami Passengera); pomijanie plików-śmieci (`.DS_Store` itp.). Test `test/folder-upload.test.js` (md5, kawałki poza kolejnością, 520 plików, sanityzacja `../`)
- [x] Chronologia rozliczenia PDF/CSV/mail (v0.97.1): `chargeService.forStatement` sortuje pozycje chronologicznie zamiast alfabetycznie po projekcie — bloki projektów wg NAJSTARSZEJ pozycji w projekcie (najstarsza czynność wypycha projekt na górę), wewnątrz od najstarszej do najnowszej; pozycja bez daty używa `createdAt` (nie skacze na górę jak przy sortowaniu SQL z NULL-first). `sortForStatement` grupuje po `projectId`, remis dat → nazwa (pl), „Bez projektu" na końcu remisu. Ekran 360°→Rozliczenia CELOWO zostaje newest-first (`overview` orderBy date desc) — podgląd bieżący, nie dokument. Test `test/statement-order.test.js`
- [x] Fix frosted glass w Safari (v0.97.2): brak `-webkit-backdrop-filter` w zbudowanym `app.css` — autoprefixer BEZ `browserslist` wycinał prefiks (choć Safari go wymaga). Dodany `browserslist` (`defaults` + `Safari >= 12` + `iOS >= 12`) → rebuild zachowuje prefiks (8× w app.css: `.evoke-card` glass, sticky-header, `.backdrop-blur`). Inline podglądu karty „szkło" w `settings.ejs` (autoprefixer go nie rusza) dostał `-webkit-` ręcznie. GOTCHA: produkcja serwuje zbudowany `app.css` z repo (Tailwinda nie budujemy na prod), więc prefiks MUSI być w committowanym buildzie
- [x] PDF: tytuł projektu nie osierocony na dole strony (v0.97.3 → POPRAWNE dopiero v0.97.4): węzeł tytułu ma `headlineLevel: 'projectTitle'`. GOTCHA: warunek `followingNodesOnPage.length === 0` z v0.97.3 NIC nie dawał — pdfmake liczy komórki tabeli jako „following" nawet gdy tabela zjeżdża niżej (zweryfikowane instrumentacją: dla osieroconego tytułu following=14, nie 0). Właściwy mechanizm (v0.97.4): (1) tabela dostała `keepWithHeaderRows: 1` (sam nagłówek kolumn nie zostaje na dole — cała tabela zjeżdża); (2) `pageBreakBefore` liczy REALNE miejsce pod tytułem: `startPosition.pageInnerHeight - startPosition.top < 90` (tytuł+nagłówek+wiersz) → przerzuca tytuł z tabelą. Zweryfikowane WIZUALNIE (Read PDF): „Sklep"/„Wizytówki" przechodzą w całości na kolejną stronę. `buildDoc` wyeksportowany do testu. Test `test/pdf-pagebreak.test.js`
- [x] Frosted glass w Safari — DEFINITYWNIE (v0.97.6, po pomyłkach v0.97.2/v0.97.5): PRAWDZIWA przyczyna potwierdzona empirycznie przez usera — **Safari NIE stosuje `-webkit-backdrop-filter` podanego przez `var()`** (inspektor pokazuje rozwiniętą wartość `blur(14px)`, ale silnik jej NIE renderuje; wpisany RĘCZNIE literał `blur(14px)` działa). To nie był ani brak `-webkit-` (v0.97.2), ani var-w-blur (v0.97.5). Fix: `.evoke-card` NIE ma już backdrop-filter w `app.css`; dla szkła `surfaceVars` (app.js) wstrzykuje LITERALNĄ regułę `.evoke-card{-webkit-backdrop-filter:blur(14px);backdrop-filter:blur(14px)}` w `surfaceStyleTag` (po linku do app.css → wygrywa; `.evoke-panel-mode .evoke-card` wyższą specyficznością zeruje przy stylu panel). Inne właściwości szkła (bg/border/shadow) zostają zmiennymi — var() działa dla normalnych właściwości, tylko nie dla `-webkit-backdrop-filter`. GOTCHA NA STAŁE: backdrop-filter zawsze literalną wartością, NIGDY przez var(). Cache-busting `app.css?v=assetVer` (v0.97.5) zostaje
- [x] Nawigacja portali klienta (`Settings.layout.portalNav` none|tabs|side-left|side-right, domyślnie none): sekcje /c (projekty/oferty/dokumenty/do zapłaty — pozycje tylko dla niepustych, przy <2 sekcjach stos) i /p (pliki od nas / prześlij+braki z badge) jako zakładki albo menu boczne w karcie; partial `_portal_nav.ejs` (part side|tabs), Alpine `sec` + deep-link `#hash`; domyślna sekcja z `?sent=1`/`?paid=1` liczona SERWEROWO (GOTCHA: doklejanie hasha w JS po uploadzie = nawigacja „tylko-hash" bez przeładowania — drugi upload nie pokazałby plików); wąskie kompozycje (panel/panel-bg/showcase/corner/split) degradują menu boczne do zakładek (`res.locals.portalNavMode` w app.js). **GOTCHA rename**: /c renderuje local `portalClient` (NIE `client`) — widok ma include(). Test `test/portal-nav.test.js`
- [x] Aktualizator z GitHuba (Ustawienia → Zaawansowane, v0.98.0): `update.service` (check = git fetch + rev-list HEAD..origin/main + wersja z `origin/main:package.json`; `redact()` wycina token z URL-i w KAŻDEJ linii logu/błędu) + ODŁĄCZONY proces `jobs/update.job.js` (spawn detached+unref — przeżywa restart Passengera; kroki: backup OBOWIĄZKOWY [reuse backup.service, `prisma.$disconnect()` przed npm — GOTCHA EPERM Windows] → git pull --ff-only → npm install → npm run prisma:deploy → zapis `tmp/restart.txt`); stan `storage/tmp/update-status.json` (merge, stale-lock 30 min) + log `update.log`; endpointy `/admin/update/{check,run,status,notify}` (JSON dla Alpine; status = health-check po restarcie, polling ignoruje błędy sieci); UI z listą commitów i logiem na żywo; cron `reminders.runUpdateCheck` (przed bramką SMTP) → event `update` w NOTIFY_TYPES (dzwonek; klik → `/admin/settings?tab=advanced`, deep-link zakładki przez `tabInit` w settings.controller); powiadomienia wyłączane plikiem-flagą `storage/update-notify-off` (wzór backup). Test `test/update.test.js`
- [x] Nawigacja portali w CHROME strony (v0.99.0): nowe tryby `portalNav` — `header` (menu w nagłówku obok logo; przy logo wyśrodkowanym pod nim; w Banner/Panelach wpina się w ICH nagłówki, w kompozycji `sidebar` nad kartą) i `rail-left/right` (pełnowysoki brandowy pas: logo + menu + stopka; przy kompozycji `sidebar` menu wchodzi w JEJ pas bez dublowania; na desktopie standardowy nagłówek/stopka dostają `md:hidden` — logo/stopka są w pasie; mobile = poziomy pasek page-top z `mobileOnly`). ARCHITEKTURA: sekcje buduje KONTROLER (local `portalNav` = {sections,keys,defaultSec}; portal.controller /p zawsze 2 sekcje, client.controller /c z regułą ≥2 sekcji → null = stos), a x-data ze stanem `sec` siedzi na wrapperze `layouts/public.ejs` — wspólny scope Alpine dla menu w chrome i `x-show` sekcji w widoku. Partial `_portal_nav.ejs`: parts side|tabs (w karcie), page-top|page-side (pływające), chrome-top (tone: bg|white|brand) i rail. Stare pływające warianty zostają („Pasek nad kartą", „Panel obok karty") — razem 10 opcji
- [x] Link powrotny „Wszystkie projekty" na /p → /c: TYLKO dla sesji, która odwiedziła portal klienta (`req.session.cPortal[token]` ustawiane przy wejściu na /c; sam link /p bywa udostępniany osobom trzecim — NIE ujawniamy im adresu /c z rozliczeniami/dokumentami). `backToPortal` w portal.controller, link nad tytułem w portal.ejs
- [x] Poprawki nawigacji portali + „Wiadomości" w menu + szklany panel + lista kopii (v0.99.1):
  - **Kontrast pozycji menu**: usunięta zawodna heurystyka `bgDark` (górna część gradientu bywa jasna → biały tekst na jasnym = niewidoczny). Chipy `_portal_nav.ejs` ZAWSZE jasne półprzezroczyste (`bg-white/70`) + `dark:` warianty (klasa `html.dark`). GOTCHA: aktywna pozycja na brandzie = `bg-white/90`, NIGDY `bg-white` — `html.dark .bg-white` ma `!important` (override na ciemną powierzchnię), więc `bg-white` w dark mode ciemnieje; `/90` jest poza tym overridem.
  - **Pływające ikonki (tryb/koperta)**: `fabCls` w `layouts/public.ejs` — białe (`bg-white/15 text-white`) gdy brandowy pas jest przy PRAWEJ krawędzi (`rail-right` albo `sidebar` z `cardSide:right`); menu w nagłówku dostaje `pr-12` (odsuw od przełącznika `fixed top-4 right-4`).
  - **„Wiadomości" jako pozycja menu**: kontrolery dokładają do `portalNav.sections` pozycję `{action:'messages', dot:msgHasReply}` (NIE w `keys` — nie liczy się do progu ≥2 sekcji na /c). `navClick()` w partialu: `action:'messages'` → `dispatchEvent('open-msgs')` zamiast przełączania `sec`. Layout: `msgInMenu` chowa pływającą kopertę, `@open-msgs.window` na jej `x-data` otwiera popup. Bez nawigacji koperta jak dotąd.
  - **Szklany panel**: przy `card:'glass'` `surfaceVars` (app.js) wstrzykuje `.evoke-panel{background:rgba(255,255,255,0.62);backdrop-filter:blur(14px)}` + `html.dark .evoke-panel{...0.55...!important}`. Blur LITERALNIE (GOTCHA Safari, nie przez var()). Dotyczy kompozycji panel/panel-bg (flush `.evoke-panel`).
  - **Lista kopii zapasowych** (Ustawienia → Zaawansowane): `backup.service.list/filePath/remove` — nazwa twardo po wzorcu `^evoke-backup-[\w-]+\.zip$` + `name===basename(name)` (zero path traversal). Trasy `GET /admin/settings/backups/:name` (download) + `POST .../:name/delete`. Tabela w settings.ejs (pobierz/usuń). Test `test/backups.test.js` (traversal odrzucany)
- [x] Poprawki wyglądu portali + PDF (v0.99.2):
  - **Dedup logo**: przy brandowym pasie z menu (`railOwn`) logo na TLE kompozycji (panel-bg aside, sidebar aside) chowa się automatycznie (`showBgLogo = !railOwn && !hideBgLogo` w layoucie) — koniec podwójnego logo. Ręczny przełącznik `layout.hideBgLogo` („Ukryj logo na tle strony", Ustawienia → Wygląd).
  - **Przełącznik trybu w pasku menu**: przy `portalNav:'header'` przełącznik jasny/ciemny renderuje się WEWNĄTRZ paska menu (`_portal_nav` part `chrome-top`, local `withTheme`) w jednej linii z pozycjami; pływający (`fixed`) jest wtedy ukryty (`!headerNav`). Wiadomości już wcześniej w menu (msgInMenu). Dotyczy wszystkich kompozycji z headerNav (header/banner/panel/panel-bg/sidebar).
  - **PDF `noWrap` na statusie**: „Rozliczono"/„Do zapłaty" w wąskich szablonach (accent) zawijało się — komórka statusu ma `noWrap:true`.
  - **PDF `pdf.hideSeller`**: przełącznik „Ukryj dane sprzedawcy na wydruku" (Ustawienia → Rozliczenia) — `buildDoc` zeruje `seller` → partiesBlock pokazuje sam blok nabywcy, totalsBlock pomija linię „Do zapłaty na konto". Sam wykaz pozycji bez sprzedawcy.
  - Testy: `test/pdf-pagebreak.test.js` (hideSeller + noWrap na strukturze `buildDoc`), `test/portal-nav.test.js` (przełącznik trybu w menu).
- [x] Pas pionowy: logo out + zwijanie hamburgerem (v0.99.3):
  - **Logo znika z PASA** (nie z panelu/tła — odwrotnie niż v0.99.2). `showBgLogo = !hideBgLogo` (bez warunku railOwn) → logo zostaje na tle kompozycji (panel-bg aside, sidebar aside); pas pionowy renderuje się BEZ logo. Awaryjnie w układach klasycznych (bez innego miejsca): standardowy `<header>` z logo pokazywany też na desktopie przy railOwn (usunięty `railOwn ? md:hidden`; stopka dalej `md:hidden` pod railOwn — jest w pasie).
  - **Zwijany pas (hamburger ☰, push)**: x-data wrappera dostał `railOpen: true`; aside `:class="railOpen ? 'md:w-60' : 'md:w-16'"` + `transition-[width] duration-300 overflow-hidden` (treść płynnie się przesuwa — flex sibling). Przycisk ☰ na górze pasa (`icon('menu')`) toggluje `railOpen`. Part `rail` w `_portal_nav`: etykiety/badge `x-show="railOpen"`, przycisk `:class="{ 'justify-center px-0': !railOpen }"` (ikony wyśrodkowane po zwinięciu); active-state połączony w JEDEN obiekt `:class`. Kompozycja `sidebar` (rail wpięty w jej pas) NIE zwija się — `railOpen` zawsze true, brak `transition-[width]`. Nawigacja bez zmian (sekcje/`sec`/okienko wiadomości działają). Mobile: pas chowany, poziomy pasek u góry jak dotąd. Test rozszerzony w `test/portal-nav.test.js`.
- [x] Pas pionowy: dedup stopki + zapamiętywanie stanu (v0.99.4):
  - **Stopka nie dublowana**: przy `railOwn` + panel/panel-bg była stopka W PASIE (163) i na tle (aside) — dwa napisy „…bezpieczna wymiana plików". Stopkę usunięto z pasa (jak logo — zostaje w kompozycji/na tle); standardowa stopka odsłonięta na desktopie przy railOwn (usunięty `railOwn ? md:hidden`) — dla układów klasycznych to jedyne miejsce.
  - **railOpen z pamięcią (localStorage `evoke-rail`)**: x-data czyta zapamiętany stan (pierwszeństwo — pas trzyma zwinięcie/rozwinięcie MIĘDZY ekranami), fallback = domyślny per-strona (/c zwinięty, /p rozwinięty). `x-init="$watch('railOpen', …setItem…)"` persystuje każdą zmianę. Test rozszerzony (getItem/setItem, brak stopki w pasie, fallback true//p, false//c).
- [x] Pas pionowy jako stały nawigator portalu + wiadomości jako podstrona (v0.99.5):
  - **Pas zawsze widoczny** (rail-*): desktop zwijany hamburgerem (push), MOBILE — wąski pasek ikon w potoku, rozwinięcie = pełnoekranowy overlay (`fixed inset-0 z-40 md:static md:inset-auto md:w-60`; GOTCHA: `w-full` na `fixed` liczyło się do szerokości pasa, nie viewportu — użyto `inset-0`), klik pozycji zamyka overlay (`secClick` dokleja `if(!matchMedia(min-width:768).matches) railOpen=false`). Zniknął poziomy pasek mobilny (page-top) — pas przejął mobile.
  - **Ikona trybu jasny/ciemny na dole pasa** (nie pływająca — koniec kolizji z hamburgerem przy pasie z prawej). Pływające ikony (tryb/koperta) tylko gdy NIE headerNav i NIE railOwn.
  - **„← Wszystkie projekty" jako pozycja pasa** na /p (znika z karty gdy rail; `buildNav` w portal.controller). Sekcje/klucze/railOpen z kontrolera jak dotąd.
  - **Wiadomości = PODSTRONA** `.../wiadomosci` (koniec popupu): wspólny `views/public/messages.ejs` (wątek + formularz + powrót). Nowe GET/POST `/{p,c,t,upload,o}/:token/wiadomosci` + `/message`; pozycja „Wiadomości" i pływająca koperta to teraz LINKI (`msgContext.page`); wejście oznacza wątek jako obejrzany (msgSeen). /upload i /o dostały kontekst wiadomości (wątek: transfer przychodzący / klient-właściciel oferty). Pas na stronach treści (/t /upload /o) przez `utils/contentNav.js` (tylko rail: Wiadomości + tryb; inne warianty — pływająca koperta-link).
  - **GOTCHA `_portal_nav`**: pozycja z `href` = LINK (`<a>`), bez `href` = przycisk sekcji (Alpine `sec`); `linkBase` renderuje sekcje jako linki na podstronie. Testy: `portal-nav` (podstrona, mobile overlay, powrót w pasie), `messages`/`attachments` (asercje na podstronę zamiast popupu).
- [x] Aktualizator: opcja pominięcia kopii zapasowej (v0.99.6): checkbox „Zrób kopię zapasową przed aktualizacją" (domyślnie wł.) w sekcji Aktualizacje. `updater()` (settings.ejs) wysyła `skipBackup` JSON-em do `POST /admin/update/run`; `startUpdate({skipBackup})` dokłada arg `--no-backup`; `update.job` czyta `process.argv.includes('--no-backup')` i pomija krok backupu (log „POMINIĘTA"). Test w `update.test.js`.
- [x] Pas pionowy: przyklejony + poprawki mobile (v0.99.7):
  - **Przyklejony (sticky)**: `md:sticky top-0 h-[100dvh] self-start` — pas NIE przewija się z treścią; hamburger (góra) i tryb jasny/ciemny (dół) ZAWSZE widoczne, nawet przy długiej liście plików. Środek (menu) ma własny `overflow-y-auto`. GOTCHA: sticky wymaga miejsca w pojemniku — działa, bo `flex-1 flex` rośnie z treścią, a aside jest `self-start h-[100dvh]` (krótszy).
  - **Wysokość 100dvh, NIE 100vh**: na mobile `100vh` chowało dół (tryb) za paskiem URL. `h-[100dvh]` = dynamiczna wysokość widoku → tryb zawsze na dole widocznego ekranu.
  - **Mobile = animacja szerokości**: pas jest `fixed` (poza potokiem; treść dostaje `pl-14`/`pr-14`), zwinięty `w-14` → rozwija się PŁYNNIE do `w-screen` przez `transition-[width]` (wcześniej snap: width→inset-0 nie animuje). Klik pozycji zwija.
  - **Hamburger zostaje w miejscu**: usunięty `:class mx-auto` (był re-centrowany przy zwijaniu) — zawsze lewo-wyrównany (w-8), pozostałe ikony przesuwają się w miarę pojawiania napisów. Test w `portal-nav`.
- [x] Poprawki pasa pionowego na mobile + czytelność szkła (v0.99.8):
  - **Sticky nagłówek w „Panel na tle 2" (panel-bg)**: logo mobilne w `isPanelBg` NIE miało `sticky` (miał je tylko `isPanel`) → przy przewijaniu uciekało. Teraz `sticky top-0 z-20 bg-white` z pełną szerokością (ujemne marginesy `-mx-6 sm:-mx-9 px-6 sm:px-9` wychodzą poza padding sekcji — pasek na całą szerokość panelu, nie tylko w obrębie px).
  - **Hamburger pasa — wyśrodkowany + przyklejony do krawędzi**: był `w-8` lewo-wyrównany zawsze → przy pasie z prawej „uciekał" z panelem przy rozwijaniu (skok na lewą krawędź). Teraz `w-full flex` + `:class` — zwinięty: `justify-center` (środek pasa), rozwinięty: dosunięty do KRAWĘDZI (`justify-end` dla rail-right, `justify-start` dla rail-left przez EJS `<%= railRight ? … %>` w wyrażeniu Alpine). Ikona zostaje w miejscu przy zwijaniu (środek zwiniętego = krawędź rozwiniętego, różnica ~4px). Ikona w `<span w-8 h-8>` wewnątrz.
  - **Mrożone szkło czytelniejsze**: krycie 0.62 → **0.74** (`--card-bg` glass + `.evoke-panel`) — tekst pomocniczy (slate-400/500) i przerywana ramka dropzone gubiły się na jasnym tle. Ciemny szklany panel 0.55 → 0.62. Ramka dropzone `border-slate-300` → `border-slate-400` (portal.ejs + upload.ejs — nie łamie stanu `dragging` bo to tylko domyślna klasa). GOTCHA: `border-slate-400` NIE było w committowanym buildzie → wymagał `build:css`.
  - **„Tło ucina się z prawej" — ciemny pasek pod pasem (TYLKO tło-obraz)**: pas ma `h-[100dvh]` (żeby tryb/moon był na dole WIDOKU), ale na iOS `100dvh` na elemencie `position:fixed` bywa liczone jak MAŁY ekran (svh) → przy schowanym pasku URL pas jest KRÓTSZY niż realny widok i pod nim prześwituje baza `<body>`. Przy tle-obrazie baza body to ciemne `#0f172a` (`background.js bodyStyle` dla `type:image`) → widać ciemny pasek z prawej (przy gradiencie prześwituje gradient → niezauważalne). Fix: brandowy **PODKŁAD** `md:hidden fixed inset-y-0 z-20 w-14 bg-brand-600 pointer-events-none` (strona zależna od `railRight`) — `inset-y-0` (top:0;bottom:0, BEZ jednostek dvh/vh) pełni całą wysokość viewportu i wypełnia kolumnę pasa kolorem brandu do samego dołu; kryty przez rozwinięty pas (wyższy z-index). GOTCHA: nie odtwarza się w headless/emulacji (tam `dvh`==`vh`) — weryfikowane symulacją skrócenia pasa.
- [x] Poprawki pasa/nagłówka/szkła po testach na iPhonie (v0.99.9):
  - **Przyklejony nagłówek = DESKTOP only**: na mobile w panel-bg logo było `sticky bg-white` z odstępem od górnej krawędzi (brzydki biały pasek). Teraz mobile logo PŁYNIE z treścią (bez tła); toggle „Przyklejony nagłówek" przykleja na DESKTOPIE brandowy pas boczny (`md:sticky md:top-0 md:h-screen md:self-start` na aside panel-bg) — logo/hero/stopka zostają w widoku przy przewijaniu panelu.
  - **Hamburger pasa — stały rozmiar**: był `w-full` (cały górny wiersz klikalny) → hover zajmował całą szerokość, a stuknięcie OBOK ikony w rozwiniętym panelu zamykało pas („po chwili znika" na iOS). Teraz pojemnik `w-full flex` steruje TYLKO pozycją (justify-center zwinięty / justify-end|start rozwinięty), a klikalny jest przycisk `w-10 h-10` — hover kompaktowy, brak przypadkowego zamykania.
  - **Podkład pasa = `absolute`, nie `fixed`**: w Safari `position:fixed` z `inset-y-0`/`100dvh` dalej bywał krótszy niż widok (ciemny pasek pod pasem przy tle-obrazie). Podkład jest teraz `absolute inset-y-0` w `relative flex-1 flex` (pojemnik ≥ viewport przez `min-h-screen`) → sięga realnego dołu dokumentu bez pułapek fixed-viewport Safari.
  - **Szare czcionki na szkle**: dodane przyciemnienie tekstu pomocniczego WEWNĄTRZ szklanych powierzchni (`.evoke-card`/`.evoke-panel`): slate-400→#64748b (slate-500), slate-500→#475569 (slate-600). Dark mode nietknięty (`html.dark .text-slate-*` ma !important).
- [x] Portale klienta bez przeładowania — Etap 1 (Turbo Drive, v0.99.10): nawigacja między stronami portali (`/c`↔`/p`, wejście w Wiadomości `.../wiadomosci`, powrót) przez **Turbo Drive** (Hotwire 8, zvendorowane lokalnie `public/js/turbo.js` — jak Alpine/Sortable, CSP `'self'` pokrywa) — fetch + podmiana `<body>` zamiast pełnego reloadu, zachowany back/scroll/URL. **Alpine radzi sobie sam** z podmianą body (obserwator mutacji re-inicjuje `x-data` — zweryfikowane: sekcje `sec`, `railOpen`, uploader działają po nawigacji; NIE trzeba `Alpine.initTree`). Skrypty zależne od DOM re-inicjowane na `turbo:load`: `bg-rotate.js`/`sticky-header.js` dostały RE-URUCHAMIALNY `init` (`window.evokeBgInit`/`evokeStickyInit`; sticky podpina scroll RAZ do window, bg czyści interwał slideshow) — bez tego wróciłoby migotanie szkła / martwy sticky. Wykluczone z Turbo (`data-turbo="false"`): wszystkie POBIERANIA (`_file_row`, dokumenty `/c`, ZIP `/p`+`/t`) i WSZYSTKIE formularze publiczne (uploader, proofing, „zgłoś wpłatę", oferta, wiadomości, onboarding, hasło) — w Etapie 1 akcje zostają na pełnym submit. Pasek postępu Turbo w kolorze brand (`.turbo-progress-bar` w input.css). **GOTCHA**: nowe pobieranie/formularz w portalach = pamiętać o `data-turbo="false"`, inaczej Turbo przechwyci klik/submit. Zakres Etapu 2 (osobno): podmiana fragmentów po akcjach (upload→lista bez reloadu, proofing/płatność/oferta w miejscu) + `Turbo.visit` w finalize uploadu.
- [x] Poprawki po Etapie 1 + panel admina (v0.99.11):
  - **Błysk tła przy nawigacji Turbo (regresja Etapu 1)**: `<body>` jest podmieniany, więc warstwa tła-obrazu (odsłaniana `opacity 0→1` po onload) re-odsłaniała się przy KAŻDYM przejściu → migotanie. Fix: warstwy tła w kontenerze `<div id="evoke-bg" data-turbo-permanent>` — Turbo przenosi go 1:1 na nową stronę (ten sam węzeł DOM, już odsłonięty), więc brak re-odsłonięcia. Zweryfikowane: po nawigacji `.bg-img-layer` to TEN SAM element (opacity zostaje 1).
  - **Pasek nagłówka panelu w dark mode** (`colors.adminHeader`): trzymał jasny kolor z ustawień w ciemnym trybie. Dodane `--admin-header`/`--admin-header-text` do bloku `html.dark` w input.css (→ `dk-surface`/`dk-text`), jak sidebar/tło.
  - **Sidebar panelu widoczny cały czas (app-shell przez STICKY)**: sidebar i nagłówek `md:sticky md:top-0` (sidebar `md:h-screen md:self-start`), strona przewija się normalnie (body). ŚWIADOMIE bez `overflow-hidden`/`overflow-y-auto` na kolumnie treści — patrz GOTCHA w v0.99.12. `nav` sidebara `min-h-0 overflow-y-auto` (długie menu). Mobile bez zmian (offcanvas, normalny scroll).
  - **Skok wysokości pozycji pasa portalu**: pozycje `part:'rail'` miały `py-2` — po zwinięciu etykieta znika (x-show), więc element był niższy niż po rozwinięciu (ikona+podpis) → hover/aktywny „skakał". Stała wysokość `h-9` (zamiast `py-2`) na pozycjach pasa i przełączniku trybu → brak skoku (zwinięty=rozwinięty=36px).
- [x] Poprawki po testach v0.99.11 (v0.99.12):
  - **Migotanie „szkła" przy nawigacji Turbo**: handler `turbo:load` wołał `evokeBgInit`, który robił `refreshGlass` (puls blur 22→14px) na WSZYSTKICH `.evoke-card`/`.evoke-panel` — przy każdej nawigacji szklane elementy (np. okienko wiadomości) mrugały. Skoro tło jest teraz `data-turbo-permanent` (stabilne), nowe karty malują się od razu na świeżym backdropie i pulsu NIE potrzeba. `turbo:load` re-inicjuje już TYLKO sticky-header. GOTCHA NA STAŁE: nie odświeżać szkła (`refreshGlass`) przy nawigacji Turbo — tylko przy pierwszym odsłonięciu tła-obrazu.
  - **„Szary ekran i część znika" w Ustawieniach → Wygląd**: app-shell z v0.99.11 (`md:overflow-hidden` na pojemniku + `main md:overflow-y-auto`) tworzył kontener przewijania/klipujący WOKÓŁ podglądów z `backdrop-filter` (podgląd szkła stron klienta) → glitch renderowania (szary prostokąt, znikające sąsiednie elementy) w Chrome/Safari. Fix: app-shell przez **STICKY zamiast overflow** — `md:sticky md:top-0` na `aside` (+`md:h-screen md:self-start`) i na `header` (`md:z-20`), strona przewija się normalnie (body). Sidebar/nagłówek dalej zawsze widoczne, a BRAK kontenera overflow = brak glitcha backdrop-filter. GOTCHA NA STAŁE: nie owijać treści admina w `overflow-*`, bo w środku są podglądy `backdrop-filter`.
- [x] Kropka powiadomienia jako badge w rogu ikony (v0.99.13): w pasie pionowym portalu kropka nowej wiadomości (`s.dot`) była osobnym elementem flex OBOK ikony → w zwiniętym pasie spychała ikonę z centrum. Teraz ikona jest owinięta `<span class="relative">` a kropka to `absolute -top-1 -right-1` (ring-brand-600 = „wycięcie" na tle pasa) — nie zajmuje miejsca w wierszu, ikona zostaje wyśrodkowana (zweryfikowane: iconCx==railCx). Dotyczy części `rail` w `_portal_nav.ejs`; pozostałe warianty (z etykietą) mają kropkę na końcu, tam ikona i tak nie skacze.
- [x] Badge nowej wiadomości na kafelku projektu w /c (v0.99.14): wątki wiadomości są rozdzielone per kontekst (`thread({projectId})` vs `thread({clientId, projectId:null})`), więc wiadomość agencji w PROJEKCIE nie pokazywała się na liście projektów `/c` — klient musiał wejść w każdy projekt. Teraz kafelek projektu dostaje badge (ikona `mail` + kropka w rogu), gdy jest oczekująca wiadomość: `client.controller.showClientPortal` liczy `p.hasNewMsg` = ostatnia wiadomość `out` w projekcie nowsza niż `msgSeen[project.clientToken]` (sesyjne, spójnie z resztą) — JEDNO zapytanie `message.findMany({projectId:{in},direction:'out'})` pogrupowane po projekcie (bez N+1). Badge na kafelkach aktywnych i archiwalnych (`client-portal.ejs`). Nagłówkowa „Wiadomości" (wątek kliencki) bez zmian.
- [x] Komunikator agencji (dwupanel) + inicjowanie rozmów + trwałe przeczytanie (v0.99.15): skrzynka `/admin/messages` przerobiona z akordeonu wątków na **dwupanel jak WhatsApp/Messenger** — lewa lista KLIENTÓW (nazwa, podgląd ostatniej wiadomości, czas, badge nieprzeczytanych), prawa = JEDEN strumień rozmowy klienta (bąbelki in/out) z **chipami kontekstu** („· Nazwa projektu"/„Transfer"). Agencja może teraz **ZAGAJAĆ** (wcześniej tylko odpowiadać na `:id/reply`): pole pisania z selektorem kontekstu `<select name="scope">` = Ogólne (`c`) / projekt (`p:<id>`) / transfer obecny w rozmowie (`t:<id>`), default = kontekst ostatniej wiadomości; checkbox „Powiadom mailem" (reuse `mail.sendClientReply`). **Trwałe** przeczytanie: otwarcie rozmowy (GET `?client=<id>`) i wysłanie → `messageService.markClientRead(clientId)` (DB `isRead`, nie sesyjne); badge maleje + `unreadCount` w menu odświeżany. Agregacja PER KLIENT (każda wiadomość ma `clientId`): `conversationList()` (grupowanie + unread), `conversation(clientId)` (strumień + include project/transfer do chipów), `send({clientId,projectId?,transferId?})`, `deleteClientConversation`. Trasy: `GET /messages?client=`, `POST /messages/:clientId/{send,delete}`, zostaje `GET /messages/:id/attachment` (usunięte `:id/{reply,read,delete}`). Widok responsywny (mobile: lista ↔ rozmowa + „← wróć"). Wysokość dwupanelu `md:h-[calc(100vh-8rem)]` z `overflow-y-auto` na panelach (w adminie BEZ backdrop-filter → overflow bezpieczny). Oferty są `clientId`-only (brak `offerId` na Message) → wpadają do „Ogólne". Test `test/messages-admin.test.js` + zaktualizowany `messages.test.js` (send zamiast reply). GOTCHA: `send` NIE dotyka `create` (klient→agencja bez zmian).
- [x] Punkty startu rozmowy z komunikatora (v0.99.16): brakowało wejścia, gdy klient nie miał jeszcze żadnej wiadomości (nie był na liście `conversationList`). Dodane: przycisk „Napisz" na kartotece klienta 360° (`/admin/messages?client=<id>`), „Napisz do klienta" na stronie projektu (`?client=<id>&scope=p:<projectId>` — pole pisania od razu w kontekście projektu), oraz selektor „+ Nowa rozmowa" w skrzynce (`allClients` z `clientService.options()` → wybór DOWOLNEGO klienta, także bez wiadomości). Kontroler `listMessages` czyta `?scope` (podpowiedź kontekstu) i przekazuje `scopeHint` do widoku (wygrywa nad domyślnym „kontekst ostatniej wiadomości"). Otwarcie klienta bez wiadomości = pusty strumień „Napisz pierwszy…" + pole pisania. Zweryfikowane E2E.
- [x] Formularz wiadomości klienta — czytelniejszy „Dołącz plik" + bez zbędnych pól (v0.99.17): w `public/messages.ejs` przycisk „Dołącz plik" z małego linka → wyraźny OBRAMOWANY (dashed) przycisk z ikoną chmury (łatwo było przeoczyć). Pola „Imię"/„E-mail" ukryte gdy nadawca ZNANY — w portalu klienta (/p, /c, /o) wiadomość niesie `clientId`, agencja wie kto pisze i odpisuje na `client.email`. Pola zostają TYLKO dla anonimowych transferów (/t, /upload bez projektu/klienta — tam e-mail bywa jedynym kanałem odpowiedzi). Flaga `msgKnownSender` przekazywana z kontrolerów (portal/client/offer = true; download/receive = `!!(transfer.project && transfer.project.clientId)`). Endpoint `create` bez zmian (senderName/Email dalej opcjonalne).
- [x] Composer wiadomości jak w komunikatorze (v0.99.18): w `public/messages.ejs` **Enter wysyła** wiadomość, **Shift+Enter = nowa linia** (`@keydown.enter` → `$event.target.form.requestSubmit()`; działa bo formularz jest `data-turbo="false"` i `requestSubmit` respektuje `required`); placeholder wprost tłumaczy skrót. Przebudowany dolny rząd: „Dołącz plik" jako kompaktowa OBRAMOWANA etykieta z ikoną `+` po LEWEJ (na mobile sam `+`, `sm:` z podpisem), po wyborze pliku chip z nazwą i „×" czyszczącym input (`$refs.file`), a przycisk „Wyślij" (brand) dosunięty do PRAWEJ (`ml-auto`) — jeden rząd zamiast pionowego stosu. Bez zmian w kontrolerach/endpoincie.
- [x] Enter wysyła w komunikatorze admina (v0.99.19): pole pisania w `admin/messages/index.ejs` wysyła wiadomość Enterem (Shift+Enter = nowa linia) — jak w portalu klienta. Handler w istniejącym bloku `<script>` (admin bez Alpine/Turbo): `textarea[data-enter-send]` łapie `keydown`, `Enter` bez Shift → `preventDefault` + `form.requestSubmit()` (respektuje `required` i `select` kontekstu). Placeholder tłumaczy skrót. CSP-safe (blok inline już był dla auto-scrolla strumienia).
- [x] Nowy wygląd maili (v0.99.20): przeprojektowany centralny szablon `wrap()` + `btn()` w `mail.service.js` (jedna zmiana → WSZYSTKIE maile). Zamiast pełnego brandowego paska u góry: cienki 4px brandowy pasek + logo/wordmark (kolor marki) na BIAŁYM nagłówku, hairline nad stopką, miękki cień, rogi 18px, jaśniejsze tło strony (#f4f4f7). Typografia: systemowy font stack (`-apple-system`…), body slate-700 `line-height 1.6`, nagłówki z ujemnym trackingiem. Przycisk większy (radius 12px). Stopka na `#fafafa` z hairline. NOWE: `preheader` (ukryty podgląd w skrzynce, domyślnie = heading) + `<meta color-scheme:light>` (stabilny wygląd w dark mode klienta pocztowego). Treści/placeholdery/szablony z Ustawień → E-mail bez zmian (tylko „opakowanie").
- [x] Motywy maili — select w Ustawieniach (v0.99.21): `emails.theme` (classic|minimal|rail|tint|badge, domyślnie classic) steruje wyglądem WSZYSTKICH wysyłanych maili — `wrap()` w `mail.service` rozgałęzia się na 5 szablonów, interfejs (`content`/`heading`/`preheader`) bez zmian, więc ZERO zmian w funkcjach maili. **classic** = pasek 4px + logo na białym (v0.99.20); **minimal** = bez karty/cienia, hairline (Stripe/Linear); **rail** = pionowy pasek marki przy lewej krawędzi; **tint** = nagłówek na jasnym tincie marki (`tintHex` miesza `colors.primary` z bielą — bez zależności od palety CSS); **badge** = wyśrodkowana ikona-badge (logo albo inicjał w tincie) + nagłówek (transakcyjny). Select (radio-karty `peer-checked`) w Ustawienia → E-mail nad tematami; zapis `mailTheme` w settings.controller (walidacja listy). Test `test/mail-theme.test.js` (stub transportera+settings: 5 rozróżnialnych szablonów, fallback classic). GOTCHA: `wrap` czyta `s.emails.theme` z ustawień — testy stubują `settingsService.get`.
- [x] Poprawki motywów maili do zgodności z mockupami (v0.99.22): (1) **broken-logo (ikonka „?")** — `wrap()` osadzał logo jako `<img>` z URL-em spod `logoPath` (app) nawet gdy to SVG (blokowane przez klienty pocztowe) albo spod `localhost` (nieosiągalne z zewnątrz). Teraz logo w mailu TYLKO gdy raster (`png|jpe?g|gif|webp`) — inaczej czysty wordmark tekstowy (jak na mockupach, zawsze się renderuje). (2) **Badge realnie wyśrodkowany** — `body(align)` dostał wariant `center`; motyw badge centruje NIE tylko nagłówek, ale i treść (`text-align:center` na wrapperze → tekst, box wartości i przycisk na środku). (3) **Stopki hairline** jak na mockupach A/B/C: `footerHair()` (cienka linia + padding) dla classic i badge (badge wyśrodkowany); szary pasek `footerBar` został tylko w tint (= mockup D). GOTCHA: `body` w `wrap()` jest teraz FUNKCJĄ `body(align)` — nie mylić z parametrem `body` w `sendClientReply` (inny scope). Test `mail-theme.test.js` bez zmian (markery classic/rail/badge dalej trafiają).
- [x] Box z kwotą w mailach jak na mockupie (v0.99.23): nowy helper `amountBox(primary, amount, subline, caption?)` w `mail.service` — brandowy tint (`tintHex`), DUŻA kwota (27px) w kolorze marki + muted subline + opcjonalna etykieta (nazwa pozycji). Zastąpił szare boxy `#f8fafc` w mailu OFERTY (muted intro „Przygotowaliśmy dla Ciebie ofertę **X**. Sprawdź…" + box „kwota / brutto · ważna do") i RETAINERZE (caption = label, subline „do zapłaty · termin"). W motywie `badge` box dziedziczy `text-align:center` z wrappera treści → kwota wyśrodkowana (zgodnie z mockupem C); w pozostałych motywach lewo. Bez zmian w interfejsie/testach (mail-theme 48/48).
- [x] Strona oferty /o — tekst nie rozjeżdża się w wąskim panelu (v0.99.24): kompozycje panel/panel-bg/sidebar pinują kartę do stałej szerokości (~448px) NIEZALEŻNIE od viewportu, więc breakpointy `sm:` (viewportowe) robiły 2 kolumny w wąskim panelu na szerokim ekranie → ucięte placeholdery inputów i zawijanie „Odrzucam / mam uwagi". Fix w `public/offer.ejs`: formularz decyzji STACKOWANY (inputy `w-full` w `space-y-3`, przyciski `flex-col` `w-full` — usunięte `sm:grid-cols-2`/`sm:flex-row`), bezpieczny przy każdej szerokości/kontenerze. Wiersz „Razem" rozbity na dwie linie (`Razem` + `w tym VAT X` jako block-subline) — koniec sierocego zawijania „zł)". GOTCHA: brak container-queries w projekcie → NIE polegać na `sm:` dla treści renderowanej w panelach o stałej szerokości; stackować albo detekować kontener. Zweryfikowane wizualnie (Playwright, cardSide=right @ 768/820/1024).
- [x] Logo maila osadzone inline — koniec „ładuje się wieczność" (v0.99.25): regresja z v0.99.22 (logo jako `<img src>` spod `appUrl`) kazała klientowi pocztowemu POBIERAĆ logo z serwera przy każdym otwarciu maila (wolno; przy localhost — w ogóle). Teraz `logoDataUri()` w `mail.service` wczytuje plik z `public/branding/` i osadza jako **data URI** (base64) wprost w treści maila → ładuje się natychmiast, bez fetchu, działa też spod localhost. SVG znów dozwolone (inline renderuje klient, który je wspiera — jak przed v0.99.22). Fallback: plik >48 KB (ryzyko przycięcia maila w Gmailu ~102 KB) albo brak → raster leci remote-URL, SVG/brak → wordmark tekstowy. GOTCHA: cap 48 KB — duże logo lepiej przekonwertować/zmniejszyć niż inline'ować (przycięłoby maila). Zweryfikowane: SVG → `data:image/svg+xml;base64,…`, zero `http://localhost` w src.
- [x] Edytowalna oferta (v0.99.26): `offer.service.update(id, {...})` — edycja KAŻDEJ oferty oprócz zaakceptowanej (odrzucona/wygasła/otwarta); podmienia pozycje (`OfferItem.deleteMany`+create), RESETUJE do `status:'open'` i czyści decyzję (`decidedAt/decisionName/decisionComment=null`) ZACHOWUJĄC token (ten sam link) → można wysłać poprawioną wersję ponownie. Zaakceptowanej nie ruszamy (ma już pozycje w rozliczeniach). `offer.service.itemsToText(items)` = odwrotność `parseItems` (do prefillu formularza; grosze niecałkowite → przecinek). Route `POST /clients/:id/offers/:oid` (`editOffer`, walidacja projektu jak w create, po `/send` i `/delete`). UI w 360°→Oferty (`clients/show.ejs`): przycisk „Edytuj" przy ofercie ≠ accepted → Alpine `startEdit(o)` wypełnia TĘ SAMĄ formę „Nowa oferta" (prefill przez `JSON.stringify` z `<%=`, GOTCHA jak w settings.ejs), `:action`/nagłówek/label zależne od `editId`, „Anuluj edycję"; `itemsText` prefill przez `itemsToText` doklejony w `client.controller` (map offers). Alert `off-edit`. Test `offers.test.js` (update reset+token, itemsToText round-trip, accepted→null).
- [x] Zapis ustawień zostaje na zakładce (v0.99.27): główny POST `/admin/settings` gubił zakładkę (redirect `?saved=1` → zawsze „Branding"). Fix: ukryte `<input name="tab" :value="tab">` w głównym formularzu (Alpine binduje bieżącą zakładkę) + kontroler czyta `req.body.tab` (whitelista `branding|wyglad|panel|pdf|email|advanced`) → redirect `?saved=1&tab=<tab>`. Pod-akcje trzymają swoją zakładkę: `apply-theme`→`wyglad`, `test-email`→`email`, `backup-auto`→`advanced` (`backups/:name/delete` już miał). GET `tabInit` czyta `?tab`. GOTCHA testowo-operacyjna (na przyszłość): NIE POST-ować częściowego formularza na `/admin/settings` do współdzielonej dev-DB — kontroler buduje PEŁNY obiekt z pól formularza, więc puste pola RESETUJĄ Settings (portalBilling→false itd.); weryfikować sam redirect/`Location` albo snapshot+pełny restore.
- [x] Kontekstowa nawigacja „wstecz" w adminie (v0.99.28): detal-strony miały zaszyty na sztywno „← Wszystkie X", więc wejście np. w projekt z karty klienta wracało do listy projektów, nie do klienta. Nowy `utils/backlink.js` `resolve(from)` — token `from=<typ>:<id>` (client/project) albo `board/calendar/sales`/`search:<zapytanie>` → `{href,label}` (whitelist typów → brak open-redirect; nieznany/pusty → null). Detal-kontrolery (`project.showProject`, `transfer.showTransfer`, `client.showClient`) ustawiają local `back`; widoki (`projects/show`, `transfers/show`, `clients/show`) renderują `back` gdy jest, inaczej dotychczasowy domyślny link (zero regresji przy wejściu z listy/bezpośrednio; w `transfers/show` `back` ma priorytet nad fallbackiem do projektu transferu). Linki wejściowe doklejają `from`: karta klienta→projekt/transfer `from=client:<id>`, projekt→transfer `from=project:<id>`, kanban `from=board`, wyszukiwarka `from=search:<q>` (`encodeURIComponent`). Test `backlink.test.js` (resolve + odrzucanie złych tokenów). Listy główne NIE dodają `from` (wejście z listy = „Wszystkie X").
- [x] Tabele admina → karty na mobile (v0.99.29): dwie jedyne strony z realnym `<table>` (`transfers/index`, `clients/index`) miały poziomy scroll (`overflow-x-auto min-w-[…]`). Wzorzec „tabela na desktopie / karty na mobile": kontener tabeli dostał `hidden md:block`, pod nim `md:hidden` lista kart (`rounded-xl border p-4`) iterująca ten sam zestaw. Transfery: karta = kierunek+tytuł+kłódka, pliki/rozmiar, pobrania, wygasa, chip statusu + data; **masowe zaznaczanie zostaje na desktopie** (karty bez checkboxów → brak duplikatu `input[name=ids]` w DOM, licznik/„zaznacz wszystkie" działają na ukrytych polach tabeli, na mobile pasek bulk się nie pojawia). Klienci: karta = nazwa/firma+status, e-mail/projekty/do zapłaty, link portalu + „Wyślij". Wymagał `build:css`. Zweryfikowane wizualnie (Playwright 390/1280 px): mobile=karty bez poziomego scrolla, desktop=tabela.
- [x] Portale bez przeładowania — Etap 2, slice 1: upload→lista w miejscu (v0.99.30): po wysłaniu plików w portalu `/p` lista i checklista odświeżają się BEZ reloadu przez **Turbo Streams** (`Turbo.renderStreamMessage`). `submitUpload` (portal.controller) gdy `Accept: text/vnd.turbo-stream.html` → przeładowuje projekt i renderuje `views/public/streams/upload.ejs` (`layout:false`, `Content-Type text/vnd.turbo-stream.html`) z akcjami `update` na stabilnych kontenerach: `#uploaded-files` (nowe pliki), `#fr-checklist` (odhaczone braki), `#fr-select` (select bez odhaczonej pozycji), `#upload-msg` (toast „Dziękujemy"). Fallback bez JS → dotychczasowy redirect `?sent=1`. Nowe partiale `_uploaded_files.ejs`/`_file_requests.ejs`/`_fr_select.ejs` (reużywane w renderze początkowym i streamie). `makeUploader` (`public/js/uploader.js`) po sukcesie finalize CZYŚCI stan (`files=[]`, `uploading/finalizing=false`) — wcześniej polegał na pełnym reloadzie; nieszkodliwe dla `/upload` (wciąż reload). `finalize()` w portal.ejs woła `renderStreamMessage` zamiast `window.location`. GOTCHA: stabilne kontenery MUSZĄ istnieć zawsze (nawet puste) — inaczej `update` nie ma celu przy pierwszym uploadzie. Zweryfikowane E2E (Playwright: plik w liście, toast, checklista, `reloaded:false`). Zakres dalej: proofing/płatność/oferta w miejscu (kolejne slice'y).
- [x] Regulacja szerokości panelu (panel / panel-bg) (v0.99.31): `Settings.layout.panelWidth` (sm|md|lg|xl|2xl, domyślnie md) — kontrolka „Szerokość panelu" w Ustawienia → Wygląd (natywne radia, `x-show` tylko dla „Panel na tle" / „…2"). W `layouts/public.ejs` `panelWCls` mapuje wartość na LITERALNE klasy Tailwind (`md:max-w-sm…2xl` — skaner je łapie, wymagał `build:css`) zastępując zaszyte `md:max-w-md` w obu sekcjach panel/panel-bg. `PANEL_WIDTHS` + walidacja w `settings.service.normLayout` i `settings.controller`. Zweryfikowane: każda wartość → właściwa klasa `md:max-w-*` w renderze /o.
- [x] Portale bez przeładowania — Etap 2, slice 2: proofing w /p w miejscu (v0.99.32): decyzja proofingu (Zatwierdzam / Zgłaszam poprawki) w portalu `/p` podmienia blok proofingu danego transferu BEZ reloadu (Turbo Streams). `_proofing.ejs` dostał local `turbo` (gdy true — formularze bez `data-turbo="false"`, Turbo przechwytuje submit; `/t` przez `download.ejs` zostaje na pełnym submicie). Portal owija include w `#proofing-<id>` i przekazuje `turbo:true`. `submitDecision` (portal.controller) gdy `Accept: turbo-stream` renderuje `views/public/streams/proofing.ejs` (`layout:false`) z akcją `update` na `#proofing-<id>` (przerenderowany `_proofing` w stanie po decyzji); fallback bez JS → redirect `?decided=1`. `setDecision` zwraca `approvalStatus/Comment/By/At` — wprost do partiala. Zweryfikowane E2E (approve → „Pliki zatwierdzone", `reloaded:false`, DB `approved`). GOTCHA testowa: plik-widmo z `mimeType image/*` → przeglądarka pobiera miniaturę nieistniejącego pliku i wywala stream; w testach dawać `application/pdf`.
- [x] App-shell dla panelu (panel / panel-bg): panel przewija się sam, pasek przeglądarki znika (v0.99.33): na DESKTOPIE (`md:`) kompozycje panelowe nie przewijają strony — `<body>`/wrapper dostają `md:h-screen md:overflow-hidden` (`shellBody`), łańcuch flex ancestorów `md:min-h-0` (`shellFlex`: wrapper, `relative flex-1 flex` railu, content-wrapper, `<main>`), a sam PANEL (`section.evoke-panel`) `md:h-full md:overflow-y-auto` → własny scrollbar w panelu, pas/tło stoją. Mobile bez zmian (przewija się `body` — wszystko pod `md:`, `shellBody/shellFlex` puste dla nie-panelMode → zero regresji w innych układach). GOTCHA: treść panelu w scroll-containerze — `justify-center` ucinał górę przy przepełnieniu (Safari); zmienione na `[justify-content:safe_center]` (centruje gdy się mieści, kotwiczy górę gdy nie — `build:css`). Zweryfikowane E2E (Playwright 1280px, długa oferta, solid+glass): `bodyScrolls:false`, `panelScrolls:true`, panelH==vh, brak glitcha szkła.
- [x] Portale bez przeładowania — Etap 2, slice 3: zgłoszenie płatności w /c w miejscu (v0.99.34): „Zgłoś wykonanie przelewu" w portalu `/c` podmienia przycisk na chip „Zgłoszono wpłatę…" + podziękowanie BEZ reloadu (Turbo Streams). Partial `_paid_declare.ejs` (chip albo form; local `turbo` → bez `data-turbo="false"`, `onsubmit=confirm()` dalej działa pod Turbo — return false anuluje submit). `client-portal.ejs`: `#paid-declare` (chip/form) + `#paid-flash` (podziękowanie) jako stabilne kontenery. `submitPaidDeclaration` (client.controller) gdy `Accept: turbo-stream` → `views/public/streams/paid.ejs` (`layout:false`, `update` na obu kontenerach); idempotencja (7-dniowy `paid_declared`) też zwraca chip streamem (bez ponownego logu); fallback bez JS → redirect `?paid=1#platnosci`. Zweryfikowane E2E (chip+podziękowanie, `reloaded:false`, event `paid_declared` w DB). Zostaje slice 4: decyzja oferty `/o`.
- [x] Portale bez przeładowania — Etap 2, slice 4: decyzja oferty w /o w miejscu (v0.99.35): „Akceptuję" / „Odrzucam (z komentarzem)" na publicznej stronie oferty `/o/:token` podmienia kartę oferty na stan po decyzji (podziękowanie / odrzucone / komunikat błędu walidacji) BEZ reloadu (Turbo Streams). Treść karty wydzielona do partiala `_offer_card.ejs` (reużywanego w renderze początkowym i w streamie — jak `_proofing`/`_paid_declare`); local `turbo` → formularz decyzji bez `data-turbo="false"` (Turbo przechwytuje submit; walidacja `:disabled` na Alpine dla „Odrzucam" zostaje). `offer.ejs` owija partial w stabilny kontener `#offer-card`. `submitDecision` (offer.controller) gdy `Accept: turbo-stream` renderuje `views/public/streams/offer.ejs` (`layout:false`, `update` na `#offer-card`): sukces → stan `done` (accepted/rejected, 200); odrzucenie bez komentarza → przerenderowany formularz z błędem (422); oferta już zdecydowana/wygasła → aktualny stan (200). Fallback bez JS → dotychczasowy redirect `?done=1` / render 400. GOTCHA: `decide` zwraca ofertę BEZ `include` — do streamu „done" przekazujemy pierwotny `offer` (ma `title`/`items`) + `state:decision`, bo widok podziękowania nie tyka pozycji. Test w `test/offers.test.js` (stream: 422 z błędem + formularz, 200 „Dziękujemy" + Charge, ponowna decyzja → aktualny stan); zweryfikowane E2E (Playwright in-process: akceptacja/odrzucenie/błąd bez reloadu, `reloaded:false`, DB accepted+Charge / rejected / open). **Etap 2 domknięty** (slice 1–4).
- [x] Rytm kontaktu (v0.99.36): pole `Client.contactEveryDays` (Int?, null=brak; migracja `20260725090000`) — indywidualny rytm „przypominaj o kontakcie co N dni". Select „Rytm kontaktu" (bez/7/14/30/60/90 dni) w formularzu nowego/edycji klienta; `client.service.normEvery` normalizuje (clamp 1..365, puste/poza zakresem → null). Widżet pulpitu „Do odezwania się" (`staleClients`) świadomy rytmu: próg per klient = jego `contactEveryDays` (fallback `days`=30 gdy brak), sort po największej zaległości WZGLĘDEM własnego rytmu (`over = dni − próg`), czerwień `overdue` = 2× próg (uogólnia dawne „60+ dni" — dla progu 30 wypada dokładnie 60); pozycja pokazuje „X dni bez kontaktu · rytm co N dni". Chip „rytm co N dni" na 360° (Przegląd). Bez zmian w „Przypomnij"/kalendarzu (dawny flow follow-up zostaje). Test w `test/crm.test.js` (próg per klient + fallback + czerwień 2×; normalizacja create/update). GOTCHA: stary test `staleClients({days:30})` dalej zielony — klient bez rytmu używa fallbacku 30, `sb.days` liczone jak dotąd.
- [x] Poprawki po testach rytmu kontaktu (v0.99.37): (1) **crash „Unknown argument `contactEveryDays`"** przy zapisie klienta na środowisku po deployu — `npm run prisma:deploy` robił tylko `migrate deploy` (stosuje migrację w bazie), ale NIE `prisma generate`, więc wygenerowany klient Prismy nie znał nowego pola. Skrypt `prisma:deploy` = teraz `prisma migrate deploy && prisma generate` (dokumentowana komenda deployu sama regeneruje klienta po migracji). Natychmiastowa naprawa istniejącego środowiska: `npm run prisma:generate` + restart. GOTCHA: po KAŻDEJ migracji dodającej pole klient MUSI być zregenerowany — inaczej `Unknown argument`. (2) **zaplanowany follow-up znikał z widżetu „Do odezwania się"** — dodanie interakcji na osi czasu (notatka/telefon/spotkanie) zapisuje `Event`, który zeruje licznik ostatniego kontaktu → klient wypadał z listy zaległych i chip „zaplanowane" był nieosiągalny (log interakcji „odświeżał" kontakt). Teraz `staleClients` pokazuje klienta z otwartym przypomnieniem NAWET gdy nie jest zaległy wg progu (`over>=0 || planned`); niezaległy-zaplanowany ma neutralny podpis „ostatni kontakt X dni temu" + chip „zaplanowane" (zaległych sortuje wyżej — `over` desc). Test w `test/crm.test.js` (świeży kontakt + otwarte przypomnienie → widoczny jako zaplanowany; świeży bez przypomnienia → poza listą).
- [x] Konfigurowalne PWA — panel jako aplikacja/okno (v0.99.38): panel i logowanie można zainstalować jako aplikację (Chrome/Edge „Zainstaluj", Safari macOS 14+ „Plik → Dodaj do Docka", telefon „Dodaj do ekranu głównego") → otwiera się we WŁASNYM oknie bez paska przeglądarki, z własną ikoną. Sterowane z Ustawienia → Zaawansowane → „Aplikacja (PWA)": przełącznik `enabled`, nazwa/krótka nazwa (puste = `appName`), kolor motywu (puste = `colors.primary`), tło startowe, tryb okna (standalone/minimal-ui/fullscreen/browser), wgrywana ikona (kwadrat, najlepiej 512×512 PNG). Model: kolumna `Settings.pwa` (JSON, migracja `20260727120000`), `normPwa` w settings.service (walidacja display z whitelisty + hex, clamp nazw). Manifest budowany DYNAMICZNIE z ustawień: `GET /manifest.webmanifest` (`utils/pwa.manifest`, `application/manifest+json`, `start_url:/admin`, `scope:/`; wgrana ikona rastrowa → 192/512+maskable, ZAWSZE skalowalny fallback), ikona zastępcza `GET /pwa/icon.svg` (brandowy kwadrat z inicjałem nazwy, `readableText` auto-kontrast). Service worker `public/sw.js` — minimalny, ŚWIADOMIE bez cache (apka dynamiczna/SSR/świeży CSS po deployu; SW tylko dla instalowalności, zero ryzyka nieświeżej strony). Tagi `<head>` (manifest + apple-meta + rejestracja SW) przez `res.locals.pwaHead` TYLKO w layoutach `admin`/`auth` i TYLKO gdy `pwa.enabled` (strony klienta /c//p//t BEZ manifestu — to „aplikacja agencji", nie klienta). Upload ikony przez `brandingUpload` (pole `pwaIcon`, sanityzacja SVG jak reszta brandingu). CSP bez zmian — `manifest-src`/`worker-src` dziedziczą `default-src 'self'` (manifest, /sw.js, inline rejestracja przez istniejące `script-src 'unsafe-inline'`). GOTCHA: `apple-touch-icon` wskazuje wgraną ikonę RASTROWĄ albo favicon (iOS ignoruje SVG jako apple-touch — SVG zostaje w manifeście dla reszty). Test `test/pwa.test.js` (manifest z ustawień, PNG→192/512+maskable, fallback SVG, normalizacja, tagi tylko gdy włączone, SW z roota); render check E2E (link+SW w panelu i logowaniu gdy włączone, brak gdy wyłączone). UWAGA: to WŁĄCZANE (domyślnie OFF) — nie zmienia zachowania istniejących instalacji do czasu włączenia w panelu.
- [x] Fix nawracającego artefaktu „szkła" na panel/panel-bg — TERAZ U ŹRÓDŁA (v0.99.39): prawdziwa przyczyna to `.evoke-panel` (glass, `backdrop-filter`) będąca ZARAZEM kontenerem przewijania (`md:overflow-y-auto`, wprowadzone app-shellem v0.99.33) NAD tłem `position:fixed`. Chrome dla elementu z `backdrop-filter`, który jest własnym scrollerem nad fixed-tłem, trzyma NIEŚWIEŻĄ próbkę backdropu → prostokątny artefakt „szkła" po repaintcie/hoverze (GPU-zależny, „nie zawsze", głównie panel-bg + rail-right; zgłoszone przez usera na realnym Chrome). Fix: rozdzielenie — `.evoke-panel` przestaje być scrollerem (`overflow` zdjęty, zostaje `md:h-full flex flex-col`), a przewijanie przenosi się na WEWNĘTRZNY przezroczysty wrapper (`flex-1 min-h-0 md:overflow-y-auto` + padding `px-6 sm:px-9 py-7`). Szkło stoi w miejscu → backdrop próbkowany stabilnie; app-shell (panel przewija się sam, pasek przeglądarki znika) BEZ zmian. Dotyczy obu kompozycji `panel` i `panel-bg`; sticky-logo/nav/hero i `[justify-content:safe_center]` przeniesione do wrappera bez zmiany zachowania. `refreshGlass` (bg-rotate.js, pierwsze odsłonięcie tła) zostaje — to inny mechanizm. Zweryfikowane E2E (Playwright: `.evoke-panel` overflow-y=visible + backdrop-filter obecny, wewnętrzny wrapper overflow-y=auto i realnie przewija, body bez scrolla = app-shell zachowany; screenshot bez regresji układu). **GOTCHA NA STAŁE**: NIGDY nie dawać `overflow-*` na element z `backdrop-filter` — scroll trzymać na osobnym, przezroczystym wrapperze. Artefakt jest GPU-zależny (headless/software-rendering go NIE pokazuje) → finalne potwierdzenie na realnym Chrome usera.
- [x] Artefakt „szkła" NAPRAWDĘ u źródła (ziarno = mix-blend) + ikona PWA + splash (v0.99.40): (1) **Prawdziwa przyczyna artefaktu** (v0.99.39 pudłował): ziarno to warstwa `mix-blend-mode: soft-light` (`background.js`), a `mix-blend-mode` w tle + `backdrop-filter` panelu = nawracający GPU-zależny artefakt „nieświeżej próbki backdropu" w Chrome (prostokąt za szkłem, TYLKO przy włączonym ziarnie — user to wychwycił). Fix U ŹRÓDŁA: `#evoke-bg` dostaje `isolation:isolate` → cały stos tła (baza + obraz + ziarno) spłaszcza się w JEDNĄ odizolowaną grupę PRZED próbkowaniem przez backdrop-filter → stabilna próbka. Dla tła NIE-obrazkowego z ziarnem baza (gradient/kolor) renderuje się teraz jako warstwa WEWNĄTRZ `#evoke-bg` (żeby ziarno miało z czym mieszać w izolowanej grupie); bez ziarna zero zmian (baza zostaje na `<body>`). `isolation` nie łapie `position:fixed`-dzieci jako containing block (tylko transform/filter), więc warstwy dalej kryją viewport. Scroll-decoupling z v0.99.39 ZOSTAJE (sensowne utwardzenie, ale NIE ono było przyczyną). **GOTCHA NA STAŁE**: `mix-blend-mode` w warstwie tła + `backdrop-filter` = artefakt → tło izolować (`isolation:isolate`) i trzymać bazę+blend w tej samej grupie. (2) **Ikona PWA obcięta**: wgrana ikona była deklarowana też jako `maskable` → system przycinał ją do koła/squircle (obcięte szerokie logo). Teraz wgrana ikona = tylko `any` (bez przycięcia), a `maskable` dostarcza pełnokwadratowy fallback SVG (`iconSvg` full-bleed bez `rx`, inicjał w strefie bezpiecznej font 260/512). Podgląd w Ustawieniach `object-contain` (realny kształt) + podpowiedź „wgraj kwadrat z marginesem". (3) **Splash**: auto-splash z manifestu (Android/desktop/macOS: ikona `any` 512 na `background_color` + nazwa) — działa dzięki poprawnej ikonie `any` i `background_color`; pole doprecyzowane na „Tło ekranu startowego (splash)". iOS home-screen wymagałby per-urządzenie `apple-touch-startup-image` (poza zakresem — bez pipeline'u obrazów rastrowych). Test `test/pwa.test.js` zaktualizowany (wgrana ikona 192/512 `any`, `maskable` na fallbacku). Artefakt GPU-zależny (headless go nie pokazuje) → finalne potwierdzenie na realnym Chrome usera.
- [x] Fix ikony PWA (regresja v0.99.40) + splash z opóźnieniem (v0.99.41): (1) **Ikona „przestała działać"**: przy WGRANEJ ikonie manifest zawierał też skalowalny fallback SVG (`/pwa/icon.svg`), a Chrome WOLI wektor → podmieniał ikonę usera na generyczny inicjał; dodatkowo łączony `purpose:"any maskable"` bywał ignorowany przez Safari (nie zna `maskable`) → brak ikony. Fix: fallback SVG dokładany TYLKO gdy brak wgranej ikony (`if(uploaded){…}else{svg}`); `maskable` usunięty CAŁKOWICIE (był też przyczyną „obcięcia" z v0.99.40 — system przycinał do koła/squircle). Wgrana ikona = wyłącznie `any` (192/512), pełnokwadratowy SVG-fallback = `any` (nic nie przycięte). GOTCHA NA STAŁE: nie mieszać SVG-fallbacku z wgranym rastrem w jednym manifeście (wektor wygrywa w Chrome, podmienia ikonę); `maskable`/łączony purpose bywa niekompatybilny (Safari ignoruje ikonę). (2) **Splash z opóźnieniem** (`pwa.splashMs`, domyślnie 1200 ms, 0=wyłącz; `normPwa` clamp 0..5000): własny ekran startowy (ikona+nazwa na „tle startowym") pokazywany PO otwarciu ZAINSTALOWANEJ aplikacji przez zadany czas, potem fade — natywny splash z manifestu bywa błyskiem przy szybkim starcie. `pwa.splashHtml` → overlay w `<body>` (layouty admin/auth) przez `res.locals.pwaSplash`; inline JS pokazuje go TYLKO w `display-mode: standalone` i RAZ na uruchomienie (`sessionStorage`), w przeglądarce natychmiast usuwany (bez błysku), przy kolejnych nawigacjach się nie powtarza. Pole „Ekran startowy — czas (ms)" w Ustawienia → Zaawansowane (`pwaSplashMs`, `clampInt`). Test `test/pwa.test.js` (brak SVG obok wgranej ikony, zero maskable, overlay gdy standalone/splashMs>0, clamp splashMs). Regresja ikony była na realnym Chrome/Safari — headless jej nie łapie.
- [x] Splash: wybór zawartości (logo/nazwa, nie tylko ikona) + przywrócone zaokrąglone rogi ikony (v0.99.42): (1) **Rogi ikony**: fallback SVG z v0.99.40 był full-bleed (bez `rx`) pod `maskable` — po usunięciu maskable (v0.99.41) w Chrome na desktopie wychodziły ostre, kanciaste rogi. Przywrócone `rx=96` (zaokrąglone); teraz ikona jest `purpose:'any'` only, więc zaokrąglenie jest bezpieczne i pożądane. (2) **Inny splash niż ikona** (`pwa.splashMode`: `icon` | `logo` | `name`, domyślnie `icon`): „logo" pokazuje logo z brandingu wyśrodkowane (na ciemnym tle bierze wariant `logo.darkPath`, żeby było widoczne), „name" samą nazwę (duży wordmark), „icon" ikonę+nazwę jak dotąd. `splashHtml` rozgałęzia się na tryby; walidacja `PWA_SPLASH_MODES` w `normPwa` + kontrolerze. Select „Zawartość ekranu startowego" obok pola czasu w Ustawienia → Zaawansowane. Test `test/pwa.test.js` (3 tryby + wariant dark-logo); zweryfikowane wizualnie (Playwright z wymuszonym `display-mode:standalone`: overlay `display:flex`, tło startowe + auto-kontrast tekstu).
- [x] Osobne logo PWA składane w ikonę z maską + rozmiar (v0.99.43): rozwiązuje „nadal brak maski na ikonie" — pole `pwa.logoPath` (osobny upload „Logo aplikacji (PWA)", `brandingUpload` field `pwaLogo`, sanityzacja SVG) + `pwa.logoScale` (30..90%, domyślnie 62). Ikona jest GENEROWANA jako SVG: kolor marki + wyśrodkowane logo osadzone jako **data URI** (`logoDataUri` czyta plik z `public/branding/`, base64; cap 1.5 MB, `basename` anti-traversal) — brak konkurencji z rastrem, więc Chrome nie podmienia jej na wektor. Dwa OSOBNE wpisy w manifeście (NIE łączony purpose — Safari używa `any`, ignoruje `maskable`): `/pwa/icon.svg` = `any` zaokrąglony (rx=96, desktop), `/pwa/icon.svg?mask=1` = `maskable` full-bleed (rx=0, mobile OS nakłada maskę). Logo w wariancie maskable zmniejszone ×0.8 (strefa bezpieczna → maska nic nie tnie). `iconSvg(s,{maskable})` osadza logo przez `<image href+xlink:href>` (`preserveAspectRatio` zachowuje proporcje) albo inicjał, gdy brak logo. Priorytet: `logoPath` (składana) > `iconPath` (gotowa ikona 1:1, `any` only — teraz w `<details>` „Zaawansowane") > inicjał. Trasa `/pwa/icon.svg` `no-cache` + wariant `?mask=1` (controller czyta `req.query.mask`). Podgląd w Ustawieniach = złożona ikona (`/pwa/icon.svg?v=<logo|scale|kolor>` cache-bust). Splash tryb „logo" i apple-touch-icon też korzystają z `pwa.logoPath`. Test `test/pwa.test.js` (logo→2 warianty, data-URI, rx any/maskable, clamp scale) + **rasteryzacja w Chromium** (Playwright: osadzone logo faktycznie renderuje się w obu wariantach — kluczowe, bo nested `<image>` w SVG bywa zawodny). GOTCHA: logo osadzać jako data URI (nie zewn. ref — SVG-ikony nie ładują zewnętrznych zasobów); maskable full-bleed + `any` zaokrąglony to OSOBNE wpisy (łączony purpose psuł Safari).
- [x] PWA: wielkość grafiki splashu + „Logo aplikacji" w selekcie + gotowa ikona jako override (v0.99.44): (1) `pwa.splashSize` (48..280 px, domyślnie 120; `normPwa` clamp) — regulacja wielkości ikony/logo na ekranie startowym (pole „Wielkość grafiki" w Ustawienia → Zaawansowane); ikona = kwadrat `sz×sz` (radius ×0.22), logo = `max-height:sz` (max-width 80%). (2) Select „Zawartość splashu": „Logo (z brandingu)" → **„Logo aplikacji"** (tryb `logo` bierze najpierw `pwa.logoPath`, potem logo brandingu). (3) **Odwrócony priorytet ikony**: gotowa ikona (`iconPath`) NADPISUJE składane logo (było `logoPath` > `iconPath`; teraz `iconPath` > `logoPath` > inicjał) — `manifest` używa `iconPath` 1:1 gdy wgrana, inaczej ikonę składaną z `logoPath`/inicjału; podgląd w Ustawieniach pokazuje FINALNĄ ikonę (override albo złożoną). Etykiety UI/komentarze zaktualizowane. Test `test/pwa.test.js` (override `iconPath` vs samo logo; `splashSize` px na ikonie/logo + clamp).
- [x] PWA: rozmiar logo do 100% + wielkość grafiki splashu bez górnego limitu (v0.99.45): `logoScale` clamp 30..90 → 30..**100** (logo może wypełnić ikonę; wariant maskable dalej ×0.8 = margines na maskę). `splashSize` traci górny limit (było 48..280 → teraz min 48 bez maksa; pole `number` bez `max`); w `splashHtml` grafika dostaje caps `max-width:88vw;max-height:74vh` + `object-fit:contain`, więc dowolnie duży px nie wylewa się poza ekran. Logo na splashu = `height:sz` (explicit zamiast `max-height`). Testy `pwa.test.js` zaktualizowane (logoScale→100, splashSize bez maksa).
- [x] PWA maska — powrót do prostego rozwiązania (v0.99.48; cofnięte v0.99.46/47): na życzenie właściciela („jest źle, przywróć v0.99.45 i po prostu dodaj maskę") COFNIĘTE zmiany z v0.99.46 (osobna trasa `/pwa/icon-maskable.svg`, źródło maskable `logoPath||iconPath`, podpowiedzi o reinstalacji) i v0.99.47 (`<details open>` + chip „· wgrana"). Pliki PWA (`utils/pwa.js`, `pwa.controller.js`, `public.routes.js`, `settings.ejs`, `test/pwa.test.js`) przywrócone do v0.99.45. Jedyna zmiana na plusie: w `manifest()` wpis **`maskable` (`/pwa/icon.svg?mask=1`) jest teraz ZAWSZE**, nie tylko gdy brak gotowej ikony (wcześniej override `iconPath` gubił maskę). `iconSvg` bez zmian (składa z `logoPath`, na kolorze marki: `any`=rx96 zaokrąglony, `maskable`=rx0 full-bleed w strefie bezpiecznej). Efekt: `any` = gotowa ikona 1:1 albo składana z logo; `maskable` = zawsze składana z logo/inicjału (przy gotowej ikonie bez logo maska pokaże inicjał — dla własnej maski użyć „Logo aplikacji"). Minimalny diff vs v0.99.45 (tylko `pwa.js`, 4/-7). GOTCHA (nauczka): przy wielu iteracjach nad jednym feature — nie mnożyć wariantów; wracać do znanego dobrego commita i robić najmniejszą możliwą zmianę.
- [x] PWA: rozdzielenie ikony i logo splashu (v0.99.49): naprawia realny błąd modelu — dotąd „Logo aplikacji" (`logoPath`) SKŁADAŁO ikonę, a „gotowa ikona" (`iconPath`) była osobnym override → nie dało się ustawić ikony i logo splashu niezależnie („ikona zawsze robiła się z loga"). Teraz jasno i osobno: **`iconPath` = „Ikona aplikacji"** → z niej ZAWSZE składamy ikonę (`any` zaokrąglony + `maskable` full-bleed, na kolorze marki, rozmiar `logoScale`); **`logoPath` = „Logo na ekranie startowym (splash)"** → używane WYŁĄCZNIE na splashu (tryb „Logo aplikacji"), NIE dotyka ikony. Zmiany: `iconSvg` osadza `iconPath` (było `logoPath`); `manifest` = ZAWSZE 2 składane wpisy `/pwa/icon.svg` (+`?mask=1`), niezależnie od pól (żadna wgrana grafika nie trafia wprost do manifestu → brak konkurencji SVG↔raster); splash tryb „ikona" = złożona ikona (`/pwa/icon.svg`), tryb „logo" = `logoPath`||logo brandingu; apple-touch = raster `iconPath`||favicon (bez logo splashu). UI: dwie osobne sekcje (Ikona aplikacji + suwak rozmiaru; Logo na splash) zamiast „Logo aplikacji"+`<details>` gotowa ikona. Kontroler/multer BEZ zmian (`pwaIcon`→iconPath, `pwaLogo`→logoPath — zmieniła się tylko semantyka użycia). Test `pwa.test.js` (ikona składa się z `iconPath`, NIE z logo splashu; manifest zawsze 2 wpisy niezależnie od pól). GOTCHA NA STAŁE: `logoPath` = logo SPLASHU (nie ikony); ikonę składa `iconPath`.
- [x] Restyling panelu — motyw „modern" (styl TailAdmin), Etap 1 = fundament + szybkie wygrane (v0.99.50): PRZEŁĄCZALNY wygląd panelu admina — obecny „classic" zostaje DOMYŚLNY i NIETKNIĘTY, „modern" to opcja. Mechanizm jak dark mode: klasa `theme-modern` na `<html>` (+ warstwa nadpisań CSS w `input.css`) czytająca TE SAME zmienne kolorów (`--admin-*`, `brand-*`) → personalizacja kolorów działa jak dotąd (zweryfikowane: modern renderuje z niestandardowym kolorem brand). Zakres Etapu 1: sidebar — aktywna pozycja jako pełna pigułka w kolorze marki (biały tekst + miękki cień; classic ma pastelowe `bg-brand-50`); nagłówek — czystsza linia + zaokrąglona szukajka; karty treści (`main .rounded-2xl`) — spójny miękki cień/ramka; tabele — nagłówek slate + hover wierszy. Model: `Settings.layout.adminTheme` (classic|modern) + `hideScroll` (bool) — BEZ migracji (JSON w layout); `normLayout` waliduje z whitelisty (`ADMIN_THEMES`), koercja bool. Wstrzyknięcie klas: `app.js` → `res.locals.adminBodyClass` (`theme-modern`/`hide-scrollbars`), `layouts/admin.ejs` → `<html class>` (współistnieje z `dark`: `class="theme-modern dark"`). **Ukrycie pasków przewijania** (`hide-scrollbars`) — niezależne od motywu, działa też w „classic" (Firefox `scrollbar-width`, WebKit `::-webkit-scrollbar`). UI: sekcja „Wygląd panelu" na górze zakładki Ustawienia → Panel (natywne radia motywu + checkbox ukrycia pasków; zapis głównym POST `/admin/settings`). Login (`auth.ejs`) NIE ruszany — niezależna personalizacja logowania = Etap 3. Test `test/admin-theme.test.js` (normLayout: whitelista/bool/domyślne; render `/admin`: klasy na `<html>` tylko gdy ustawione, classic bez nich = regresja). Zweryfikowane E2E (Playwright: classic vs modern vs modern+niestandardowy kolor vs modern+dark). ROADMAPA: Etap 2 = reskin ekranów pod modern (kafelki statystyk z trendem, kalendarz, chat/inbox, tabele/formularze); Etap 3 = profil + task list/kanban + pełna niezależna personalizacja logowania; Etap 4 = konta+role + 2FA passkeys.
- [x] Restyling panelu — Etap 2, slice 1: zwijane lewe menu (v0.99.51): pasek boczny panelu można zwinąć do samych ikon (styl TailAdmin). Przełącznik = hamburger w nagłówku (na DESKTOPIE zwija/rozwija pasek, na MOBILE otwiera offcanvas jak dotąd — `@click="innerWidth<768 ? nav=true : toggleRail()"`). Mechanizm jak dark mode: klasa `html.rail-collapsed` dokładana **inline-skryptem w `<head>`** (czyta `localStorage['evoke-admin-rail']`) → BRAK migotania (szerokość poprawna przed pierwszym malowaniem; zweryfikowane: reload z zapamiętanym stanem startuje od razu zwinięty), globalny `window.toggleRail` persystuje stan. NIEZALEŻNE od motywu (działa i w classic, i w modern). CSS (`input.css`, desktop-only `@media min-width:768px`): `[data-admin-sidebar]` 15rem→4rem (`transition: transform+width .3s`), `[data-rail-label]` display:none, `[data-rail-head]`/`[data-rail-item]` justify-center, `[data-rail-badge]` → mała kropka na ikonie (nie tracimy sygnału „są nowe"). Markery w `layouts/admin.ejs`: `data-admin-sidebar` na `<aside>` (+`overflow-hidden`), `data-rail-item`/`data-rail-label` na pozycjach menu (ikona wydzielona `shrink-0` od chowanej etykiety), `title` = tooltip po zwinięciu; stopka „Konto i hasło"/„Wyloguj" dostała ikony (`cog`/`arrowRight`) + `data-rail-item`. Modern: aktywna pigułka po zwinięciu = brandowy kwadrat z ikoną (selektor pigułki rozszerzony na `aside a.bg-brand-50` — obejmuje też link konta w stopce). Mobile bez zmian (offcanvas w pełnej szerokości — `rail-collapsed` md-only, zweryfikowane: 240px na 390px viewport). Test w `test/admin-theme.test.js` (markery paska + `toggleRail` w layoucie). Zweryfikowane E2E (Playwright: 240→64px po kliknięciu, persist, FOUC-free reload, classic+modern).
- [x] Restyling panelu — Etap 2, slice 2: kafelki statystyk „modern" jako badge (v0.99.52): w motywie „modern" ikona w rogu kafelka KPI (`_widgets/_stat.ejs`) staje się brandowym badge'em (kwadrat 40×40, `border-radius:12px`, tło = tint marki `rgb(brand-600/0.10)`, ikona w kolorze marki) — sygnaturowy look TailAdmin. CSS-only pod `.theme-modern [data-stat-icon]` (marker `data-stat-icon` dodany do obu gałęzi `_stat.ejs` — classic go IGNORUJE, kafelki bez zmian: mała ikona slate w rogu). Dark+modern: mocniejszy tint (0.22) + ikona `brand-400` (kontrast na ciemnej powierzchni). Kafelki i tak dziedziczą miękki cień karty z Etapu 1 (`main .rounded-2xl`). Kafelki Pulsu (`pulse.ejs`) mają inną strukturę (nagłówek+wartość, bez ikony w rogu) — dostają sam cień karty, bez badge'a. Zweryfikowane E2E (Playwright: badge w modern, classic bez zmian, dark).
- [x] Fix zwijanego paska: stała wysokość pozycji (v0.99.53): po zwinięciu wiersz (sama ikona 16px) był niższy niż rozwinięty (ikona + tekst, line-height 20px) → „skok"/dziwna zmiana wysokości przy zwijaniu (zgłoszone przez usera). Fix: `[data-admin-sidebar] [data-rail-item] { min-height: 2.25rem }` — równa oba stany (jak `h-9` w pasku portalu). Zweryfikowane: wysokości pozycji identyczne rozwinięty=zwinięty (36/36/36 px).
- [x] Restyling panelu — Etap 2, slice 3: reskin chatu/skrzynki „modern" (v0.99.54): dwupanel `/admin/messages` w stylu TailAdmin. **Awatary** (inicjał klienta) w liście rozmów i nagłówku rozmowy — TYLKO modern (element ma klasę `hidden` → `.theme-modern [data-avatar]{display:inline-flex!important;...kółko z tintem marki}`; classic pixel-identyczny, bez awatarów). Aktywna rozmowa: pastel `bg-brand-50` + **akcent marki po lewej** (`box-shadow: inset 3px 0 0 brand-600` na `.theme-modern [data-conv-list] a.bg-brand-50`; marker `data-conv-list` na `<aside>` listy). Markup: pozycja listy owinięta w `flex items-center gap-3` + `data-avatar` (w classic `hidden` → jedno widoczne dziecko, gap bez efektu = zero zmian); nagłówek dostał `data-avatar` przed nazwą. **GOTCHA (naprawione)**: pierwotny selektor pigułki sidebara `.theme-modern aside a.bg-brand-50` PRZECIEKAŁ na listę rozmów (inne `<aside>` w treści) → aktywna rozmowa robiła się solidną pigułką; zscope'owane do `.theme-modern [data-admin-sidebar] …`. **Dark (fix kontrastu, classic+modern)**: aktywna rozmowa nie może zostać na jasnym `bg-brand-50` (jasny tekst nieczytelny) → `html.dark [data-conv-list] a.bg-brand-50{background:rgb(brand-600/0.22)!important}`. Zweryfikowane E2E (Playwright, seed 2 klientów+wiadomości ze sprzątaniem): modern=awatary+akcent, classic=bez zmian, dark=czytelny.
- [x] Restyling panelu — Etap 2, slice 4: composer wiadomości (ikonki + załącznik) + fix zawijania paska (v0.99.55):
  - **Fix zawijania etykiet paska**: przy rozwijaniu label pojawia się (display) ZANIM szerokość paska urośnie (transition) → w wąskiej kolumnie łamał się na 2 linie („Puls Agencji" itp.). `white-space: nowrap` na `[data-rail-item]`/`[data-rail-head]` + istniejący `overflow-hidden` na aside = etykieta czysto wysuwa się/chowa bez łamania.
  - **Composer komunikatora `/admin/messages` jak w referencji (TailAdmin)**: „dołącz" (ikona chmury) po lewej | zaokrąglone pole | „wyślij" (kółko marki z ikoną) po prawej; kontekst + „Powiadom mailem" w kompaktowym rzędzie nad polem. Chip wybranego pliku z „×". UNIWERSALNE (classic i modern — to poprawa funkcji, nie skin).
  - **Załącznik w wiadomości agencji (end-to-end)**: `message.service.send` przyjmuje `file` (jak `create`; dozwolona wiadomość sam-plik `body=''`); trasa `/admin/messages/:clientId/send` dostała `messageUpload` (multer, reuse), kontroler przekazuje `req.file`. Klient POBIERA załącznik agencji: nowy `attachmentInThread(msgId, scope)` (TEN SAM filtr co `thread` + id + plik → brak IDOR), 5 tras publicznych `GET /:surface/:token/wiadomosci/:msgId/attachment` (/c /p /t /upload /o; handlery reużywają resolverów token→kontekst + bramkę hasła transferu), `public/messages.ejs` renderuje załącznik jako LINK pobrania (`msgContext.page/:id/attachment`, `data-turbo="false"`). Wcześniej klient widział TYLKO nazwę pliku (tekst) — teraz pobiera.
  - Test `test/message-attachment.test.js` (agencja wysyła plik multipartem → klient pobiera ze swojego wątku, bajty zgodne; obcy token = 404 = IDOR zablokowany). Composer zweryfikowany E2E (Playwright: chmura+pole+kółko wysyłki, classic i modern).
- [x] Restyling panelu — Etap 2, slice 5: reskin kalendarza „modern" (v0.99.56): siatka miesiąca `/admin/calendar` bliżej TailAdmin. Modern (theme-gated, markery w widoku): nagłówek dni na slate + WERSALIKI (`data-cal-head`), kafle wydarzeń jako pigułki z AKCENTEM po lewej w kolorze kafla (`.cal-chip` + `border-left: 3px solid currentColor` — kolor = tekst kafla, więc każdy typ dostaje swój akcent), delikatne podświetlenie „dziś" (`[data-cal-cell][data-today]` → tint marki 0.05). Classic bez zmian (markery obojętne). **Dark (fix, classic+modern)**: komórki spoza miesiąca to `bg-slate-50/60` — modyfikator krycia NIE łapie override'u `.bg-slate-50`, więc świeciły jasnym blokiem na ciemno; `html.dark .bg-slate-50\/60 { background: rgba(255,255,255,.03) }` stonował. Zweryfikowane E2E (Playwright, seed 5 przypomnień: modern=slate/akcenty/dziś, classic=bez zmian, dark=komórki stonowane).
- [x] Kalendarz: widoki tydzień + dzień (v0.99.57): dołożone do dotychczasowego miesiąca (zgłoszony brak — referencja TailAdmin ma month/week/day). Przełącznik **Miesiąc / Tydzień / Dzień** w nagłówku (`?view=month|week|day`, data referencyjna `?date=YYYY-MM-DD`). `calendar.controller.index` rozgałęzia się na 3 widoki: liczy zakres (miesiąc / pn–nd tygodnia / doba), nawigację prev/next per widok (miesiąc ±1, tydzień ±7 dni, dzień ±1), etykiety PL (tydzień „13–19 lipca 2026", dzień „środa, 15 lipca 2026"), `viewDate` do linków przełącznika i `isToday` do „Dziś". Wszystkie 3 używają istniejącego `calendar.eventsInRange(from,to)` (zero nowych zapytań/serwisu). Widok tygodnia = 7 kolumn (nagłówek dnia `data-cal-head` + wszystkie wydarzenia z prefiksem godziny); dzień = agenda chronologiczna (godzina + kolorowy pasek priorytetu + tytuł, sortowane po czasie). Alpine `calendar()` bez zmian (mapy `REM`/`EVBYDAY` budowane z `allDays` = weeks∪days), modale/menu/D&D działają w każdym widoku. **Powrót po zapisie zachowuje widok**: formularze niosą hidden `view`+`date`, `back()` i `action()` (menu) je honorują (miesiąc→`?month=`, tydzień/dzień→`?view=&date=`). Chipy dziedziczą modern (`cal-chip` akcent, `data-cal-head` slate). Zweryfikowane E2E (Playwright: tydzień 7 kolumn z godzinami, dzień agenda; wszystkie 4 URL-e = 200).
- [x] Puls: kafelki finansowe „modern" + trend (v0.99.58): kafelki KPI Finansów (`pulse.ejs`) dostały brandowy badge ikony w rogu (`data-stat-icon` — CSS ze slice 2, modern; classic = mała ikona slate) i miękki cień karty (Etap 1). Ikony: przychód `trendingUp`, do zapłaty `banknote`, przeterminowane `clock`, MRR `activity`. Kafelek przychodu zachowuje PRAWDZIWY trend miesiąc/miesiąc (▲/▼ % + „od zeszłego miesiąca (X)") — jedyny naturalny delta m/m; pozostałe kafle to migawki punktowe (do zapłaty/przeterminowane/MRR), więc BEZ fabrykowanego trendu (uczciwie). Kafle Dostarczania/Klientów bez zmian (drugorzędne, kompaktowe). Zweryfikowane E2E (Playwright, seed przychodu: badge trendu +25%, ikony-badge, wykres).
- [x] Kalendarz: siatka godzin + długość przypomnienia (v0.99.59): widoki tydzień/dzień przerobione z list na **siatkę godzin 0–24** (44px/godz, scroll do 7:00). Przypomnienia = bloki pozycjonowane wg godziny startu i **długości** (nowe pole `Reminder.durationMin`, migracja `20260729120000`); terminy płatności/wygasania transferów = pasek „cały dzień" u góry (`splitDay` w kontrolerze). Nakładanie: proste zachłanne kolumny (`layoutTimed` — top/height/colIndex/colCount liczone SERWEROWO, blok pozycjonowany inline-stylem). Linia „teraz" (czerwona) w dniu dzisiejszym (`nowTop()`). Klik w siatkę = nowe przypomnienie o klikniętej godzinie (`openAt` liczy godzinę z offsetY). **Długość**: pole „Długość" w modalu (wartość + jednostka min/godz/dni → `parseDurationMin` w `reminder.service`, cap 30 dni; puste = 60 min domyślnie na siatce); `openEdit` konwertuje `durationMin` z powrotem na wartość+jednostkę. Miesiąc bez zmian (siatka dni + D&D Sortable; siatka godzin NIE ma `.cal-events`, więc D&D tylko w miesiącu). Test `test/calendar.test.js` (parse min/godz/dni + null, update, render widoków). Zweryfikowane E2E (Playwright: bloki wg długości, nakładka 09:20/09:40 → 2 kolumny, pasek „cały dzień" z Charge).
- [x] Kalendarz: przypomnienia cykliczne (v0.99.60): pole „Powtarzaj" (Nie/Codziennie/Co tydzień/Co miesiąc) + opcjonalne „do dnia" w modalu (tylko przy TWORZENIU). Model: `Reminder.repeat`/`repeatUntil`/`seriesId` (migracja `20260729130000`, index na seriesId). Podejście = **materializacja** (jak Retainer): `reminder.service.create` przy `repeat` generuje wystąpienia od `dueAt` do `repeatUntil` (albo domyślnie +6 mies.), twardy limit `MAX_OCCURRENCES=200`, wspólny `seriesId`, przez `$transaction` (SQLite-safe). Każde wystąpienie = niezależny `Reminder` (własny done/drag) — pokazują się naturalnie przez `inRange`, zero logiki rozwijania. Edycja wystąpienia zmienia tylko je (kontrolki „Powtarzaj" ukryte przy edycji; „część serii" info). **Usuń serię**: `removeSeries(id)` (deleteMany po seriesId) — trasa `delete` czyta `req.body.series`; przycisk „Usuń serię" w modalu edycji i w menu podręcznym (gdy `REM[id].seriesId`); `action(url, extra)` dokleja flagę. `mapReminder` niesie `seriesId`. Test `test/calendar.test.js` (tygodniowo 01→29.08 = 5 wystąpień wspólny seriesId, `repeat:none` = pojedyncze, removeSeries kasuje całą serię). Zweryfikowane E2E (modal: pole „do" pojawia się przy powtarzaniu).
- [x] Kalendarz: poprawki po testach usera (v0.99.61): (1) **Godzina zakończenia zamiast długości** — pole „Koniec" (datetime-local) w modalu zamiast wartość+jednostka; puste = 1 godz. `reminder.service.durationFromEnd` liczy `durationMin` z (koniec−start); create/update czytają `endAt` (fallback do `durationValue`/`durationUnit` — zgodność testów). openEdit rekonstruuje `endAt` z `dueAt+durationMin`. (2) **Drag&drop na siatce godzin (tydzień/dzień)** — pointerowy drag bloków: pionowo = godzina (snap 15 min), poziomo = inny dzień (wykrywanie kolumny pod kursorem); `POST reminders/:id/reschedule` ustawia dokładny `dueAt` (`reminder.service.reschedule`); klik po dragu blokowany (capture listener), żeby nie otwierał edycji. Miesiąc dalej Sortable, siatka godzin = własny drag (markery `data-timegrid`/`data-daycol`/`data-daykey`/`data-timed`). (3) **Ładniejszy modal + kolory** — modal przebudowany na etykietowany układ (Początek|Koniec, Priorytet|Kolor, Powtarzaj, Klient|Projekt); `Reminder.color` (preset sky|emerald|amber|rose|violet|slate; migracja `20260729140000`) — swatche w modalu (auto „A" = wg priorytetu), `chipCls` używa `colorCls(e.color)` przed kolorem priorytetu. Test `test/calendar.test.js` (endAt→90 min, kolor preset/zły, reschedule na godzinę+minuty). Zweryfikowane E2E (Playwright: drag 09:00→12:00 w DB, zielone/różowe bloki, modal z etykietami).
- [x] Restyling panelu — Etap 2, slice 6: formularze „modern" + poprawki kalendarza (v0.99.62): (1) **Formularze (modern)** — kontrolki `input/select/textarea` w `main` dostają spójny promień 10px + płynny hover ramki (slate-400), theme-gated (`.theme-modern main …`, wyklucza checkbox/radio/color/range/file; dark: hover do `dk-muted-2`). Classic bez zmian; karty formularzy i tak mają cień z Etapu 1. (2) **Kalendarz — usunięty opis-hint** pod siatką („· klik = dodaj… · prawy-klik = menu… · przeciągnij…") — została sama legenda kolorów. (3) **Zwijany panel boczny** (Nadchodzące/Zrobione): przycisk (strzałka) w nagłówku kalendarza `@click="sideOpen=!sideOpen"` → kolumna kalendarza `:class="sideOpen ? 'lg:col-span-2' : 'lg:col-span-3'"` (pełna szerokość), panel `x-show="sideOpen"`; stan w `localStorage['evoke-cal-side']` (persist przez `$watch`), przycisk `hidden lg:inline-flex` (desktop). Test w `test/calendar.test.js` (brak hintu, obecność `sideOpen`). Zweryfikowane E2E (Playwright: zwinięcie → kalendarz pełnej szerokości; modern form radius).
- [x] Restyling panelu — Etap 3, slice 1: strona „Mój profil" (v0.99.63): `/admin/account` przebudowane z samej zmiany hasła na profil w stylu referencji TailAdmin — 3 karty: **Mój profil** (avatar [wgrywany albo inicjał], nazwa, rola/stanowisko, telefon, e-mail logowania read-only; formularz zapisu), **Bezpieczeństwo** (zmiana hasła jak dotąd + wiersz **2FA** z wyłączonym przełącznikiem „Wkrótce (Etap 4)"), **Sesja** (Wyloguj z panelu). Model: kolumna `Settings.profile` (JSON `{name, role, phone, avatarPath}`, migracja `20260729150000`), `normProfile` w settings.service (trim+cap). Avatar: `brandingUpload.single('avatar')` → `public/branding/` (SVG sanityzowane jak reszta brandingu). Kontroler `account.controller`: `updateProfile` (name/role/phone + avatar/removeAvatar) + `changePassword` bez zmian; trasa `POST /admin/account/profile`. **Integracja z nagłówkiem**: prawy róg panelu pokazuje teraz avatar (inicjał/obraz) + nazwę profilu (klik → profil) zamiast surowego e-maila (`settings.profile` w `res.locals`, `layouts/admin.ejs`). Test `test/account-profile.test.js` (render „Mój profil" + zapis name/role/phone). Zweryfikowane E2E (Playwright: karty profil/bezpieczeństwo/sesja, avatar+nazwa w nagłówku). ROADMAPA Etapu 3: slice 2 = task list/kanban (na bazie `Reminder`); slice 3 = pełna personalizacja logowania.
- [x] Restyling panelu — Etap 3, slice 2: menu profilu + konfigurowalne szybkie akcje (v0.99.64): (1) **Menu profilu (prawy górny róg)** — klik w avatar+nazwę otwiera dropdown (Alpine `menu`): nagłówek (nazwa+e-mail), „Mój profil", „Ustawienia", „Wyloguj" (styl referencji TailAdmin). **Usunięte z lewego paska**: stopka „Konto i hasło"/„Wyloguj" (cały footer `<aside>`) ORAZ pozycja „Ustawienia" z nawigacji (`items.filter(key !== 'settings' && key !== 'account')`) — teraz dostępne wyłącznie z menu profilu. (2) **Konfigurowalne szybkie akcje** — rejestr `panelUi.ACTIONS` (10 skrótów: transfer/upload/project/client [domyślnie on] + calendar/sales/messages/board/templates/pulse [off]); `mergeActions`/`sanitizeActions`; zapis w `Settings.panel.actions` (delty `{key,hidden}`, BEZ migracji — JSON w panel). `dashboard.controller` liczy `quickActions` (widoczne) → widżet `_widgets/actions.ejs` renderuje z konfiguracji (primary = brand). Config UI: sekcja „Szybkie akcje" w Ustawienia → Panel (checkboxy z ikonami, 2 kolumny); `settings.controller` dokłada `actions` do budowanego `panel` (`actionShow_<key>`), `quickActionsFull` do widoku. Testy: `test/quick-actions.test.js` (mergeActions domyślne/konfig/sanitize), `test/admin-theme.test.js` zaktualizowany (seed menu z widoczną pozycją, bo „Ustawienia" już nie w pasku; asercja „Menu konta"). Zweryfikowane E2E (Playwright: dropdown, pasek bez footera/Ustawień, 7 akcji na pulpicie, checkboxy w Ustawieniach). GOTCHA: pełny zapis ustawień z przeglądarki zachowuje `actions` (checkboxy zawsze w formularzu Panel); NIE POST-ować częściowego formularza do dev-DB (wyzeruje akcje).
- [x] Restyling panelu — Etap 3, slice 3: task list / kanban (v0.99.65): nowa strona `/admin/tasks` — kanban przypomnień (`Reminder`) w kubełkach czasowych **Zaległe / Dziś / Ten tydzień / Później / Zrobione** (BEZ nowego modelu — to inny widok tych samych `Reminder` co kalendarz). `tasks.controller`: `bounds/bucketOf/targetDate` (matematyka dat, pn–nd tygodnia); `index` bucketuje otwarte przypomnienia (`reminderService.forTasks` = otwarte + ostatnie zrobione), `create` (szybkie dodanie: tytuł + kubełek + priorytet → `Reminder` z `dueAt` = reprezentacyjny termin kubełka, 9:00), `move` (drag → JSON: kubełek = nowy termin zachowując godzinę, albo `done`), `toggle`/`remove` (redirect). Widok: pasek szybkiego dodania + 5 kolumn (kolory: zaległe red / dziś brand / tydzień sky / później slate / zrobione green), karty z kropką priorytetu, terminem, kontekstem (klient/projekt → link), „✓ Zrobione"/„Przywróć" + kosz. **Drag&drop** SortableJS (group `tasks`, `filter:'a,button'`+`preventOnFilter:false` — klik w przyciski karty nie startuje dragu) → `POST /tasks/:id/move`. Pozycja menu `tasks` („Zadania", ikona `check`) w `panelUi.MENU` + opcja w szybkich akcjach. GOTCHA (naprawione): partial `_task_card.ejs` używał lokala `done` jako flagi — kolidował z parentowym `done` (lista zrobionych) → wszystkie karty na przekreślone; zmienione na `isDone`. Test `test/tasks.test.js` (render + dodanie do kubełka „Później" = termin w przyszłości + move→done + move→dziś odznacza i ustawia dzisiejszy termin). Zweryfikowane E2E (Playwright: 5 kolumn, bucketowanie 7 zadań, karty). Pełna edycja przypomnienia (godzina/długość/cykl/kolor) dalej w kalendarzu.
- [x] Restyling panelu — Etap 3, slice 4 (DOMYKA Etap 3): niezależna personalizacja logowania (v0.99.66): kolumna `Settings.login` (JSON, migracja `20260729160000`) + `normLogin` (whitelist `LOGIN_STYLES` card|split, `side` left|right, teksty trim+cap, `hideName` bool). Nowy **układ „split"** (formularz obok brandowego panelu z hero, `md:order-*` steruje stroną formularza) obok dotychczasowego **„card"** (wyśrodkowana karta — DOMYŚLNY, zero regresji). Konfigurowalne teksty: `title` („Zaloguj się"), `subtitle`, `heroTitle`/`heroSubtitle` (panel marki w split), `footer` (puste = `texts.footer` jak dotąd), `hideName` (chowa nazwę appName przy logo). Formularz wydzielony do partiala `admin/_login_form.ejs` (wspólny dla obu układów — jedno miejsce na pola/przycisk). UI: sekcja „Strona logowania — układ i teksty" w Ustawienia → Wygląd (nad istniejącą sekcją tła; radia układu/strony na `peer-checked`, pola tekstowe z placeholderami = wartości domyślne, `x-show` dla opcji split). `settings.controller` buduje obiekt `login` (whitelist stylu/strony) i przekazuje w update. Dotychczasowe mechanizmy logowania BEZ zmian (własne tło `loginBackground`, logo powierzchni `login`, `layout.applyToLogin`). Test `test/login-custom.test.js` (normalizacja: zły styl/strona → domyślne, trim, bool; render card z własnymi tekstami/stopką; split z hero + `md:order-2` + obecny formularz). Zweryfikowane E2E (Playwright: card „Witaj ponownie", split z panelem marki).
- [x] Logowanie: układ „panel pełnej wysokości" + szerokości + wyłączanie elementów (v0.99.67): trzeci układ logowania `panel` (obok `card`/`split`) — flush biały panel przy KRAWĘDZI na PEŁNĄ WYSOKOŚĆ, hero i stopka po stronie tła (odpowiednik „Panel na tle" ze stron klienta). `login.width` (sm|md|lg|xl|2xl, `LOGIN_WIDTHS`) mapowane na LITERALNE klasy `md:max-w-*` (skaner Tailwinda), `login.side` steruje krawędzią (`md:order-*`). GOTCHA: layout `auth.ejs` owija treść w wyśrodkowaną ramkę z paddingiem — dla `panel` renderuje `min-h-screen flex` BEZ paddingu (inaczej panel nie dotykałby krawędzi); rozgałęzienie czyta `settings.login.style`. **Wyłączanie elementów** (twarde, niezależne od pustych tekstów): `hideLogo`, `hideTitle` (nagłówek+podpis), `hideHero`, `hideFooter` — działają we WSZYSTKICH trzech układach (checkboxy „Pokaż / ukryj elementy" w Ustawienia → Wygląd). Hero/`side` dostępne teraz też dla `panel` (`x-show="lStyle==='split' || lStyle==='panel'"`). Test w `test/login-custom.test.js` (szerokość z whitelisty + `min-h-screen` + hero; zła szerokość → md; wszystkie 4 przełączniki chowają swoje elementy, formularz zostaje). Zweryfikowane E2E (Playwright: szerokości 448/672/384 px przy pełnej wysokości 720 px, panel po lewej/prawej, wariant minimalny = sam formularz).
- [x] Etap 4, slice 1: konta użytkowników + role (v0.99.68): logowanie przestaje być zawiązane na `.env` — teraz KAŻDE aktywne konto z tabeli `User` może się zalogować. Role: **admin** (wszystko) | **staff** (projekty/klienci/transfery/wiadomości/kalendarz/zadania — BEZ Ustawień, aktualizacji i kont). Model: `User.active` (Boolean, domyślnie true) + `User.lastLoginAt` (migracja `20260729170000`; `role` już było). `auth.service` przepisany: `authenticate(email,password)` zwraca obiekt sesji `{id,email,name,role}` albo null — (1) e-mail pasuje do konta w bazie z hasłem → bcrypt + sprawdzenie `active`; (2) **BOOTSTRAP**: e-mail = `ADMIN_EMAIL` a konto w bazie nie ma jeszcze hasła → fallback do `.env` (istniejące instalacje działają bez zmian, nie da się zamknąć sobie dostępu). `verifyCredentials` zostaje jako cienka nakładka (zmiana hasła). CRUD kont: `listUsers/createUser/updateUser/deleteUser/touchLogin`. Sesja: `auth.controller` zapisuje realnego usera + `touchLogin` (best-effort). Bramka: `middleware/auth.requireAdmin` (403 + `views/errors/403.ejs`) wpięta PREFIKSOWO w `admin.routes` — `router.use(['/settings','/update','/users'], requireAdmin)` (żadna pod-trasa nie umknie, też przyszłe); `injectUser` dokłada `res.locals.isAdmin` (stare sesje bez roli = admin). UI: strona `/admin/users` (edycja inline: nazwa/rola/hasło/aktywne + usuwanie + formularz dodania), pozycje „Konta"/„Ustawienia" w menu profilu tylko dla admina. Zabezpieczenia w `users.controller`: nie zmienisz sobie własnej roli ani nie wyłączysz siebie, nie usuniesz siebie, nie usuniesz OSTATNIEGO aktywnego admina. Test `test/users-roles.test.js` (login z bazy, staff 403 na /settings i /users ale 200 na /admin i /projects, złe hasło 401, konto wyłączone 401, hasło <8 znaków odrzucone, bootstrap `.env` dalej działa). Zweryfikowane E2E.
- [x] Etap 4, slice 2: 2FA — TOTP + kody zapasowe (v0.99.69): logowanie dwuetapowe (hasło → kod z aplikacji). **Własna implementacja TOTP** w `utils/totp.js` na wbudowanym `crypto` — BEZ nowej zależności (zasada projektu: shared-hosting-safe): base32 (RFC 4648), HOTP (RFC 4226), TOTP (RFC 6238) z oknem ±1 kroku (±30 s, przesunięty zegar), porównanie `timingSafeEqual`, `otpauthUri` + kody zapasowe (10 × `XXXX-XXXX`, alfabet bez mylących znaków). **Test `test/totp.test.js` sprawdza WSZYSTKIE 6 oficjalnych wektorów RFC 6238** — gwarancja zgodności z Apple Passwords / Google Authenticator / 1Password. Model: `User.totpSecret` (base32, musi być odwracalny), `User.totpEnabledAt` (null = konfiguracja niepotwierdzona), `User.recoveryCodes` (JSON z **hashami bcrypt** — nigdy jawnie; migracja `20260729180000`). `auth.service`: `begin2fa` (sekret, jeszcze nieaktywny) → `confirm2fa` (weryfikacja kodem → aktywacja + zwrot kodów zapasowych JEDEN raz) → `verifySecondFactor` (TOTP albo kod zapasowy — zużyty ZNIKA z puli), `disable2fa`, `regenerateRecoveryCodes`, `recoveryCodesLeft`; `authenticate` zwraca flagę `needs2fa`. Przepływ logowania (`auth.controller`): hasło OK + 2FA → `session.pending2fa` (poczekalnia, wygasa po 10 min) + strona `admin/login-2fa.ejs`; dopiero poprawny kod ustawia `session.user`. **KLUCZOWE (przetestowane): samo hasło NIE daje dostępu do panelu** — `/admin` w stanie pending przekierowuje na login. Trasa `POST /admin/login/2fa` pod tym samym `loginLimiter` co hasło. UI w „Mój profil" → Bezpieczeństwo: włącz (QR z `utils/qr` + sekret tekstem), potwierdź kodem, kody zapasowe (raz), „Nowe kody zapasowe", „Wyłącz 2FA" (wymaga hasła). GOTCHA: `qr.svg` ma naturalny rozmiar (util współdzielony z QR przelewu) → w widoku skalowany `[&>svg]:w-full` w kontenerze `w-44`. **Fix przy okazji (realny bug multi-user)**: `changePassword` weryfikował i zmieniał hasło konta z `.env` zamiast ZALOGOWANEGO — pracownik zmieniłby hasło administratorowi; teraz działa na `session.user` (fallback do `.env` tylko dla sesji z bootstrapu). Test `test/twofa.test.js` (pełny przepływ + jednorazowość kodów zapasowych + wyłączenie).
- [x] Ekran 2FA w wybranym układzie logowania (v0.99.70): drugi krok logowania (kod) był zawsze wyśrodkowaną kartą — przy układzie „split"/„panel" wyglądał niespójnie. Wygląd strony logowania wydzielony do wspólnej skorupy `admin/_login_shell.ejs` (układ card|split|panel + szerokość panelu, logo, hero, stopka, przełączniki `hide*` z `Settings.login`), a treść wstawia się przez local `innerPartial`: `_login_form` (e-mail+hasło) albo `_login_2fa_form` (kod). `login.ejs`/`login-2fa.ejs` to teraz 1-linijkowe widoki wołające skorupę. Nagłówek/podpis: skorupa bierze teksty z ustawień, a strona może je nadpisać (`lgHeading`/`lgSub`); ekran 2FA wymusza nagłówek (`lgForceHeading` — nawet przy `hideTitle` trzeba wytłumaczyć, czego oczekujemy). Blok logo wydzielony do `_login_logo.ejs` (był powielony 3×). Asercja w `test/twofa.test.js`: przy `login.style='panel'` ekran 2FA ma `md:max-w-2xl` i hero z ustawień. Zweryfikowane E2E (2FA w układzie split i panel).
- [x] Etap 4, slice 3 (DOMYKA Etap 4): passkeys / WebAuthn (v0.99.71): logowanie kluczem platformowym (Touch ID / Face ID / Windows Hello / iCloud Keychain / menedżer haseł) — **bez hasła**. Weryfikację podpisów (CBOR/COSE) robi **`@simplewebauthn/server`** (v13, czysty JS, ma build CommonJS — `require()` działa; GOTCHA: `require('@simplewebauthn/server/package.json')` rzuca `ERR_PACKAGE_PATH_NOT_EXPORTED`, ale sam moduł ładuje się normalnie). ŚWIADOMIE nie piszemy tego sami — błąd w weryfikacji podpisu = obejście logowania. Model `Credential` (migracja `20260729190000`): `userId` (Cascade), `credentialId` (unique, base64url), `publicKey`, `counter` (ochrona przed klonowaniem), `transports`, `label`, `lastUsedAt`. `webauthn.service`: `rp()` (rpID = hostname z `APP_URL`, origin = pełny adres), `isSupportedOrigin()` (HTTPS albo localhost — ograniczenie przeglądarek), `registrationOptions` (`residentKey:'preferred'` → logowanie BEZ podawania e-maila, `excludeCredentials` blokuje dublowanie klucza), `verifyRegistration`, `authenticationOptions` (bez `allowCredentials` = usernameless), `verifyAuthentication` (aktualizuje `counter`+`lastUsedAt`, odrzuca konto `active:false`). Challenge trzymany w sesji (`pkChallenge` / `pkLoginChallenge`). Klient: `public/js/passkey.js` (~100 linii vanilla — base64url ↔ ArrayBuffer + `navigator.credentials`, `credToJson` z fallbackiem gdy brak `toJSON`); BEZ vendorowania kolejnej biblioteki, CSP `'self'` bez zmian. UI: sekcja „Klucze dostępu (passkeys)" w profilu (dodaj/lista/usuń, komunikat gdy brak HTTPS) + przycisk „Zaloguj kluczem (passkey)" pod formularzem logowania (Alpine, pokazywany tylko gdy przeglądarka wspiera — czyli działa też w układach split/panel, bo siedzi w `_login_form`). Passkey jest sam w sobie dwuskładnikowy (coś, co masz + Touch ID/PIN), więc po nim NIE pytamy o TOTP; TOTP zostaje jako metoda zapasowa przy logowaniu hasłem. Test `test/passkeys.test.js` (rp/origin, CRUD kluczy + Cascade, bramki endpointów, publiczne opcje logowania z challenge) + **pełny przepływ zweryfikowany E2E w Chromium z wirtualnym authenticatorem** (CDP `WebAuthn.addVirtualAuthenticator`): rejestracja klucza → wylogowanie → logowanie SAMYM kluczem → panel, `lastUsedAt` zaktualizowany. UWAGA operacyjna: `npm audit` zgłasza `brace-expansion` (DoS przy patologicznych wzorcach glob) z zależności `archiver`/`ejs` — aplikacja nie podaje tam danych użytkownika, a `audit fix --force` robiłby łamiące aktualizacje archiver/ejs, więc zostawione świadomie.
- [x] Kafelki KPI bez rozciągania + wzbogacony kanban projektów (v0.99.72): (1) **Kafelki KPI pulpitu** — treść „pływała" w środku wysokiej ramki, bo grid rozciągał kafelek do wysokości najwyższego widżetu w rzędzie (`h-full` + domyślne `align-items:stretch`). Fix U ŹRÓDŁA: kafelki `stat-*` dostają `self-start` w `spanCls` (dashboard.ejs) → NATURALNA wysokość (zmierzone: 178 px zamiast 550 px odziedziczonych po „Ostatniej aktywności"); widżety treściowe dalej wypełniają rząd. Kompozycja `_stat.ejs` przebudowana na wzór referencji (TailAdmin): badge ikony U GÓRY, pod nim etykieta → wartość → podpis (było: ikona w prawym rogu obok etykiety, treść centrowana pionowo). Usunięte `h-full`/`justify-center`. (2) **Kanban projektów** (`/admin/projects/board`) — dołożone sygnały operacyjne z danych, które i tak pobieramy: podsumowanie kolumny (suma „do zapłaty" brutto + osobna linia „X po terminie" na czerwono), kropka koloru etapu w nagłówku, badge nieprzeczytanych wiadomości na karcie projektu (`message.groupBy` — jedno zapytanie, bez N+1), czerwone „do zapłaty · po terminie" gdy projekt ma zaległość, placeholder „Przeciągnij tu projekt" w pustej kolumnie, przycisk „Nowy projekt" w nagłówku, `filter:'a'`+`preventOnFilter:false` w Sortable (klik w nazwę otwiera projekt, nie startuje dragu — jak w kanbanie zadań). `project.service.board()` liczy `overdue`/`unreadMessages` per projekt i sumy per kolumna.
- [x] Composer wiadomości: ikona wysyłki W POLU + bez podpowiedzi o Enterze (v0.99.73): (1) placeholdery straciły dopisek „(Enter wysyła, Shift+Enter — nowa linia)" — zaśmiecał pole, a zachowanie jest oczywiste (komentarz w kodzie zostaje). Dotyczy panelu i portalu klienta. (2) Przycisk „wyślij" w komunikatorze panelu (`admin/messages/index.ejs`) przeniesiony **DO WNĘTRZA pola** (`relative` wrapper + `absolute right-2 bottom-1.5`, `pr-12` na textarea) i reaguje na treść: wyszarzony (`text-slate-300`) gdy pole puste, brandowe kółko z cieniem gdy jest tekst albo załącznik — wzorzec ChatGPT/Linear (wybrany przez właściciela z 4 wariantów). Wymagało `body` w x-data formularza (`x-model="body"`) — warunek `(body.trim() || fileName)`. **ŚWIADOMIE bez zmian w portalu klienta** (`public/messages.ejs`): tam zostaje jawny przycisk „Wyślij" z napisem — klient używa formularza rzadko i etykieta jest bezpieczniejsza niż subtelna ikona (w panelu to narzędzie codzienne, ikona wystarcza).
- [x] Siatka pulpitu z regulowaną WYSOKOŚCIĄ + ikona wysyłki wpisana w pole (v0.99.74): (1) **Wysokość widżetu** — dotąd konfigurowało się tylko szerokość, więc nie dało się złożyć układu „dwa niskie kafelki po lewej + jeden wysoki po prawej, wyrównane do tej samej wysokości" (v0.99.72 `self-start` leczył objaw). Nowe pole `rows` w rejestrze `panelUi.WIDGETS` (whitelist `ROWS = [2,3,4,6]`, przenoszone przez `mergeWidgets`/`sanitizeWidgets` dokładnie jak `span`; bez migracji — JSON w `Settings.panel.dashboard`). Siatka `#dashGrid` dostała `lg:auto-rows-[96px]`, element widżetu literalną klasę `lg:row-span-2|3|4|6` (`ROW_CLS` w dashboard.ejs; **tylko od `lg`** — na mobile stos rośnie z treścią). Arytmetyka jednostek składa się idealnie: S(2)=212 px, M(3)=328, L(4)=444, XL(6)=676 — **dwa S + gap 20 px = dokładnie L**, trzy S = XL (zmierzone E2E: dolne krawędzie wyrównane co do piksela). Auto-placement CSS Grid sam pakuje kolumny wg kolejności DOM, BEZ `grid-auto-flow: dense` (kolejność przeciągania musi być przewidywalna). W trybie edycji drugi przycisk obok szerokości cykluje `S→M→L→XL` (`cycleRows`/`rowsLabel`, podmiana klasy na żywo), `save()` wysyła `rows`. Usunięty `self-start`, `_stat.ejs` dostał `h-full` (treść zawsze przy GÓRNEJ krawędzi — pierwotna skarga „ikony i tekst pływają w środku ramki" nie wraca przy żadnej wysokości). (2) **Wewnętrzne przewijanie widżetów** — skoro wysokość jest sztywna, listy MUSZĄ klipować: `activity`/`tasks`/`messages`/`followup` mają `<ul class="flex-1 min-h-0 overflow-y-auto">`, `revenue`/`actions` analogicznie na kontenerze treści (`actions` przy okazji przerobiony na `flex flex-col`). **GOTCHA NA STAŁE**: `min-h-0` jest KONIECZNE — bez niego dziecko flexa nie skraca się i scroll nie działa (treść wylewa się poza ramkę). (3) **Fix przy okazji (realny bug z v0.99.64)**: `saveDashboardLayout` zapisywał `panel` bez gałęzi `actions` → każdy zapis układu pulpitu ZEROWAŁ konfigurację szybkich akcji. Teraz przenosi `current.panel.actions`. (4) **Ikona wysyłki wpisana w pole** (`admin/messages/index.ejs`): zaokrąglony prostokąt `right-1.5 top-1.5 bottom-1.5 w-9 rounded-xl` (zmierzone: równe 6 px od góry, prawej i dołu, radius 12 px) zamiast kółka z v0.99.73; `pr-14` na textarea. Textarea dostała `block` — jako `inline-block` tworzyła ~7 px baseline-gap w `relative` wrapperze i dolny odstęp wychodził inny niż górny. Logika stanu bez zmian (wyszarzony gdy pusto, `brand-600` gdy jest treść albo załącznik — potwierdzone `rgb(110,0,165)`). Test `test/panel.test.js` (rows przez merge/sanitize, nieznana wartość → domyślna z rejestru, klasy siatki w renderze) + E2E Playwright (S+S=L, scroll w niskiej ramce, odstępy przycisku, oba motywy). GOTCHA testowa: `settings.service` trzyma CACHE — w E2E układ zapisywać endpointem `POST /admin/dashboard/layout`, nie `prisma.settings.update` (zapis wprost do bazy nie jest widoczny bez restartu).
- [x] Paleta Cmd/Ctrl+K + awatary klientów (v0.99.75): (1) **Paleta wyszukiwania** — `Cmd/Ctrl+K` (albo klik w pole szukania w nagłówku) otwiera nakładkę z natychmiastowymi wynikami: nowy `GET /admin/search.json` (`search.controller.json`) spłaszcza istniejący `search.service.search(q, 6)` do grup `{key,label,icon,items:[{label,sub,href,avatar?}]}` (linki niosą `?from=search:<q>` → kontekstowy powrót z v0.99.28). Partial `admin/_search_palette.ejs` (Alpine): debounce 150 ms, anty-wyścig odpowiedzi (licznik `seq`), nawigacja ↑/↓/Enter/Esc po PŁASKIEJ liście (`get flat()`), stany „wpisz 2 znaki"/„szukam…"/„brak wyników", stopka „Wszystkie wyniki →". Pole w nagłówku ZOSTAJE formularzem GET (bez JS działa jak dotąd) + chip „⌘K". Ikony grup renderowane serwerowo do mapy i wstawiane przez `x-html` (client-side nie ma helpera `icon`). **GOTCHA NA STAŁE (kosztowała 1 czerwony test)**: layout `layouts/admin.ejs` NIE MOŻE użyć `include()` — widoki panelu z localem `client` (skrzynka, kartoteka, edycja klienta) przełączają EJS w tryb client-side i `include` przestaje istnieć („include is not a function"). Dlatego paleta idzie przez `utils/searchPalette.html()` (render partiala do stringa, w produkcji raz + cache, w dev za każdym razem → refresh dalej wystarcza) i `res.locals.searchPalette` wstawiany jako `<%- %>`. (2) **Awatary klientów** — kolumna `Client.avatarPath` (migracja `20260730090000`) + `utils/avatar.js` (JEDNO miejsce na wygląd): wgrane logo → `<img>`, brak → kółko z inicjałem (`\p{L}\p{N}`, polskie znaki OK) w STABILNYM kolorze z nazwy (hash djb2 → paleta 8 par, świadomie BEZ koloru marki — brand zostaje dla akcji), rozmiary `xs|sm|md|lg`, ścieżka escapowana. Helper w `res.locals.avatar`. Upload przez `brandingUpload.single('avatar')` na `POST /admin/clients` i `/clients/:id` (`avatarFrom(req)` w kontrolerze: plik → ścieżka, checkbox „usuń" → null, brak → `undefined` = nie rusza), pola w `clients/new.ejs`+`edit.ejs` (`enctype="multipart/form-data"`, podgląd bieżącego awatara). Użycia: lista klientów (tabela + karty mobile), nagłówek 360°, karta kanbana (`board()` select + `avatarPath`), skrzynka (usunięty marker `data-avatar` i reguła CSS „inicjały tylko w modern" — awatary są teraz wspólne dla obu motywów) i wyniki palety. Przy okazji `sanitizeIfSvg` wyciągnięty do `utils/svgSanitize` (był skopiowany w `settings.controller` i `account.controller` — teraz jedna implementacja dla całego brandingu). Testy `test/search-palette.test.js` (bramka logowania, <2 znaki, grupy, `from=search:`, awatar w JSON, paleta na widoku z localem `client`) + `test/crm.test.js` (helper: img/inicjał/stabilny kolor/escape; serwis: create zapisuje, string podmienia, null czyści, brak pola nie rusza). E2E Playwright: Cmd/Ctrl+K → wpisanie frazy → ↓ → Enter nawiguje, Esc zamyka, klik w pole otwiera, `<img>` vs kółko z inicjałem na liście/360°/kanbanie/skrzynce, upload i „usuń" awatara przez formularz.
- [x] Sortowanie/filtry list + eksport CSV (v0.99.76): (1) **Transfery** — `transfer.service.list` przyjmuje `sort` z whitelisty (`SORTS`: `created_desc|created_asc|expires_asc|expires_desc|downloads_desc|size_desc`; `ORDER_BY` mapuje na `orderBy`, „rozmiar" liczony w JS bo suma `File.size` to BigInt, `expires_asc` przenosi transfery BEZ terminu na koniec — w SQLite NULL jest „najmniejszy" i przesłaniał najbliższe terminy). Widok: szukajka + `kierunek` + `sortowanie` w JEDNYM formularzu GET (zachowują się nawzajem), zakładki statusu też niosą pozostałe filtry (`withFilter()` w widoku), a nagłówki kolumn Rozmiar/Pobrania/Wygasa/Utworzono są KLIKALNE (`sortTh(label, keys)` — dwie wartości = przełączanie kierunku ze strzałką ↑/↓). (2) **Klienci** — filtr po tagu (`list({tag})`): SQL zawęża przez `contains`, a DOKŁADNE dopasowanie robi JS na rozbitej liście, więc „vip" NIE łapie „vip-plus"; select z chmury tagów (`tagCloud`) w tym samym formularzu co szukajka/status/sortowanie. (3) **Eksport CSV** — wspólny `utils/csv.js` (`build/send/cell/money/slug`, separator `;`, BOM, CRLF) wyciągnięty z inline'u w `clientChargesCsv` (bez zmiany formatu tamtego pliku); nowe `GET /admin/clients.csv` i `GET /admin/transfers.csv` respektują TE SAME parametry co widok (kontrolery mają jedną funkcję `clientFilters(req)`/`transferFilters(req)` używaną przez widok i eksport), przyciski „CSV" w paskach filtrów niosą bieżące query. `csv.slug` podmienia `ł`→`l` ręcznie — to jedyna polska litera bez rozkładu NFD (dotąd „Żółta" dawało „zo-ta" w nazwie pliku). Test `test/csv-export.test.js` (format utila, bramka logowania, nagłówki `text/csv`+`Content-Disposition`, escapowanie średnika w danych, dokładny filtr tagu, wszystkie tryby sortowania + nieznana wartość → domyślna, kolejność zachowana w pliku). **GOTCHA testowa**: `fetch().text()` USUWA BOM przy dekodowaniu — sprawdzać przez `arrayBuffer()` (`EF BB BF`).
- [x] Widżet „Wymaga uwagi" + wykres przychodu na pulpicie (v0.99.77): (1) **`stats.service.attention()`** — JEDEN agregat sygnałów, które dotąd trzeba było wyłapywać z 4 ekranów: pozycje po terminie (kwota BRUTTO przez `grossOf`), transfery wysłane, NIEPOBRANE i wygasające w 3 dni (ten sam warunek co ostrzeżenie mailowe w `jobs/reminders.job.js`, tam okno 24 h), nieprzeczytane wiadomości (`messageService.unreadCount`), zadania na dziś i zaległe (`reminderService.dueCount`). Zwraca `{items, count}` — TYLKO niepuste sygnały, każdy z `label/value/sub/icon/tone/href`, więc widok nic nie filtruje. Partial `_widgets/attention.ejs`: wiersze z kolorem wagi (czerwony przeterminowane, bursztyn wygasa, brand wiadomości/zadania) i linkiem do właściwego ekranu; pusty stan „Wszystko pod kontrolą". (2) **Widżet `chart`** (span 8) — wykres przychodu z 6 miesięcy, dane z `pulse().money.chart` BEZ nowego zapytania (warunek w `dashboard.controller` rozszerzony na `revenue || chart`). Dwa renderingi w INLINE SVG (zero bibliotek, CSP `'self'`): linia + wypełnienie (`<path>` + `linearGradient`, punkty na przegięciach, bieżący miesiąc mocniejszy) i słupki. Przełącznik w nagłówku, wybór pamiętany w `localStorage['evoke-chart-type']` (domyślnie linia). Etykiety kwot: pełne złotówki `toLocaleString('pl-PL')`, skrót „X tys." dopiero od 10 000 zł — przy niższym progu 5200 zł i 4600 zł wyglądały identycznie („5 tys."). Oba widżety są domyślnie WIDOCZNE w rejestrze (`panelUi.WIDGETS`), więc dochodzą na koniec pulpitu bez ruszania zapisanego układu. Test `test/attention.test.js` (4 sygnały, brutto z VAT, linki, pełny kształt wiersza, pobrany transfer wypada z sygnału; render obu widżetów + oba warianty wykresu + pamięć typu) — snapshot+restore Settings. Zweryfikowane E2E (przełącznik linia↔słupki, stan po reloadzie, wykres z zasianym przychodem).
- [x] Kafelki KPI: wartość przy dolnej krawędzi (v0.99.78): przy wysokim `rows` (regulowana wysokość widżetów, v0.99.74) kafelek KPI był wyższy niż jego treść, a `h-full` bez flexa zostawiał martwy margines POD wartością (ikona+etykieta+wartość przyklejone do góry, dół karty pusty). Fix w `_stat.ejs`: kontener kafelka (`<a>`/`<div>`) dostaje `flex flex-col justify-between` — badge ikony zostaje u góry, blok etykieta/wartość/podpis „opada" do dolnej krawędzi niezależnie od wysokości (zmierzone: odstęp dolny = padding karty, 20-21 px, identycznie przy `rows:2` i `rows:6`). Bez ikony (kafelki bez `wicon`) flex się nie włącza — nie ma czego rozpychać na dwa końce, więc kompozycja zostaje jak dotąd. Zero zmian w logice/danych — czysty layout. Zweryfikowane E2E (Playwright: kafelek wysoki obok wysokiego widżetu treściowego — dokładnie scenariusz ze zgłoszenia — i kafelek niski, żeby nic się nie zepsuło).
- [x] Fix wykresu przychodu: rozciąganie w poziomie bez zniekształceń (v0.99.79): zgłoszony przez właściciela zrzut pokazał ucięty wiersz „w tym miesiącu" na szerokim ekranie. Przyczyna: statyczny SVG (`viewBox` skalowany przez `class="w-full"`, bez jawnej wysokości) trzyma aspect-ratio z viewBox — przy poszerzaniu okna/widżetu WYSOKOŚĆ rosła razem z szerokością (przeglądarka dolicza brakującą wysokość z proporcji), aż przekraczała stałą wysokość karty (`rows`, v0.99.74) i wypychała wiersz z kwotą poza kartę (zmierzone: przy 1920 px overflow +26 px, przy 2400 px +138 px). Próba z `preserveAspectRatio="none"` rozwiązałaby overflow, ale rozciągałaby KÓŁKA punktów w elipsy na szerokich widżetach (kosmetyczna regresja) — odrzucona. Właściwy fix: geometria licząca się PO STRONIE KLIENTA. `chart.ejs` renderuje pusty `<svg data-chart-svg>` + kontener `data-chart-mount` z danymi jako atrybut (`data-chart-data`, JSON); JS (`ResizeObserver` na kontenerze — łapie też zwijanie paska bocznego, nie tylko resize okna) mierzy REALNĄ szerokość i rysuje SVG w prawdziwych pikselach (`viewBox` = zmierzona szerokość × STAŁA wysokość 196, budowa przez `createElementNS`+`textContent`, bez `innerHTML` ze stringów). Efekt: szerokość rozciąga się swobodnie, wysokość zawsze stała (zweryfikowane E2E: 196 px przy 1024/1440/1920/2400 px szerokości), kółka zawsze idealnie okrągłe (rx==ry). Przełącznik linia/słupki (`$watch('type', …)` w x-init) woła `window.evokeChartRedraw($el)` zamiast przełączać `x-show` na dwóch gałęziach SVG — jedna implementacja rysowania, przełączenie samego typu. Test `test/attention.test.js` zaktualizowany pod nową strukturę (`data-chart-mount`/`data-chart-svg`/`ResizeObserver`/`evokeChartRedraw` zamiast statycznych `<path>`/`<rect>` w HTML). GOTCHA NA STAŁE: wykres na pulpicie z regulowaną wysokością (`rows`) NIE MOŻE polegać na samym `viewBox`+CSS-owej szerokości bez jawnej wysokości — aspect-ratio z viewBox przy szerokim widżecie zawsze wypchnie treść pod spodem poza stałą wysokość karty.
- [x] Wykres przychodu też w Pulsie + wspólny rysownik (v0.99.80): Puls miał STATYCZNY wykres słupkowy rysowany w EJS — bez przełącznika wariantu i z tym samym błędem skalowania, który naprawialiśmy w widżecie pulpitu (`viewBox` + `w-full` bez jawnej wysokości → na szerokim ekranie puchł proporcjonalnie do szerokości; zmierzone przed fixem). Rysownik wyciągnięty z inline'u w `_widgets/chart.ejs` do **wspólnego `public/js/chart.js`** (wzorzec `bg-rotate.js`/`sticky-header.js`; ładowany `defer` w layoucie admina, inicjalizacja po `querySelectorAll('[data-chart-mount]')` → obsługuje dowolną liczbę wykresów na stronie, unikalne id gradientu per instancja). Sam blok wykresu + przełącznik = nowy partial `admin/_revenue_chart.ejs` (locals `chartData`, `storeKey`, `chartTitle?`), używany przez widżet pulpitu i `pulse.ejs`. **Typ wykresu pamiętany OSOBNO per strona** (`data-chart-store`: `evoke-chart-type` na pulpicie, `evoke-pulse-chart-type` w Pulsie) — zweryfikowane E2E: przełączenie Pulsu na słupki NIE zmienia pulpitu. **GOTCHA NA STAŁE**: `tailwind.config.js` skanuje tylko `src/views`, `src/assets/js`, `src/utils` — **NIE `public/js`**, więc klasy `fill-brand-600`/`stroke-slate-200` przeniesione do pliku w `public/js/` wypadłyby z buildu. Dlatego kolory wykresu idą przez zmienne CSS ustawiane inline (`el.style.fill = 'var(--chart-label)'`), zdefiniowane w `input.css`: `--chart-axis/-label/-amount/-point/-brand/-brand-soft`. Efekt uboczny na plus: wykres wreszcie działa w **dark mode** (klasy `fill-*` nie miały nadpisań w `html.dark` — etykiety zostawały jasnoszare na ciemnym tle), a marka schodzi tam na `brand-400`, bo `brand-600` w tekście na ciemnym tle był nieczytelny. Wysokość SVG stała (196 px) przy 1024/1440/1920/2400 px — zmierzone. Testy w `test/attention.test.js` (osobne klucze, wspólny rysownik z layoutu, brak statycznych słupków w EJS).
- [x] Pulpit: uchwyt zmiany rozmiaru + OSOBNY układ na telefon (v0.99.81): (1) **Uchwyt zamiast przycisków cyklicznych** — w nakładce trybu edycji zniknęły dwa przyciski (¼–1/1 i S–XL), a w prawym dolnym rogu widżetu jest uchwyt (`data-resize`, `cursor-nwse-resize`): przeciąganie zmienia SZEROKOŚĆ I WYSOKOŚĆ naraz, snapując do dozwolonych wartości (`SPANS`/`ROWS` bez zmian — jednostka kolumny liczona z REALNEJ szerokości `#dashGrid`: `(width − 11×gap)/12`, jednostka wiersza 96+20 px). Klasy podmieniane na żywo, więc efekt widać w trakcie ciągnięcia. Fallback bez myszy: uchwyt jest fokusowalny i łapie strzałki (←→ szerokość, ↑↓ wysokość, po jednym kroku listy). Sortable ignoruje uchwyt (`filter: '[data-resize],button'` + `preventOnFilter: false` — wzorzec z kanbanów), więc drag za uchwyt NIE przenosi widżetu (zweryfikowane E2E). (2) **Osobny układ mobilny** — nowe pola `mspan` (1 = ½, 2 = pełna; domyślne odtwarzają dotychczasowe zachowanie: kafelki KPI ½, treść pełna) i `morder` (pozycja na telefonie, NIEZALEŻNA od desktopowej) w rejestrze `panelUi.WIDGETS` + `MOBILE_SPANS`; `mergeWidgets` normalizuje `morder` do ciągłych 0..n-1 (odporne na dziury i na nowe widżety po aktualizacji), `sanitizeWidgets` waliduje z whitelisty. Bez migracji — JSON w `Settings.panel.dashboard`. **Render zmiennymi CSS, nie klasami**: element dostaje `style="--ms:…; --mo:…"`, a `input.css` ma jedną regułę `@media (max-width: 1023.98px) { #dashGrid > [data-key] { grid-column: span var(--ms,2)/…; order: var(--mo,0) } }` — świadomie, bo `order-1..order-14` wykracza poza domyślną skalę Tailwinda, a to działa dla dowolnej liczby widżetów. Znikło wyliczanie mobile z typu widżetu w `spanCls()`. (3) **Tryb „Telefon" w edycji** — przełącznik Desktop/Telefon w pływającym pasku; podgląd WYSIWYG przez klasę `mobile-preview` (2 kolumny, `max-width:420px`, te same `--ms`/`--mo` co realny telefon → jedno źródło prawdy). W trybie telefonu uchwyt ustępuje przyciskowi ½ ↔ 1/1 (dwa stopnie nie mają czego snapować). **Kolejność mobilna a Sortable**: wejście w tryb telefonu sortuje DOM wg `--mo` (zapamiętując kolejność desktopową), `onEnd` przelicza `--mo` z sekwencji DOM, a wyjście/zapis przywraca DOM do desktopowej — dzięki temu jedno Sortable obsługuje oba tryby i kolejności się nie mieszają. `save()` wysyła `{key,hidden,span,rows,mspan,morder}`. Testy w `test/panel.test.js` (whitelisty, ciągłość `morder`, `--ms`/`--mo` w renderze, uchwyt i przełącznik); E2E: uchwyt (drag i strzałki) zmienia klasy, drag za uchwyt nie przenosi, po zapisie telefon 390 px pokazuje „Wymaga uwagi" na górze i kafelki KPI po ½, a desktop 1440 px zostaje bez zmian.
- [x] Pełnoekranowe menu na telefonie + spójny kolor paska systemowego (v0.99.82): zgłoszone z iPhone'a (PWA i Safari): „po otwarciu menu zmienia się kolor paska u góry". Przyczyna była w NASZYM kodzie, nie w Safari — przyciemnienie offcanvasu (`fixed inset-0 bg-black/40 z-30`) leży NAD nagłówkiem panelu (`md:z-20`), więc pasek z logo/ikonami szarzał. (1) **Offcanvas pełnoekranowy**: `aside[data-admin-sidebar]` `w-60` → `w-full md:w-60` — na telefonie menu zakrywa nagłówek (nie ma czego przyciemniać), od `md` wszystko zostaje jak było (sticky pasek 15rem + zwijanie railem; zweryfikowane: 240 px → 64 px po zwinięciu). Przyciemnienie ZOSTAJE (kryte przez menu, chroni w trakcie animacji `translate-x`). Doszedł przycisk **X** (`data-nav-close`, `md:hidden`) — przy pełnej szerokości nie ma już przyciemnienia, w które można kliknąć — oraz blokada przewijania tła (`:class="nav && 'overflow-hidden md:overflow-visible'"` na `<body>`; GOTCHA o `overflow-*` i `backdrop-filter` dotyczy kontenera TREŚCI panelu, nie `<body>` przy zakrytym ekranie). (2) **Sekcja konta w menu** (`data-nav-account`, `md:hidden`): awatar + nazwa/e-mail, „Mój profil", „Konta"/„Ustawienia" (za `isAdmin`, jak w dropdownie) i `POST /admin/logout` — na telefonie dropdown w rogu jest ciasny, a pełny ekran daje na to miejsce; na desktopie bez zmian (dropdown przy awatarze). (3) **`theme-color` synchronizowany z treścią**: statyczna wartość z `utils/pwa.js` (`pwa.themeColor || colors.primary`) nie miała związku z tym, co realnie jest u góry ekranu. Nowe `window.evokeSyncThemeColor()` ustawia meta na tło elementu, który faktycznie jest na górze: `[data-admin-sidebar]` gdy `html.nav-open`, inaczej `header`. Wołane przy starcie, przy zmianie `nav` (`x-init`+`$watch` na `<body>`) i z `window.toggleTheme` (dark mode). **GOTCHA**: kolor czytamy z `getComputedStyle(el).backgroundColor`, NIE z `--admin-*` — w dark mode zmienne są zdefiniowane przez `var(--dk-*)` i `getPropertyValue` zwróciłby nierozwinięte `var(...)`. Meta jest tworzona, gdy jej nie ma (działa też przy wyłączonym PWA — dla paska Safari); `utils/pwa.js` i ustawienie „Kolor motywu" NIETKNIĘTE (zostaje wartością startową przed JS). Zweryfikowane E2E (390 px: menu == szerokość viewportu, zakrywa nagłówek, X zamyka, 4 pozycje konta; theme-color przełącza się przy różnych kolorach nagłówka/sidebara `#123456`↔`#abcdef`; dark → `rgb(30,41,59)`; 1440 px bez regresji). Test w `test/admin-theme.test.js`. UWAGA: iOS w trybie standalone potrafi buforować kolor status bara do następnego uruchomienia aplikacji — finalne potwierdzenie na telefonie właściciela.
- [x] Paski Safari przestają szarzeć przy otwartym menu — usunięte przyciemnienie (v0.99.83): domknięcie v0.99.82. Pełnoekranowe menu działało, ale właściciel zgłosił z iPhone'a (prywatna karta Safari na `link.evoke.pl`), że pasek nadal zmienia kolor. Diagnoza ze zrzutów: przy zamkniętym menu górny obszar status bara i dolny toolbar Safari są jasne, przy otwartym oba SZARE — a między dolną krawędzią menu a paskiem Safari widać **szary pas**. To zdradziło przyczynę: przyciemnienie (`div.fixed.inset-0.bg-black/40.z-30.md:hidden`) zostało w v0.99.82 świadomie („kryte przez menu, chroni w trakcie animacji") i to był błąd — menu to `position:fixed` z `inset-y-0`, które na iOS przy dynamicznym pasku NIE zawsze pokrywa pełną realną wysokość (ta sama pułapka co przy pasie portali klienta, v0.99.8), więc w odsłoniętym skrawku zostawała czarna warstwa 40%, a Safari barwi swoje paski kolorem pobranym przy krawędziach ekranu. Fix: (1) **przyciemnienie usunięte całkowicie** — przy menu na całą szerokość nie ma go po co klikać (jest przycisk X), a jedyne co realnie robiło, to barwiło paski; (2) **zabezpieczenie na skrawek**: `@media (max-width: 767.98px) { html.nav-open body { background-color: var(--admin-sidebar) } }` w `input.css` — nawet gdy `fixed` nie dojdzie do krawędzi, kolor pod spodem jest TEN SAM co menu, więc paski nie mają skąd wziąć szarości (klasa `nav-open` już istniała dla `evokeSyncThemeColor`). Reszta v0.99.82 (pełna szerokość, X, sekcja konta, sync `theme-color`, blokada przewijania) nietknięta. Zweryfikowane E2E (390 px: zero elementów `bg-black/40` w DOM, tło body przy otwartym menu == tło menu i wraca do `--admin-bg` po zamknięciu; 1440 px bez regresji). **GOTCHA NA STAŁE**: przy offcanvasie na CAŁĄ szerokość NIE trzymać przyciemnienia — na iOS `position:fixed` bywa krótszy niż widok i odsłonięta warstwa barwi paski przeglądarki.
- [x] Mobile: obszar nad nagłówkiem w kolorze nagłówka + przyklejony nagłówek (v0.99.84): po potwierdzeniu, że v0.99.83 rozwiązała szarzenie pasków, właściciel poprosił o dwie kosmetyki. (1) **Obszar NAD stroną w kolorze paska** — pas pod status barem odcinał się jasnym `--admin-bg` od białego nagłówka. Safari barwi swoje paski kolorem CANVASU, a canvas bierze tło **root elementu** (`<html>`) — gdy `html` ma własne tło, tło `<body>` już na canvas nie propaguje. Stąd `@media (max-width: 767.98px) { html { background-color: var(--admin-header) } }` (+ `html.nav-open` → `--admin-sidebar`, spójnie z regułą dla body z v0.99.83). Treść bez zmian — `<body>` dalej maluje `--admin-bg` w swoim boxie; reguła jest TYLKO poniżej `md` (na desktopie `html` zostaje bez tła). Działa też w dark mode (`--admin-header` → `--dk-surface`). (2) **Nagłówek przyklejony na telefonie**: `md:sticky md:top-0 md:z-20` → `sticky top-0 z-20` — hamburger, szukajka i profil są zawsze pod ręką (zmierzone: po scrollu 600 px nagłówek zostaje na `top:0`). Pełnoekranowe menu ma `z-40`, więc dalej wjeżdża nad nagłówek. Test w `test/admin-theme.test.js`; zweryfikowane E2E (390 px: `html`==tło nagłówka, przy otwartym menu `html`==`body`==tło menu, dark mode, sticky po scrollu; 1440 px bez regresji — `html` bez tła, sidebar 240 px).
- [x] Ukrywanie rozliczonych pozycji (v0.99.85): checkbox „Ukryj rozliczone" przy nagłówku listy pozycji — na 360° klienta (zakładka Rozliczenia) i na stronie projektu (sekcja `#rozliczenia`). Stan pamiętany w `localStorage` pod **WSPÓLNYM** kluczem `evoke-hide-settled`, więc przełącznik przenosi się między ekranami (wzorzec Alpine+localStorage jak w `admin/_revenue_chart.ejs`, `$watch` zapisuje każdą zmianę). Filtr jest czysto WIZUALNY (`x-show` na już wyrenderowanych wierszach) — serwis `charge.service` nietknięty, `forStatement({status})` dla PDF/CSV to osobny, niezależny mechanizm. Szczegóły: (1) na karcie klienta lista jest GRUPOWANA po projekcie, więc nagłówek grupy dostaje `x-show` tylko wtedy, gdy KAŻDA pozycja grupy jest rozliczona (`_groupUnpaid` liczone przed pętlą) — inaczej zostałby osierocony napis bez wierszy pod spodem; (2) gdy wszystko jest rozliczone, po ukryciu pojawia się komunikat „Wszystkie pozycje są rozliczone (ukryte)" z przyciskiem „Pokaż" (`@click="hideSettled = false"`) — bez tego lista byłaby pusta bez wyjaśnienia; (3) sam checkbox renderuje się TYLKO gdy jest co ukrywać (`charges.some(c => c.paidAt)`). Zweryfikowane E2E (Playwright: grupa w całości rozliczona znika, mieszana zostaje, stan przeżywa reload, ten sam przełącznik na stronie projektu, „Pokaż" przywraca). GOTCHA testowa: probe `querySelector('[x-cloak]')` daje FAŁSZYWY negatyw — Alpine USUWA ten atrybut po inicjalizacji, więc widoczność sprawdzać po treści, nie po `x-cloak`.
- [x] Fix obcinania wykresu na pulpicie — minimalna wysokość widżetu (v0.99.86): właściciel zgłosił, że wykres przychodu „obcina się na desktopie, na mobile jest ok". **Diagnoza empiryczna** (Playwright, macierz `rows` × `span` przy 1280/1440/1920/2400 px) wskazała przyczynę precyzyjnie: przy DOMYŚLNEJ wysokości (`rows:4` = 444 px) nic się nie ucina na żadnej szerokości, ale po zmniejszeniu widżetu uchwytem (funkcja z v0.99.81) treść przestaje się mieścić — `rows:3` (328 px) ucina wiersz z kwotą o 11 px, `rows:2` (212 px) ucina SVG o 91 px i kwotę miesiąca całkowicie (overflow 147 px). Szerokość (`span`) nie ma znaczenia — obcięcie jest CZYSTO PIONOWE. Na telefonie problemu nie ma, bo klasy `lg:row-span-*` działają dopiero od `lg`, a niżej wysokość rośnie z treścią — to zgadzało się ze zgłoszeniem co do joty. Fix: nowe pole **`minRows`** w rejestrze `utils/panelUi.js` (tylko dla `chart`: 4) egzekwowane na TRZECH ścieżkach — (1) `mergeWidgets` podnosi zapisaną wysokość przy ODCZYCIE (**samoleczenie**: układ zapisany zanim minimum istniało naprawia się sam po deployu, bez ruchu użytkownika), (2) `sanitizeWidgets` podnosi przy ZAPISIE (konfiguracja nie kłamie względem renderu), (3) uchwyt zmiany rozmiaru w `dashboard.ejs` dostaje `minRowsMap` z kontrolera i filtruje dozwolone wysokości per widżet (`rowsFor(key)` w `snap()` i w obsłudze strzałek) — nie da się już zejść za nisko. Widżety-listy (aktywność/zadania/wiadomości/do odezwania się) minimum NIE mają — tam wewnętrzny scroll jest naturalny i pożądany; wykres jest inny, bo jego treść ma STAŁĄ wysokość (przełącznik 40 px + SVG 196 px + wiersz kwoty 28 px) i przewijanie w niskiej karcie niczego nie ratuje. Test w `test/panel.test.js` (merge/sanitize podnoszą 2→4 i 3→4, 6 zostaje, listy nietknięte, `minRowsMap` w renderze). Zweryfikowane E2E (zapisany `rows:2` renderuje się jako 444 px z zerowym overflow; strzałka w górę nie schodzi poniżej minimum, w dół rośnie do 676 px). GOTCHA testowa: `JSON.stringify` w `x-data` idzie przez `<%= %>`, więc w HTML cudzysłowy są zescapowane (`&#34;`) — asercje muszą to uwzględniać.
- [x] Edycja ZAWARTOŚCI istniejącego transferu — dodawanie i usuwanie plików (v0.99.87): dotąd transfer dało się edytować tylko „ustawieniowo" (tytuł/wiadomość/hasło/wygasanie), a pomyłka w zestawie plików wymagała skasowania całego transferu i wysłania klientowi NOWEGO linku. Teraz na stronie szczegółów (`transfers/show.ejs`, działa dla obu kierunków): **„+ Dodaj pliki"** przy „Pobierz ZIP" w nagłówku listy (zwykły multipart, auto-submit po wybraniu — bez osobnego kreatora) i **kosz przy każdym pliku** (hover-reveal obok ikony pobierania, `confirm` z nazwą). Serwis: nowe `removeFile(transferId, fileId)` — kasuje plik z DYSKU (`storage.removeStored`) i wiersz z bazy, wzorzec `document.service.remove()`; filtr po `transferId` blokuje usunięcie pliku innego transferu (GOTCHA: `onDelete: Cascade` działa tylko w drugą stronę — kasowanie transferu kasuje pliki, ale samo usunięcie `File` NIE rusza dysku). `addFiles()` reużyte bez zmian (to samo, którego używa upload od klienta). Trasy `POST /transfers/:id/files` (z `receiveUpload('files')` — tym samym middleware co przy tworzeniu, więc chunked i multipart działają) + `POST /transfers/:id/file/:fileId/delete`. **Zmiana plików unieważnia proofing**: gdy transfer ma `proofing` i klient już zdecydował, `resetApproval` czyści `approvalStatus/Comment/By/At` + log w historii — klient oceniał INNY zestaw (wzorzec jak `offer.service.update` przy edycji oferty). **Nowy stan brzegowy — transfer wychodzący z ZEREM plików** (wcześniej nieosiągalny, bo tworzenie wymaga pliku): panel radził sobie sam (404 na ZIP), ale publiczny `/t/:token/zip` wydawał PUSTE archiwum i — gorzej — **zliczał pobranie**, zjadając limit i mogąc wygasić transfer; dołożona bramka `!transfer.files.length → 404` przed licznikiem (jak w panelu). Test `test/transfer-files.test.js` (dodanie 2 plików multipartem, usunięcie z dysku i bazy, bramka logowania, brak usuwania cudzego pliku, reset proofingu + zdarzenie, zerowy ZIP bez zliczenia pobrania). Zweryfikowane E2E. GOTCHA: nazwę pliku w inline `confirm` wstawiać przez `JSON.stringify` (nie wprost w apostrofy) — inaczej apostrof albo cudzysłów w nazwie rozwala literał JS w atrybucie (zweryfikowane na nazwie `O'Brien "raport".txt`).
- [x] Dodawanie plików do transferu przez upload DZIELONY (v0.99.88): domknięcie v0.99.87 — „+ Dodaj pliki" szło zwykłym multipartem, więc duży plik odbijał się od limitu rozmiaru requestu na shared hostingu (dokładnie ten problem, dla którego istnieje chunked upload w kreatorze). Teraz ten sam mechanizm co wszędzie: `public/js/chunked-upload.js` tnie na 5 MB i wysyła na `/admin/transfers/chunk` (endpoint WSPÓLNY z kreatorem, już istniał), potem widok POST-uje zwykłe urlencoded z `uploadId` na `/transfers/:id/files`. **Serwer bez zmian** — trasa od początku miała `receiveUpload('files')`, który sam wykrywa `uploadId`/`X-Upload-Id` i składa `req.files` w kształcie multera; kontroler nic o tym nie wie. Komponent Alpine `addFiles(transferId)` w `transfers/show.ejs`: pasek postępu w nagłówku karty („Wysyłanie… X%" → „Finalizowanie…" z pulsem, bo sklejanie kawałków po stronie serwera potrafi potrwać), po sukcesie `location.reload()`, przy błędzie alert + czyszczenie `input.value` (żeby dało się wybrać te same pliki ponownie). Fallback: brak `window.chunkedUpload` → `form.submit()` = zwykły multipart jak dotąd. Test w `test/transfer-files.test.js` (2 kawałki wysłane POZA KOLEJNOŚCIĄ + finalizacja urlencoded → pełny rozmiar i zgodne md5). Zweryfikowane E2E (plik 12 MB → dokładnie 3 requesty na `/chunk`, md5 zgodne, pasek postępu widoczny). ZAKRES: to funkcja WYŁĄCZNIE panelu — trasy siedzą w `admin.routes.js` pod `requireAuth`, w `src/views/public/` nie ma po nich śladu; klient dalej ma tylko normalny upload przez `/upload/:token` i portal, bez kasowania i edycji.
- [x] Sekcja konta w menu mobilnym zwijana (v0.99.89): w offcanvasie cztery pozycje (Mój profil / Konta / Ustawienia / Wyloguj) siedziały na stałe pod nawigacją i zjadały dół ekranu. Teraz pasek z awatarem i nazwą użytkownika jest PRZYCISKIEM (`data-nav-account-toggle`, `aria-expanded`, chevron obracany przez `rotate-180`), a pozycje rozwijają się dopiero po stuknięciu (`x-data="{ acct: false }"` + `x-show` z fade) — czyli tak jak dropdown profilu na desktopie. Stan wraca do zwiniętego przy zamknięciu menu (`x-effect="if (!nav) acct = false"` — `nav` jest w scope `<body>`), żeby kolejne otwarcie zawsze wyglądało tak samo. Desktop bez zmian (sekcja dalej `md:hidden`, dropdown przy awatarze nietknięty). Test w `test/admin-theme.test.js` (przełącznik + `x-show` PO nim w dokumencie + domyślnie zwinięte); zweryfikowane E2E (390 px: po otwarciu menu 0 widocznych pozycji konta, po stuknięciu 4, ponowne stuknięcie zwija, zamknięcie i otwarcie menu resetuje do zwiniętego; 1440 px bez regresji — sidebar 240 px, sekcja mobilna schowana).
- [x] iPhone: nagłówek naprawdę przyklejony + stopka menu w kadrze (v0.99.90): właściciel zgłosił z iPhone'a dwie rzeczy — (1) górny pasek panelu „nie jest przyklejony", (2) pasek z nazwiskiem w offcanvasie jest POZA ekranem i trzeba przewinąć menu, żeby go zobaczyć. W Chromium (390 i 1440 px) nagłówek trzymał `top:0` po przewinięciu i nie było przodka z `overflow` — czyli problem jest wyłącznie po stronie iOS, a **WebKita nie ma w tym środowisku, więc nie dało się go odtworzyć lokalnie**. Obie usterki mają wspólny mianownik: pułapka viewportu Safari, ta sama co przy pasach portali (v0.99.7–9). (1) **Offcanvas**: `fixed inset-y-0` → `fixed top-0 h-[100dvh]` (`md:h-screen` bez zmian) — przy `position:fixed` Safari potrafi rozciągnąć `bottom:0` do DUŻEGO viewportu (ze schowanym paskiem URL), więc dolna sekcja `shrink-0` (konto) lądowała pod krawędzią; `dvh` śledzi realnie widoczną wysokość. (2) **Nagłówek**: na telefonie `sticky` → **`fixed inset-x-0 top-0` + rozpórka `h-16 md:hidden`**, bo iOS przy inercyjnym przewijaniu nie odrysowuje elementów `sticky` na bieżąco (pasek „ucieka" w górę i wraca po zatrzymaniu — dokładnie „nie jest przyklejony"); element o STAŁEJ wysokości z `fixed top-0` jest na iOS stabilny, w odróżnieniu od pełnoekranowego `fixed inset-y-0`. Desktop zostaje na `md:sticky md:inset-x-auto` — `fixed` nie zna kolumny treści i nachodziłby na pasek boczny (zweryfikowane: nagłówek startuje na x=240, dokładnie za sidebarem). Test w `test/admin-theme.test.js` (fixed+rozpórka na telefonie, sticky od md, `dvh` na offcanvasie, brak `inset-y-0`). Zweryfikowane E2E w Chromium (390 px: `position:fixed`, top 0 po scrollu 700 px, treść startuje pod paskiem, wysokość menu == wysokość widoku, pasek konta w kadrze bez przewijania; 1440 px bez regresji). **Potwierdzenia na realnym iOS BRAK** — Chromium nie odtwarza ani inercyjnego scrolla Safari, ani jego liczenia viewportu dla `fixed`.
- [x] Historia wysłanych maili: rodzaj + adresat + REALNY temat (v0.99.91): wpisy `email_sent` były niespójne — każde miejsce budowało własne zdanie, przypomnienia o płatności i retainery gubiły ADRESATA, transfer logował tylko „Wysłano link e-mailem" (bez tytułu), a **odpowiedź agencji na wiadomość klienta nie zostawiała ŻADNEGO śladu**. Teraz jeden format: **„Rodzaj → adresat · „temat"" (+ opcjonalny dopisek w nawiasie)**. Mechanizm: (1) `mail.service.send()` zwraca `{...info, to, subject}` (także w trybie dev bez SMTP — historia jest spójna na środowisku bez poczty), więc w historii ląduje TEN temat, który realnie poszedł do klienta, także gdy pochodzi z szablonu w Ustawieniach → E-mail; (2) nowy `event.service.emailSent({kind, to, info, extra, ...ctx})` składa message i zapisuje komplet do `meta` (`{kind,to,subject}`). Rodzaje: Link do transferu / Link uploadu (wg `direction`), Panel projektu, Link onboardingowy, Oferta, Rozliczenie|Proforma (PDF) (+dopisek: liczba pozycji i nazwa załącznika), Przypomnienie o płatności (+liczba pozycji i kwota), Pozycja cykliczna (+kwota brutto), Powiadomienie o wiadomości (NOWE — wcześniej brak wpisu). **Zero zmian w widokach** — historia projektu, oś czasu klienta, widżet aktywności i powiadomienia renderują `message`, więc komplet informacji pojawia się wszędzie naraz. GOTCHA: `extra` ma nieść to, czego NIE MA w temacie (kwota, załącznik) — przy retainerze pierwotny `extra: charge.label` dublował nazwę już obecną w temacie. Test `test/email-log.test.js` (format + meta + adresat z `info` + brak tematu nie psuje wpisu + E2E na wysyłce linku do transferu); zaktualizowany `retainer.test.js`. Zweryfikowane realną wysyłką wszystkich 6 rodzajów.
- [x] Licznik na ikonie zainstalowanej aplikacji (v0.99.92): badge'y w interfejsie (menu boczne, dzwonek) były od dawna — brakowało **kropki z liczbą na ikonie aplikacji** w docku/na ekranie głównym. Zrobione przez **Badging API** (`navigator.setAppBadge` / `clearAppBadge`), bez nowej zależności: inline-skrypt w `layouts/admin.ejs` (liczniki `unreadCount` + `unreadMessages` są już w `res.locals` dla badge'y menu, więc ZERO nowych zapytań), renderowany tylko gdy `pwa.enabled && pwa.badge`. Zasięg: zainstalowane PWA — Chrome/Edge na komputerze, iOS/iPadOS 16.4+ po „Dodaj do ekranu głównego"; w zwykłej karcie API nie ma i blok jest bezszkodliwym no-opem (`try/catch` + sprawdzenie obecności metody). Zero=`clearAppBadge` (kropka znika, nie zostaje „0"). **Wylogowanie czyści licznik** — `layouts/auth.ejs` woła `clearAppBadge`, inaczej wisiałaby ostatnia znana liczba bez kontekstu. Przełącznik „Licznik na ikonie aplikacji" w Ustawienia → Zaawansowane (sekcja PWA), `pwa.badge` w `normPwa` **domyślnie WŁĄCZONY** (brak pola w zapisie = true, więc istniejące instalacje dostają licznik po włączeniu PWA bez ruchu). **OGRANICZENIE (świadome, udokumentowane w UI)**: bez Web Push licznik odświeża się tylko przy otwartej aplikacji (na każdym wczytaniu strony panelu) — po zamknięciu zostaje ostatnia wartość. Push wymagałby VAPID + subskrypcji + realnego SW z obsługą zdarzeń (nasz SW jest ŚWIADOMIE bez cache, sam do instalowalności) — osobny temat. Test w `test/pwa.test.js` (normalizacja domyślnie-on/wyłączenie, skrypt tylko przy PWA+badge, reszta PWA nietknięta przy wyłączonym liczniku, czyszczenie na ekranie logowania); zweryfikowane E2E z podstawionym API (3 nieprzeczytane → `setAppBadge(3)`, wylogowanie → `clearAppBadge`).
- [x] Liczniki odświeżane w otwartej karcie (v0.99.93): właściciel zgłosił, że licznik na ikonie „nie działa od razu" i zapytał, czy to zależy od crona. **NIE — cron nie ma z tym nic wspólnego**: liczniki to zapytania do bazy wykonywane przy KAŻDYM renderze strony panelu (`res.locals.unreadCount/unreadMessages/calendarDue` w middleware `admin.routes`), a cron co najwyżej tworzy zdarzenie, które potem jest liczone. Prawdziwa przyczyna: licznik (a także dzwonek i badge menu!) był zapisywany TYLKO w momencie wczytania strony — kto siedzi na jednym ekranie, nie widział nowej wiadomości aż do przeładowania. Fix: lekki endpoint `GET /admin/badges.json` (`notification.controller.badgeCounts` — 3 COUNT-y, `no-store`, za `requireAuth`) + `public/js/badges.js` odpytujący go **co 60 s tylko gdy karta jest WIDOCZNA** (`visibilityState`) i **natychmiast po powrocie do karty** (`visibilitychange` — najczęstszy moment, gdy liczba jest nieaktualna). Aktualizuje naraz: dzwonek, badge menu (wiadomości/zadania) i licznik na ikonie PWA (`window.evokeAppBadge` wystawione przez inline-skrypt z v0.99.92 — gdy licznik wyłączony w Ustawieniach, funkcji nie ma i `badges.js` jej nie woła). **Markup**: elementy badge renderują się TERAZ ZAWSZE (przy zerze z klasą `hidden`), więc JS tylko podmienia liczbę i przełącza klasę — zero dorabiania markupu z JS (i zero ryzyka, że Tailwind wytnie klasy, bo `public/js` nie jest skanowane). GOTCHA: `badges.js` NIE zastępuje Web Push — przy ZAMKNIĘTEJ aplikacji nadal nic się nie odświeża (licznik na ikonie zostaje z ostatniej wartości); to nadal osobny temat. Test `test/badges.test.js` (bramka logowania, kształt i `no-store`, realne podniesienie licznika po nowej wiadomości/zdarzeniu, markup + wpięty skrypt); zweryfikowane E2E (nowa wiadomość przy otwartej stronie → dzwonek 0→1, menu 0→1, ikona 0→2 BEZ przeładowania; powrót do karty → natychmiastowa aktualizacja).
- [x] Powiadomienia push — licznik odświeża się przy ZAMKNIĘTEJ aplikacji (v0.99.94): domknięcie v0.99.92/93. Właściciel zapytał, czy strona może odświeżać się w tle, żeby badge pojawiał się sam przy zwiniętej apce — odpowiedź: przeglądarka zamraża JS zamkniętej strony, więc JEDYNYM mechanizmem jest **Web Push** (Periodic Background Sync odrzucony: tylko Chrome, brak iOS, realnie kilka razy na dobę). Biblioteka **`web-push` 3.6.7** — czysty JS (zero `binding.gyp`/`.node`), więc mieści się w zasadzie shared-hosting-safe; ŚWIADOMIE nie piszemy szyfrowania (ECDH+HKDF+AES128GCM) i podpisu VAPID sami, tak jak przy passkeys. **Klucze VAPID w `storage/vapid.json`, NIE w Settings** — obiekt ustawień ląduje w `res.locals` każdego widoku i bywa serializowany do JSON w atrybutach Alpine, więc klucz PRYWATNY nie ma prawa tam trafić (test tego pilnuje); plik generowany przy pierwszym użyciu (`mode 0600`), **dopisany do `.gitignore`** (inaczej klucz prywatny poszedłby do repo!) i **do kopii zapasowej** (`backup.service` — jego utrata unieważnia WSZYSTKIE subskrypcje). Model `PushSubscription` (migracja `20260812090000`, `endpoint` unikalny → ponowne włączenie odświeża zamiast dublować, `label` z User-Agent, Cascade po userze). `push.service`: `subscribe/unsubscribe/list/count`, `sendToAll` (kasuje subskrypcje po 404/410 — inaczej lista rosłaby w nieskończoność) i `notify()` liczące aktualny badge. **Dwa punkty wyzwalania**: `event.service.log` dla `NOTIFY_TYPES` ORAZ `message.service.create` — wiadomość od klienta NIE tworzy `Event`, więc sam hook w event.service pominąłby najważniejsze powiadomienie; oba bez `await` (zapis zdarzenia/wiadomości nie może zależeć od wysyłki). GOTCHA (cykl require): `event.service` → `push.service` → `event.service` po licznik — oba ładowane LENIWIE w środku wywołania. Service worker (dotąd świadomie pusty) dostał `push` (banerek + `setAppBadge`, `tag` = jeden wątek zamiast zasypywania ekranu) i `notificationclick` (podnosi otwarte okno zamiast mnożyć nowe). UI: sekcja „Powiadomienia push (to urządzenie)" w **Mój profil → Bezpieczeństwo** (subskrypcja należy do PRZEGLĄDARKI, nie konta — jak passkeys) z przyciskiem „Wyślij testowe" i obsługą stanu `denied`; przełącznik `pwa.push` w Ustawienia → Zaawansowane (domyślnie ON, bezczynny bez subskrypcji). Test `test/push.test.js` (klucze + brak wycieku prywatnego do ustawień, upsert bez duplikatów, etykiety urządzeń, bramki ustawień/logowania, SW); E2E w Chromium: subskrypcja zapisana, `notify` zaszyfrował ładunek i wysłał, martwy endpoint auto-usunięty (`sent:0, removed:1`). **NIEZWERYFIKOWANE**: realne dostarczenie do przeglądarki (wymaga prawdziwego serwera push) — do potwierdzenia przyciskiem „Wyślij testowe" na produkcji po HTTPS. Na iPhonie działa TYLKO dla apki dodanej do ekranu głównego (iOS 16.4+) i po zgodzie.
- [x] Treść powiadomień push: sensowne tytuły + przełącznik prywatności (v0.99.95): właściciel zobaczył „Nowa wiadomość **from Evoke LINK**" i poprosił o usunięcie dopisku. **GOTCHA NA STAŁE — tego NIE DA SIĘ usunąć**: „from <nazwa>" to atrybucja źródła dokładana przez PRZEGLĄDARKĘ (zabezpieczenie antyspoofingowe — strona nie może wysłać powiadomienia udającego inne źródło); Notification API nie ma na to opcji (są tylko `title`, `body`, `icon`, `badge`, `tag`, `silent`, `requireInteraction`, `actions`, `data`), a nasz ładunek nie zawiera słowa „from" w ogóle (zweryfikowane grepem). Jedyna dźwignia to sama NAZWA (manifest: `pwa.name || appName`) — właściciel wybrał zostawienie „Evoke LINK". Poprawione więc to, co realnie generujemy: (1) **BŁĄD: zdarzenia dublowały nazwę aplikacji** — `event.service.log` wysyłał `title: 'Evoke LINK'`, co z atrybucją czytało się jako „Evoke LINK from Evoke LINK"; teraz `EVENT_LABELS` (mapa dla `NOTIFY_TYPES`: uploaded → „Nowe pliki od klienta", approved → „Klient zatwierdził pliki", paid_declared → „Klient zgłosił wpłatę" itd., fallback „Powiadomienie") jako tytuł, a opisowe zdanie z historii jako treść. (2) **Generyczne „Nowa wiadomość"** — w portalach pola „Imię"/„E-mail" są ukryte (`msgKnownSender`, v0.99.17), więc `senderName` bywa pusty MIMO że znamy `clientId`; `notifyMessage` dociąga nazwę klienta → „Wiadomość od Firma Kowalski". Kolejność: podpis nadawcy → nazwa klienta → „Nowa wiadomość". (3) **Przełącznik `pwa.pushBody`** (domyślnie ON) „Pokazuj treść wiadomości w powiadomieniu" — wyłączony pokazuje tylko KTO napisał (powiadomienia widać na ekranie blokady). Dotyczy WYŁĄCZNIE wiadomości od klientów; opisy zdarzeń to nasze teksty, nie treść klienta. Architektura: bramka `canSend()` wydzielona z `notify()`, więc nazwa klienta dociągana jest DOPIERO po jej przejściu — przy wyłączonym pushu zero dodatkowych zapytań. Formatowanie przeniesione z `message.service` do `push.service` (to on odpowiada za prezentację). Test w `test/push.test.js` (etykiety + brak nazwy aplikacji w tytułach, trzy warianty nadawcy, przełącznik prywatności, zdarzenia nietknięte przez przełącznik); zweryfikowane przechwyceniem REALNEGO ładunku (stub `webpush.sendNotification`).
- [x] Żywe wiadomości — wątek jak w komunikatorze (v0.99.96): wątek odświeża się SAM i wysyłka nie przeładowuje strony — po obu stronach (panel `/admin/messages` i wątek klienta `/p /c /t /upload /o`). **Polling, nie websockety**: Passenger usypia proces na shared hostingu, więc trwałe połączenia są zawodne; wzorzec 1:1 z `public/js/badges.js` — interwał **5 s** TYLKO gdy karta widoczna + natychmiastowy strzał po powrocie do karty, guard `busy`, cichy `catch`. Serwer oddaje **gotowe bąbelki z tego samego partiala** co render strony (`admin/messages/_bubble.ejs`, `public/_msg_bubble.ejs`) — zero duplikowania markupu w JS. Serwis: `scopeWhere` (jeden kształt wątku dla widoku/pollingu/oznaczania), `newerThan`, `conversationNewerThan`, `conversationLastBefore` (żeby dopisany bąbelek nie powtarzał chipa kontekstu), `readIdsFor` (hak na ptaszki). Endpointy JSON `{lastId, html, readIds}` z `no-store`: `GET /admin/messages/poll?client=&after=` (za `requireAuth`) i `GET /:surface/:token/wiadomosci/poll?after=` ×5 — każdy przechodzi **TĘ SAMĄ bramkę** (token/hasło/dostępność) co `showMessages`; wspólny render w `utils/messagePoll.js`. Wysyłka: przy `Accept: application/json` kontrolery oddają bąbelek zamiast redirectu (fallback bez JS = dotychczasowy redirect). `public/js/live-messages.js`: dopisywanie + dedup po `data-msg-id` (wyścig „wysyłka + trwający polling"), przewijanie tylko gdy user jest przy dole, czyszczenie stanu Alpine composera, **natywne powiadomienie** w panelu gdy karta nieaktywna (zgoda proszona przy pierwszej wysyłce — wymaga gestu). **GOTCHA (kosztowała czerwony test)**: `admin/messages/index.ejs` dostawał local o nazwie `client` → truthy `client` przełącza EJS w tryb client-side i `include()` przestaje istnieć („include is not a function"); local przemianowany na `convClient` (wzorzec `portalClient`/`onbClient`), parametry URL `?client=` zostają. Test `test/messages-live.test.js` (kursor, izolacja wątków, bramka logowania, `no-store`, JSON vs redirect) + E2E dwoma kontekstami: klient→panel i panel→klient bez reloadu, bez duplikatów.
- [x] Ptaszki „wysłane / przeczytane" zamiast komunikatu o wysyłce (v0.99.97): przy WŁASNEJ wiadomości pojawia się ✓ (wysłane) i ✓✓ w kolorze (przeczytane przez drugą stronę) — w panelu przy `out`, u klienta przy `in`. **Zero migracji**: `Message.isRead` było używane wyłącznie dla `in` (dzwonek), więc dla `out` było wolne — teraz znaczy „przeczytane przez drugą stronę" dla OBU kierunków. Nowe `markThreadOutRead(scope)` wołane w 5 handlerach `showMessages` (klient otwiera wątek = czyta agencję), obok istniejącego `markClientRead` (agencja otwiera rozmowę = czyta klienta). Ikony `tick`/`checkCheck` w `utils/icons.js`; render w obu partialach bąbelka jako `data-ticks`/`data-read` + warianty Tailwind `data-[read=1]:` (wymagał `build:css`), a `live-messages.js` przestawia stan po pollingu (`readIds`), więc ✓✓ zapala się bez przeładowania. Toast „Dziękujemy! Wiadomość wysłana" znika sam ze ścieżki z JS — przy wysyłce w miejscu nie ma redirectu `?msg=1`, który go pokazywał; kod toastu zostaje dla fallbacku bez JS. **GOTCHA**: wariant `data-[read=1]:` działa NA DANYM elemencie, więc znacznik siedzi też na obu ikonach (nie tylko na wrapperze) — inaczej po pollingu nie przełączyłby się ✓ na ✓✓. Testy: `markThreadOutRead` nie rusza `unreadCount()` (dzwonek liczy tylko `in`), pusty scope = 0 zmian, bąbelek niesie `data-read`; E2E oba kierunki (klient→agencja i agencja→klient).
- [ ] Do zrobienia: (Etapy 1–4 restylingu i bezpieczeństwa zamknięte; backlog z przeglądu referencji TailAdmin/Twenty ZAMKNIĘTY — Cmd/Ctrl+K, awatary klientów, sortowanie/filtry i eksport CSV list, „Wymaga uwagi", wykres na pulpicie.) AI: tylko jako OPCJONALNA integracja z kluczem API (Groq/Gemini free tier) — lokalny model na shared hostingu niewykonalny; domyślnie wyłączone; do osobnej rozmowy z właścicielem. Portale bez przeładowania — Etap 2 domknięty (slice 1–4). (backup off-site — ODPUSZCZONY na życzenie właściciela: hosting/dostawca ma własny, sprawdzony system kopii zapasowych.) UWAGA: white-label per-klient/projekt POZA ZAKRESEM NA STAŁE (decyzja 2026-07-02) — nie proponować.

## Workflow Git
Nie pushować automatycznie. Bump wersji + wpis w changelogu dopiero po potwierdzeniu. W komunikatach/URL-ach redagować token dostępowy.
