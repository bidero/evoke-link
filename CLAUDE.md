# Evoke LINK — przewodnik projektu (dla Claude i dewelopera)

Prywatny system wymiany plików agencja ↔ klient (uproszczony, „agencyjny" WeTransfer).
Działa w dwóch kierunkach i grupuje wszystko w **Projektach**. Nazwa aplikacji: **Evoke LINK**
(katalog/repo: `evoke-link`; w historii bywała nazywana „Evoke Transfer").

Domena produkcyjna: **link.evoke.pl** (na razie dev lokalny; produkcja to finalny etap).

## Stack (i dlaczego taki)
- **Node.js + Express** — prosty, znajomy (deweloper ma tło PHP/WordPress).
- **SQLite + Prisma** — zero serwera bazy; backup = kopia pliku. Łatwa migracja na PostgreSQL (zmiana providera).
- **EJS + Tailwind (v3, CLI) + Alpine.js** — szablony serwerowe zamiast SPA: mniej kodu, łatwe utrzymanie, brak builda JS. Alpine z CDN (uwaga: strona klienta wymaga internetu — przed produkcją wrzucić lokalnie).
- **CommonJS** (require), nie ESM.
- Biblioteki świadomie „shared-hosting-safe" (bez kompilacji natywnej): **bcryptjs** (nie bcrypt), **cookie-session** (nie session-file-store — ten rzucał EPERM na Windows; cookie-session przeżywa restart i działa wszędzie), tokeny przez `crypto`, ZIP przez `archiver` (strumieniowo), mail przez `nodemailer`, upload przez `multer`.

## Uruchomienie lokalne
```bash
npm install
npm run prisma:deploy     # zastosuj migracje (dev: prisma migrate dev)
npm run build:css         # Tailwind → public/css/app.css (po każdej zmianie klas!)
npm start                 # http://localhost:3000 → /admin/login (dane z .env)
```
Login admina: `.env` → `ADMIN_EMAIL` / `ADMIN_PASSWORD` (lub `ADMIN_PASSWORD_HASH`, `npm run hash -- "haslo"`).

## Hosting / deploy (SeoHost.pl, DirectAdmin + Passenger)
- Plik startowy: `app.js` (root). Restart: `touch tmp/restart.txt`.
- Deploy: `git pull && npm install && npm run prisma:deploy && touch tmp/restart.txt`. (CSS jest w repo — NIE budujemy Tailwinda na produkcji; `prisma` jest w dependencies, więc migrate deploy działa po produkcyjnym npm install.)
- Sprzątanie wygasłych: cron w DirectAdmin → `node src/jobs/cleanup.job.js` (NIE node-cron — Passenger usypia proces).
- Duże pliki: upload na razie zwykły multipart; przed produkcją dołożyć **chunked upload** (limit rozmiaru requestu na shared hostingu).

## Struktura
```
app.js                    entry (Passenger)
src/app.js                konfiguracja Express
src/config/               wczytanie .env
src/db/client.js          singleton Prisma
src/routes/               admin / public / auth
src/controllers/          obsługa requestów (cienka warstwa)
src/services/             logika: transfer, project, storage, zip, mail, event, auth
src/middleware/           auth, upload (multer), error
src/utils/                format (rozmiary/daty), icons (helper SVG Lucide)
src/views/                EJS (layouts / admin / public / errors)
src/jobs/cleanup.job.js   cron
prisma/schema.prisma      model danych
storage/                  pliki użytkowników + evoke.db (poza repo, .gitignore)
```

## Model danych (Prisma)
- **User** — konto agencji (w MVP logowanie z `.env`, tabela pod przyszłość).
- **Client** — klient agencji (CRM). Pola: `name` (pełna/wyświetlana nazwa), `firstName?`/`lastName?` (personalizacja maili — placeholdery `{imie}`/`{nazwisko}`), `email?`, `company?`, `phone?`, `nip?`, `address?`, `status` lead|active|inactive, `tags?`, `token` (portal klienta `/c/:token`), `note?`. Onboarding: `onboardingToken?` (unikalny, formularz `/onboard/:token`), `onboardingExpiresAt?` (7 dni), `onboardingCompletedAt?` (null = niewypełniony; token zostaje po wypełnieniu — konsumpcję wyznacza `completedAt`, ponowna wizyta = karta „Dziękujemy"). Ma wiele projektów i pozycji rozliczeniowych.
- **Project** — pojemnik. `clientId?` (relacja do Client) / `clientName?` (legacy etykieta), `clientToken` (unikalny, link panelu `/p/:token`), `clientPasswordHash?`, `position` (kolejność drag&drop), `status` active|archived.
- **Charge** — pozycja rozliczeniowa, kwota NETTO w GROSZACH (`amount`). Należy do `projectId?` **lub** `clientId?` (dokładnie jedno; klient pozycji = `clientId ?? project.clientId`). Pola: `label?`, `amount` (netto), `vatRate?` (% VAT 23/8/5/0; null = bez VAT → netto=brutto; brutto = `amount + round(amount*vatRate/100)` przez `chargeService.grossOf`), `date?`, `dueDate?` (wiekowanie należności + przypomnienia), `paidAt?`, `remindedAt?`, `note?`. `chargeService.totals()` zwraca `{net,vat,gross,total(=brutto),paid,outstanding}` — wszystkie sumy/kafelki liczą BRUTTO (agregaty przez `findMany`+`grossOf`, bo SQL `_sum` nie liczy VAT).
- **Transfer** — JEDEN model dla obu kierunków: `direction` = `outgoing` (agencja→klient, `/t/:token`) lub `incoming` (klient→agencja, `/upload/:token`). Pola: `token`, `passwordHash?`, `expiresAt?`, `maxDownloads?`, `downloadCount`, `clientVisible` (czy w panelu klienta), `notifyOnDownload` (e-mail przy 1. pobraniu), `projectId?`, `status` active|expired|deleted.
- **File** — `originalName`, `storedName`, `storedPath` (względna do storage), `size` (BigInt!), `mimeType`. onDelete Cascade.
- **Event** — historia + powiadomienia + dane widżetów. `type` (created|downloaded|uploaded|viewed|email_sent|note|updated|expired|error|onboarded), `projectId?`, `transferId?`, `clientId?` (oś czasu klienta), `isRead`, `dismissed`, `meta` (JSON string). Powiadomienia (dzwonek) = tylko `NOTIFY_TYPES` (uploaded|downloaded|error|approved|changes|onboarded|paid_declared|offer_accepted|offer_rejected); `viewed` jest w osi czasu, ale NIE dzwoni. Klik w powiadomienie: transfer → projekt → klient (fallbacki w `notification.controller.open`).
- **Message** — wiadomość klient↔agencja. `direction` in|out, kontekst `clientId?`/`projectId?`/`transferId?` (definiuje wątek), `senderName?`/`senderEmail?`, `isRead` (dla agencji). Skrzynka panelu grupuje po kontekście (wątki); klient widzi wątek w popupie + badge nowej odpowiedzi (sesja `msgSeen`).
- **Retainer** — cykliczna pozycja rozliczeniowa klienta (abonament). `clientId` (Cascade), `label`, `amount` (NETTO, grosze), `vatRate?`, `dayOfMonth` (1–28), `dueDays` (termin +X dni), `active`, `lastPeriod?` ('YYYY-MM' — anty-duplikat), `note?`. Cron (`reminders.job`) generuje z aktywnych zwykłe `Charge`; MRR = suma brutto aktywnych.
- **Reminder** — zadanie/przypomnienie kalendarza (menedżer zadań). `title`, `note?`, `dueAt`, `done`/`doneAt?`, `priority` low|normal|high, opcjonalnie `clientId?`/`projectId?`. `calendar.service` agreguje je z terminami płatności (`Charge.dueDate`) i wygasaniem transferów (`Transfer.expiresAt`) w jeden strumień wydarzeń.
- **Settings** — branding/customizacja (1 rekord, `id=1`). Kolumny: `appName`, `logoPath`, `faviconPath`, `ogImagePath` (obraz podglądu linku OG), `customCss`, `panel` (JSON `{menu:[{key,hidden,label}], dashboard:[{key,hidden}]}` — układ panelu admina; listy kanoniczne + scalanie w `utils/panelUi.js`: zapis trzyma delty, nieznane klucze odrzucane, nowe pozycje doklejane, „Ustawienia" zawsze widoczne) + JSON-y: `colors` (`primary`, `adminAccent`, `adminText`, `adminSidebar`, `adminHeader` (pasek nagłówka, puste=jak sidebar), `adminBg`, `darkBg/darkSurface/darkText`), `texts` (`heroTitle`, `heroSubtitle`, `footer`), `background` (`type` gradient|custom|image|solid, `preset`, `color`, `custom`, `imagePath`/`images[]`/`rotate`/`rotateSec`, `overlay`, `imageGradient`+`imageGrad`, `grain`/`grainType`/`grainStrength`, `scroll`), `logo` (`size`, `align`, `darkPath`), `layout` (`style`,`card`,`cardSide`,`radius`,`button`,`stickyHeader`,`font`,`hideName`,`heroOnBg`,`applyToLogin`), `emails` (loga/tematy/wstępy + przypomnienia), `pdf` (szablon + dane sprzedawcy).
- **User** — w MVP login z `.env`, ale `auth.service.setAdminPassword` zapisuje hash admina do tej tabeli (po zmianie hasła w panelu baza ma pierwszeństwo nad `.env`).

## Konwencje i pułapki (WAŻNE)
- **GOTCHA EJS: NIE nazywać locala `client` w `res.render` widoku, który używa `include()`** — `ejs.renderFile` kopiuje z locals opcje kompilatora (`_OPTS_PASSABLE_WITH_EXPRESS`: m.in. `client`, `cache`, `views`, `async`, `strict`, `delimiter`); truthy `client` przełącza szablon w tryb client-side i `include` przestaje istnieć („include is not a function"). Dlatego widok onboardingu dostaje `onbClient`. (`client-portal.ejs` dostaje `client` i działa tylko dlatego, że nie ma include'a.)
- **Warstwa storage** (`storage.service.js`) — wszystkie operacje na plikach przez nią (łatwa zmiana na S3). Pliki w `storage/transfers/<token>/`.
- **Ikony**: `<%- icon('nazwa') %>` / `<%- eventIcon(type) %>` (helper `src/utils/icons.js`) — kontur w stylu Lucide, kolor `brand-600` (#6e00a5). BEZ emoji.
- **Kolor przewodni**: `#6e00a5` = `brand-600` (paleta `brand-*` w `tailwind.config.js`). Używać klas `brand-*`, nie `indigo-*`. Tailwind skanuje `src/views`, `src/assets/js`, `src/utils`.
- **Sesja**: cookie-session. Wylogowanie = `req.session = null` (NIE `.destroy()`).
- **Hasła** (transfer/projekt): bcryptjs; odblokowanie zapamiętane w sesji (`unlocked` / `portalUnlocked`).
- **Formularze uploadu (`new.ejs`, `upload.ejs`, `portal.ejs`)** wysyłają pliki przez **XHR** i ręcznie doklejają pola do FormData — przy dodaniu nowego pola PAMIĘTAJ dopisać je w skrypcie (był bug: `projectId`/`clientVisible` nie były wysyłane).
- **Prisma generate** na Windows rzuca `EPERM` na pliku silnika, gdy działa lokalny serwer → **zatrzymaj serwer przed `prisma generate`/`npm install`**.
- **Migracje**: `prisma migrate dev` bywa nieinteraktywne i odmawia przy ostrzeżeniach (np. nowy UNIQUE index). Wtedy: napisz `prisma/migrations/<ts>_nazwa/migration.sql` ręcznie i `prisma migrate deploy`.
- **BigInt**: `File.size` to BigInt — formatować przez `fmt.bytes()`, nie serializować wprost do JSON.
- **Po zmianie klas Tailwind**: `npm run build:css`. **Po zmianie tylko widoków EJS**: wystarczy refresh (dev nie cache'uje).
- **Customizacja (Etap 5–6)** — kluczowe miejsca:
  - Kolory bez rebuildu: paleta `brand-*` to zmienne CSS (`--brand-*`); middleware w `app.js` wstrzykuje `brandStyleTag` (z `primary`, dla stron klienta/logowania) i `adminStyleTag` (z `adminAccent` + `--admin-bg/-text/-sidebar/-sidebar-text/-header/-header-text`, dla panelu). `utils/color.js` generuje paletę + `readableText` (auto-kontrast).
  - Tło stron klienta: `utils/background.js` (`bodyStyle`/`overlayHtml`/`isDark`, presety + własny gradient `custom`, ziarno z `grainStrength`). W layoutach: `bgStyle` na `<body>`, `bgOverlay` jako nakładki (`z-0`), treść w `z-10`.
  - **Tekst na tle**: `<body>` stron klienta/logowania jest ZAWSZE ciemny (`text-slate-800`) — bo treść siedzi w białych kartach. Jasny kolor (gdy `bgDark`) dostaje tylko „chrome" na gradiencie: nagłówek (logo/nazwa) i stopka. NIE ustawiać jasnego tekstu na całym `body` (regresja: niewidoczne `h1` na kartach).
  - **x-data w `settings.ejs`**: stan wstrzykiwany przez `x-data="settingsForm(<%= JSON.stringify(formState) %>)"` — MUSI być `<%=` (HTML-escape), NIE `<%-`. Przy `<%-` cudzysłowy JSON-a urywają atrybut → Alpine pada → color-inputy czernieją (był bug „drugi zapis = czarne").
  - Upload brandingu: `middleware/brandingUpload.js` (pola `logo`/`favicon`/`bg`, do 5 MB, też SVG). SVG czyszczone przez `utils/svgSanitize.js`. Własny CSS czyszczony przez `utils/css.js` (usuwa `<`), wstrzykiwany jako `customStyleTag` we WSZYSTKICH layoutach.
  - Zmiana hasła: strona `/admin/account` (`account.controller`), `auth.service.verifyCredentials` jest **async** (sprawdza DB, potem `.env`). Reset z CLI: `npm run set-password -- "haslo"` / `-- --clear`.
  - Hero/podpis: renderowane w `layouts/public.ejs` (blok nad kartą, gdy `texts.heroTitle` ustawiony), kolor adaptacyjny do jasnego/ciemnego tła.
- **Chunked upload (dzielenie dużych plików)** — omija limit rozmiaru requestu hostingu. Architektura drop-in:
  - Klient: `public/js/chunked-upload.js` → `window.chunkedUpload(files, '<endpoint>/chunk', onProgress)` dzieli pliki na 5 MB i wysyła sekwencyjnie, zwraca `uploadId`. Potem widok wysyła żądanie tworzące (urlencoded) z nagłówkiem `X-Upload-Id`.
  - Serwer: `chunk.service.js` skleja kawałki w `storage/tmp/chunks/<uploadId>/`; `middleware/chunkUpload.js` → `receiveChunk` (endpoint `/chunk`, surowe bajty) + `receiveUpload(field)` zastępuje `multer.array` (gdy jest `X-Upload-Id` → składa pliki w `req.files` w kształcie multera, inaczej multipart fallback). **Kontrolery i serwisy bez zmian** — `req.files` ma ten sam kształt `{ originalname, path, size, mimetype }`.
  - Endpointy `/chunk` MUSZĄ być przed trasami `:id` (np. `/transfers/chunk` przed `/transfers/:id`). Porzucone sesje sprząta `chunk.sweepOld()` (start aplikacji, >24h).
  - **Równoległość**: klient wysyła kawałki współbieżnie (pula 3). Dlatego serwer zapisuje KAŻDY kawałek do osobnego pliku `<fi>_<ci>.part` i skleja je po indeksach przy `assembleFiles` (NIE dopisuje sekwencyjnie — kolejność przyjścia jest dowolna).
  - **Sklejanie jest async + strumieniowe** (`assembleFiles` zwraca Promise; `receiveUpload` robi `await`) — nie blokuje pętli zdarzeń przy dużych plikach. Klient po wysłaniu kawałków pokazuje stan „Finalizowanie…" (animowany pasek), bo na localhoście sam upload bywa zbyt szybki, by pasek był widoczny, a odczuwalne „zawieszenie" to właśnie sklejanie po stronie serwera.
- **Układ stron klienta (warianty wyglądu)** — `Settings.layout` JSON `{ style: classic|centered|split, card: solid|glass|elevated, cardSide: left|right (split), hideName, heroOnBg, applyToLogin, radius, button: rounded|pill }`. `centered` = landing z DUŻYM hero (text-5xl); `split` używa `cardSide` (kolejność przez `md:order-*`). `hideName` chowa nazwę aplikacji (placeholder w nagłówku bez logo + fallback w kolumnie hero). Tło-obraz ma opcję `background.imageGradient` (nakładka gradientu brandowego) i mocniejszy szum (mix-blend `overlay`). Render w `layouts/public.ejs` (3 warianty kompozycji). Styl karty/rogi/przycisk przez zmienne CSS (`--card-*`, `--btn-radius`) wstrzykiwane jako `surfaceStyleTag` (app.js `surfaceVars`) + wspólne klasy `.evoke-card`/`.evoke-btn` (input.css) używane przez 5 widoków publicznych i logowanie. Hero pokazywany gdy `heroTitle && heroOnBg` (na wszystkich układach). **GOTCHA (nawracający, GPU-zależny): artefakt „gradientu” przy tytule karty „szkło”.** Warstwa tła (`position:fixed`, `background.js overlayHtml`) odsłania się `opacity 0→1` po `onload` zdjęcia, JUŻ PO pierwszym malowaniu karty z `backdrop-filter` → część GPU trzyma nieświeżą próbkę backdropu (ciemny/gradientowy pas za tytułem, znika przy scroll/resize). Instant-reveal (v0.83.2, bez `transition`) łagodzi, ale nie usuwa. Trwały fix: `public/js/bg-rotate.js` po odsłonięciu wymusza re-sample kart (`.evoke-card`/`.evoke-panel`) — na 1 klatkę `blur(22px)`, potem powrót do CSS. NIE „naprawiać” tego w CSS karty ani przez powrót do animowanego reveal.
  - **GOTCHA nazwa `uiLayout`**: ustawienia układu w res.locals są pod `uiLayout`, NIE `layout` — `layout` koliduje z express-ejs-layouts (nazwa pliku układu z kontrolera; w widoku to string).
  - **GOTCHA kontrolki**: radia/checkboxy układu w `settings.ejs` są NATYWNE (EJS `checked`, bez Alpine `x-model`/`x-for`) — wzorzec `<template x-for>`+`x-model` na radiach był zawodny w przeglądarce (nie dało się wybrać). Podświetlenie działa przez `peer-checked` natywnie. Reaktywne kontrolki (kolory/tło) dalej na Alpine, bo mają podgląd na żywo.

## Testy
`npm test` = `node --test --test-concurrency=1` na katalogu `test/` (smoke + przepływ wiadomości + ostrzeżenie o wygasaniu i in.). Każdy plik startuje `app.listen(0)`, woła endpointy globalnym `fetch`-em, sprząta utworzone dane i kończy się sam. Uwagi:
- **`--test-concurrency=1` jest OBOWIĄZKOWE** — pliki testowe współdzielą wiersz Settings (id=1); przy równoległych procesach snapshot/restore przeplatały się i wskrzeszały stany „ze środka" innych testów (użytkownikowi zmieniał się wygląd po `npm test`). Testy piszące Settings: snapshot + restore w `finally` ORAZ jawnie przypinać zależne pola (np. `layout.style`), nie dziedziczyć ich z ustawień użytkownika.
- działa na **dev-DB** (`storage/evoke.db`) — testy tworzą i kasują własne rekordy; uruchamiaj lokalnie.
- login admina wymaga `ADMIN_PASSWORD` w `.env` (test wiadomości robi `t.skip` bez niego).
- cookie-session ustawia 2 ciasteczka (`evoke` + `evoke.sig`) — brać oba przez `res.headers.getSetCookie()`.
- joby (np. `reminders.job.js`) mają guard `require.main === module`, więc `require` w teście nie odpala `run()` — można wołać wyeksportowane funkcje (`runExpiryWarnings`).

## Status / roadmapa
- [x] Etap 0 — szkielet, logowanie, layout, model danych
- [x] Etap 1 — transfery wychodzące (`/t/:token`, hasło, wygasanie, limit, ZIP, edycja)
- [x] Etap 2 — uploady przychodzące (`/upload/:token`, e-mail)
- [x] Etap 3 — projekty + historia
- [x] Etap 4 — dashboard (realne dane) + powiadomienia (dzwonek, licznik)
- [x] Panel klienta — portal projektu `/p/:token` (hasło, widoczność per transfer `clientVisible`, pobieranie + upload)
- [x] Etap 5 — customizacja (logo/kolory/treści z panelu; model Settings; kolor bez rebuildu)
- [x] Etap 6 — rozszerzona customizacja: tła stron klienta (presety/własny gradient/obraz/ziarno z mocą), osobne kolory panelu (akcent/sidebar/tło/czcionka, auto-kontrast), logo (rozmiar+pozycja, SVG sanityzowane), brandowane logowanie, zmiana hasła admina (DB), pole na własny CSS
- [x] Chunked upload — dzielenie dużych plików na 5 MB (panel + /upload + portal), drop-in z fallbackiem multipart, sprawdzone E2E (integralność md5)
- [x] Hero/podpis renderowane na stronach klienta; reset hasła z CLI (`npm run set-password`)
- [x] Warianty układu stron klienta (klasyczny/karta-na-tle/hero+karta) + styl karty (biel/szkło/uniesiona) + rogi/przycisk + branding logowania; równoległy chunked upload (pula 3)
- [x] Bezpieczeństwo/ops: backup automatyczny (`src/jobs/backup.job.js`), multer 2.x, rate-limit (login + hasła klienta), Alpine/Sortable lokalnie + CSP (helmet), nodemailer 9 (audit: 0 vuln)
- [x] Rozliczenia: `Charge` (projekt/klient, grosze, `dueDate`), kafelek przeterminowanych, CSV + PDF (pdfmake) + wysyłka mailem, przypomnienia cronem (`jobs/reminders.job.js`)
- [x] CRM: baza klientów + strona 360° (Przegląd/Projekty/Rozliczenia/Transfery/Oś czasu), portal klienta `/c/:token`, `firstName`/`lastName` + personalizacja maili
- [x] Globalna wyszukiwarka (`services/search.service.js`, `/admin/search`, pole w nagłówku) — klienci/projekty/transfery (też po imieniu/nazwisku)
- [x] Więcej układów stron klienta (showcase/panel/panel-bg/sidebar/corner/hero-card/minimal/banner) + sticky header + typografia + gotowe motywy; dark mode (klasa, auto-kontrast)
- [x] Uczciwe placeholdery e-mail per-pole (`mail.PLACEHOLDER_SUPPORT`); osobny kolor paska nagłówka panelu (`colors.adminHeader`)
- [x] Podgląd linku OG/meta + własny obraz OG (`Settings.ogImagePath`); „klient otworzył link" (`Event(type:viewed)` na `/t` i `/p`); kod QR linku (inline SVG, `utils/qr.js`)
- [x] Miniatury obrazów + Quick Look (lightbox) w panelu transferu (`/admin/transfers/:id/preview/:fileId`); fix migania gradientu tła za kartą „szkło" (warstwa odsłaniana po `onload`)
- [x] Wiadomości klient↔agencja (dwukierunkowo): koperta + wątek (popup) na `/p`/`/t`/`/c` + badge nowej odpowiedzi; panel = widok wątków (grupowanie po kontekście) + odpowiedź + mail; model `Message`
- [x] Ostrzeżenie o wygasaniu transferu — mail do agencji o wychodzących wygasających <24 h, niepobranych (cron `reminders`, toggle `emails.expiryWarn`, anty-powtórka `Transfer.expiryWarnedAt`)
- [x] Testy `node:test` (`npm test`): smoke + przepływ wiadomości + ostrzeżenie o wygasaniu (`test/*.test.js`)
- [x] Kalendarz / menedżer zadań — siatka miesiąca + „Nadchodzące", przypomnienia (status/priorytet, powiązanie z klientem/projektem) + agregacja terminów płatności i wygasania; badge „do zrobienia" w menu (`calendar`/`reminder` service, `/admin/calendar`)
- [x] Automatyzacja: „otworzył link" też na `/upload` i `/c`; „Przedłuż transfer" (reset ostrzeżenia + reaktywacja); dzienny digest do agencji = także maile o przypomnieniach na dziś (toggle `emails.dailyDigest`, cron `reminders`)
- [x] Puls agencji (`/admin/pulse`, `stats.service.pulse()`): przychód mies. + delta, do zapłaty/przeterminowane, wykres 6 mies. (inline SVG), skuteczność pobrań (30 dni), aktywni klienci, top klienci wg przychodu — bez nowego modelu
- [x] Fala 3 (drag&drop): kanban projektów `/admin/projects/board` (`Project.stage` lead|active|delivered|paid — niezależny od `status`; przeciąganie kart = POST `:id/stage`) + D&D w kalendarzu (przeciągnięcie przypomnienia na inny dzień = `reminders/:id/move`, godzina zostaje)
- [x] Proofing: akceptacja/poprawki plików per transfer wychodzący (opt-in checkbox `Transfer.proofing`; klient na `/t` i w portalu `/p` klika Zatwierdzam / Poprawki+komentarz → `approvalStatus/Comment/By/At`, event `approved|changes` w NOTIFY_TYPES + mail do agencji; status w panelu transferu i chip w portalu)
- [x] Lista braków (file requests): model `FileRequest` (per projekt, Cascade); sekcja na stronie projektu (dodaj/odhacz/usuń); checklista w portalu `/p` + select „Czego dotyczą te pliki?" przy uploadzie → auto-odhaczenie punktu i link do transferu (GOTCHA: finalize w portal.ejs dokleja `fileRequestId` ręcznie — XHR)
- [x] Osobne logo per powierzchnia: `Settings.logo.{adminPath,adminDarkPath,loginPath,loginDarkPath}` (JSON — bez migracji); `logoTag(settings,{surface})` + `hasLogo` (utils/logo.js). Reguła: własne jasne logo powierzchni → ciemne tylko z JEJ pary (bez mieszania z globalnym ciemnym); puste = dziedziczy bazowe. GOTCHA: logoCfg w settings.controller MUSI przenosić te pola przy zapisie
- [x] Własne tło strony logowania: kolumna `Settings.loginBackground` (JSON jak `background`; null = dziedziczy tło klienta); locals `loginBg*` w app.js (fallback do `bg*`); auth.ejs używa loginBg*; UI w Ustawienia → Wygląd („Strona logowania — tło": preset/własny gradient/kolor/obraz+przyciemnienie, upload `loginBgImage`)
- [x] Kwoty netto + opcjonalny VAT (`Charge.vatRate`, migracja `20260702200000`): `amount` = netto, select VAT (bez/23/8/5/0%) w formularzach projektu i klienta; rozbicie netto/VAT/brutto w pozycjach, podsumowaniach, PDF (kolumny warunkowe) i CSV; wszystkie sumy „do zapłaty" liczą brutto
- [x] Archiwizacja projektu jednym kliknięciem: `POST :id/archive` (toggle active↔archived) — przycisk na kafelku listy (stretched-link, `redirect=list`) i na stronie projektu; `projectService.setStatus`
- [x] Zmiana daty rozliczenia po oznaczeniu: edytowalne pole daty przy pozycji (`chargeService.setPaidDate`; projekt: `POST :id/charges/:cid/paid-date`; klient: przez `updateCharge` z polem `paidAt`, auto-submit)
- [x] Sortowanie klientów po polsku (`localeCompare('pl')` w `client.service` `list()`+`options()` — SQLite ORDER BY jest BINARY); tablica kanban 4 kolumny od `lg` (było `xl` → dwie pod sobą)
- [x] Wybór sortowania na liście klientów: select `sort` (nazwa/firma/nazwisko × rosnąco/malejąco) w tym samym formularzu co szukajka+status (zachowują się nawzajem); `client.service.SORTS` + `sortClients` (puste wartości zawsze na końcu, remis rozstrzyga nazwa)
- [x] Dopasowanie do brutto po VAT (v0.85.2): Puls (`stats.service` — przychód mies./wykres/top klienci/do zapłaty/przeterminowane), kalendarz (tytuł terminu płatności), maile (przypomnienie o płatności, digest), logi zdarzeń przy dodaniu/rozliczeniu pozycji, lista wyboru pozycji przy wydruku — wszystko przez `grossOf`
- [x] Link onboardingowy: jednorazowy formularz `/onboard/:token` (7 dni, regeneracja wymienia token), klient sam uzupełnia firmę/NIP/adres/kontakt (e-mail tylko gdy pusty w kartotece); sekcja w kartotece 360° (utwórz/kopiuj/QR/wyślij mailem/wygeneruj nowy + chip stanu), event `onboarded` (dzwonek) + mail do agencji, szablon maila w Ustawienia → E-mail (`emails.onboardSubject/onboardIntro`); test `test/onboarding.test.js`
- [x] Konfigurowalny panel: pulpit z widżetami (rejestr `utils/panelUi.js`, partiale `views/admin/_widgets/`; 6 kafelków + akcje + aktywność + NOWE: zadania z kalendarza, przychód+top klienci z Pulsu, nieprzeczytane wiadomości) — edycja na pulpicie (ikonka ołówka przy tytule w nagłówku layoutu → `CustomEvent('dash-edit')` łapany przez `@dash-edit.window`; drag&drop Sortable, oko ukrywa, przełącznik szerokości ¼/⅓/½/⅔/1 = `span` 3/4/6/8/12 na siatce `lg:grid-cols-12`; kontrolki Zapisz/Anuluj w pływającym pasku na dole — nic nie przesuwa treści; POST `/admin/dashboard/layout`; dane pobierane tylko dla widocznych, ukryte = placeholder) + menu boczne z ikonami (kolejność/ukrywanie/własne nazwy w Ustawienia → Panel; scalone `res.locals.panelMenu` w admin.routes); `Settings.panel`, migracja `20260703120000`; test `test/panel.test.js` (snapshot+restore Settings!)
- [x] Dane do przelewu w portalu /c: sekcja „Do zapłaty" (nierozliczone pozycje BRUTTO wprost + z projektów — `chargeService.unpaidForClient`, zaległe na czerwono, suma) + blok danych przelewu z `Settings.pdf.seller` (odbiorca/konto/tytuł „Rozliczenie — {klient}") + **QR przelewu wg rekomendacji ZBP** (`utils/payment.js`: `bankDigits` → 26 cyfr NRB albo brak QR, `zbpPayload` `NIP|PL|konto|kwota gr|odbiorca|tytuł|||`); blok konta tylko gdy `seller.bank` ustawiony; przełącznik `pdf.portalBilling` (Ustawienia → Rozliczenia (PDF), domyślnie włączony) chowa całą sekcję. **GOTCHA QR**: `qrcode-generator` domyślnie obcina znaki do 1 bajta — `utils/qr.js` ustawia `stringToBytesFuncs['UTF-8']` (inaczej polskie znaki w payloadzie wychodzą zmielone po skanie)
- [x] Widżet pulpitu „Do odezwania się" (`followup`): klienci bez żadnej aktywności ≥30 dni (`clientService.staleClients` — max z Event/Message wprost i przez projekty, fallback createdAt, bez statusu inactive; 60+ dni na czerwono); przycisk „Przypomnij" = Reminder „Odezwij się: X" na jutro 9:00 (`POST /clients/:id/followup`), otwarte przypomnienie → chip „zaplanowane" (bez dublowania)
- [x] Fix: transfer przypisany do projektu klienta podstawia jego e-mail w polu „Wyślij link e-mailem" (`transfer.getById` dołącza `project.client`)
- [x] Retainery (cykliczność): model `Retainer` (per klient: `label`, `amount` NETTO gr, `vatRate?`, `dayOfMonth` 1–28, `dueDays`, `active`, `lastPeriod` 'YYYY-MM' = anty-duplikat; migracja `20260704090000`); cron `reminders.job → runRetainers()` (odpalany PRZED bramką SMTP — nie wymaga maili; bez generowania wstecz) tworzy z aktywnych zwykłe `Charge` (label „X — lipiec 2026", termin +dueDays) → działają przypomnienia/sumy/PDF; sekcja „Cykliczne (retainery)" w 360°→Rozliczenia (dodaj/wstrzymaj/usuń/„Generuj teraz" z anty-duplikatem, alerty `ret-*`); **MRR** (`retainer.service.mrrActive` brutto) = kafelek + lista w Pulsie (`stats.pulse().retainers`) i dopisek w widżecie przychodu; test `test/retainer.test.js`
- [x] Mail do klienta o nowej pozycji cyklicznej: toggle+szablon `emails.retainerNotify/retainerSubject/retainerIntro` (Ustawienia → E-mail, domyślnie WYŁĄCZONY); `retainer.service.notifyClient` po każdej generacji (cron i „Generuj teraz"; kwota brutto, termin, dane przelewu, link do portalu /c; loguje event `email_sent`, błąd nie blokuje generacji)
- [x] „Zgłoś wykonanie przelewu" w portalu /c: przycisk pod „Do zapłaty" (`POST /c/:token/paid`, messageLimiter, bramka `pdf.portalBilling`) → event `paid_declared` (NOTIFY_TYPES — dzwoni; ikona banknote, bursztyn) + mail do agencji (`sendPaymentDeclared`); chip „Zgłoszono wpłatę {data} — czekamy na potwierdzenie" przez 7 dni = anty-duplikat (jedna deklaracja/7 dni); NIE oznacza pozycji jako opłaconych — potwierdza agencja
- [x] Ikony widżetów pulpitu: `icon` w rejestrze `panelUi.WIDGETS` (nowe PATHS: trendingUp, phone) — kafelki mają ikonę przy etykiecie (prawy róg, slate-300), widżety treściowe w nagłówku h2 (partiale dostają `wicon` przez include)
- [x] Szablony projektów: model `ProjectTemplate` (JSON `items` — migracja `20260704120000`), `template.service` (definicja TEKSTEM: braki `Etykieta | notatka`, przypomnienia `Tytuł | +dni | priorytet` — linia = pozycja, zero JS w formularzu); strona `/admin/projects/templates` (lista+dodaj+usuń, przycisk „Szablony" na liście projektów), select w „Nowy projekt" → `applyToProject` rozstawia FileRequests + Remindery (termin = utworzenie + offsetDays, 9:00, projectId+clientId); test `test/templates.test.js`. GOTCHA: trasa `/projects/templates` PRZED `/projects/:id`
- [x] Interakcje 360°: formularz notatki na osi czasu z typem (notatka/telefon/spotkanie/e-mail — `NOTE_KINDS` w client.controller, prefiks w message + `meta.kind`) i polem „follow-up za X dni" (Reminder „Follow-up: {klient}" 9:00)
- [x] Wskaźniki na 360° (zakładka Przegląd): karta „Statystyki klienta" — LTV (suma zapłaconego brutto), śr. czas płatności (dni od `Charge.date`/createdAt do `paidAt`), wykres przychodu 12 mies. (zapłacone brutto per miesiąc, inline SVG). Liczone przez `clientService.clientMetrics(charges)` z już wczytanych pozycji w `overview` — zero dodatkowych zapytań; test w `test/crm.test.js`
- [x] Oferty/wyceny z akceptacją w portalu: modele `Offer`+`OfferItem` (migracja `20260704160000`; pozycje netto/szt.+ilość+VAT), `offer.service` (definicja pozycji TEKSTEM „Etykieta | netto | VAT | ilość"; `totals`/`state` open|accepted|rejected|expired; `decide` — akceptacja tworzy Charge z pozycji [projektowe albo wprost na kliencie], rusza projekt `lead→active` na kanbanie, log `offer_accepted`; odrzucenie z komentarzem → `offer_rejected`). Publiczna strona `/o/:token` (`public/offer.ejs`: pozycje, suma, Akceptuję/Odrzucam — odrzucenie WYMAGA komentarza, `:disabled` na Alpine + walidacja serwera). Admin: zakładka „Oferty" w 360° (`offer.controller` admin+public jak onboarding; utwórz/kopiuj link/wyślij mailem/usuń, chipy stanu). Lista ofert w portalu `/c` (wygasłe ukryte). Maile: `sendOfferLink` (klient) + `sendOfferDecision` (agencja). Nowe NOTIFY_TYPES `offer_accepted`/`offer_rejected` (dzwonek). Test `test/offers.test.js`
- [x] Załączniki w wiadomościach: pojedynczy plik na Message (`attachmentPath/Name/Size/Mime`; migracja `20260704180000`). Formularz w popoverze `/p /t /c` = `multipart/form-data` + input „Dołącz plik" (`middleware/messageUpload.js` → multer do tmp; `message.service.create` przenosi do `storage/transfers/_messages/` przez `storage.saveMessageFile`, sprząta tmp gdy treść pusta). Bąbelki: badge z nazwą u klienta (public.ejs), link pobierania w skrzynce (`GET /admin/messages/:id/attachment`, requireAuth, `Content-Disposition: attachment`). `deleteThread` kasuje pliki z dysku. Backup obejmuje je automatycznie (są pod `transfers/`). Test `test/attachments.test.js`
- [x] Lejek sprzedaży (widok „Sprzedaż", `/admin/sales`): `offer.service.pipeline()` agreguje oferty ponad klientem — otwarte (sort po najbliższym terminie ważności) + wartość lejka, wygasające w 7 dni, wygasłe (do odświeżenia), skuteczność (win rate = accepted/(accepted+rejected)) i wartość wygranych. Widok `admin/sales.ejs` (kafelki + lista z „Ponaglij" [reuse `/offers/:oid/send`] i „Otwórz"). Pozycja menu `sales` w `panelUi.MENU` (ikona `funnel`; dla zapisanej konfiguracji menu dokleja się na końcu). Test w `test/offers.test.js`
- [x] Dokumenty klienta: model `Document` (migracja `20260704200000`; plik w `storage/transfers/_documents/`, `visibleToClient`). `document.service` (list/listVisible/create[multer→`storage.saveDocumentFile`]/toggleVisible/remove[kasuje plik]). Zakładka „Dokumenty" na 360° (`document.controller` admin: wgraj/toggle/pobierz/usuń; `middleware/documentUpload.js`, 50 MB). Pobieranie: admin `GET /admin/clients/:id/documents/:docId` (requireAuth), klient `GET /c/:token/documents/:docId` (tylko `visibleToClient` + własność). Sekcja dokumentów w portalu `/c` (widoczne). Backup obejmuje pliki (`transfers/`). Test `test/documents.test.js`
- [x] Upload całych katalogów + odporność sieci (v0.97.0): drag&drop folderu (`collectDataTransferFiles` → `webkitGetAsEntry`, rekurencyjnie) i input `webkitdirectory` na wszystkich dropzone'ach (`/upload`, `/p`, panel `new`); ścieżka względna (`katalog/plik.pdf`) jedzie jako `File.originalName` → ZIP odtwarza strukturę, a `Content-Disposition` przy pobraniu bierze samą nazwę (basename). Kawałki ponawiane z backoffem (0,5 s / 2 s, timeout 120 s) — koniec z „Błąd sieci podczas przesyłania" przy chwilowym zerwaniu łącza (typowo przy PDF-ach). `chunk.service`: sanityzacja ścieżek (wycina `..`, dyski, znaki sterujące), `MAX_FILES` 500→5000, wspólny `manifest.json` zastąpiony plikami `<fi>.meta` (bez read-modify-write per kawałek — koniec z 400 „Nieprawidłowy indeks pliku" przy dużej liczbie plików i wyścigu między procesami Passengera); pomijanie plików-śmieci (`.DS_Store` itp.). Test `test/folder-upload.test.js` (md5, kawałki poza kolejnością, 520 plików, sanityzacja `../`)
- [x] Chronologia rozliczenia PDF/CSV/mail (v0.97.1): `chargeService.forStatement` sortuje pozycje chronologicznie zamiast alfabetycznie po projekcie — bloki projektów wg NAJSTARSZEJ pozycji w projekcie (najstarsza czynność wypycha projekt na górę), wewnątrz od najstarszej do najnowszej; pozycja bez daty używa `createdAt` (nie skacze na górę jak przy sortowaniu SQL z NULL-first). `sortForStatement` grupuje po `projectId`, remis dat → nazwa (pl), „Bez projektu" na końcu remisu. Ekran 360°→Rozliczenia CELOWO zostaje newest-first (`overview` orderBy date desc) — podgląd bieżący, nie dokument. Test `test/statement-order.test.js`
- [x] Fix frosted glass w Safari (v0.97.2): brak `-webkit-backdrop-filter` w zbudowanym `app.css` — autoprefixer BEZ `browserslist` wycinał prefiks (choć Safari go wymaga). Dodany `browserslist` (`defaults` + `Safari >= 12` + `iOS >= 12`) → rebuild zachowuje prefiks (8× w app.css: `.evoke-card` glass, sticky-header, `.backdrop-blur`). Inline podglądu karty „szkło" w `settings.ejs` (autoprefixer go nie rusza) dostał `-webkit-` ręcznie. GOTCHA: produkcja serwuje zbudowany `app.css` z repo (Tailwinda nie budujemy na prod), więc prefiks MUSI być w committowanym buildzie
- [x] PDF: tytuł projektu nie osierocony na dole strony (v0.97.3 → POPRAWNE dopiero v0.97.4): węzeł tytułu ma `headlineLevel: 'projectTitle'`. GOTCHA: warunek `followingNodesOnPage.length === 0` z v0.97.3 NIC nie dawał — pdfmake liczy komórki tabeli jako „following" nawet gdy tabela zjeżdża niżej (zweryfikowane instrumentacją: dla osieroconego tytułu following=14, nie 0). Właściwy mechanizm (v0.97.4): (1) tabela dostała `keepWithHeaderRows: 1` (sam nagłówek kolumn nie zostaje na dole — cała tabela zjeżdża); (2) `pageBreakBefore` liczy REALNE miejsce pod tytułem: `startPosition.pageInnerHeight - startPosition.top < 90` (tytuł+nagłówek+wiersz) → przerzuca tytuł z tabelą. Zweryfikowane WIZUALNIE (Read PDF): „Sklep"/„Wizytówki" przechodzą w całości na kolejną stronę. `buildDoc` wyeksportowany do testu. Test `test/pdf-pagebreak.test.js`
- [x] Frosted glass w Safari — DEFINITYWNIE (v0.97.6, po pomyłkach v0.97.2/v0.97.5): PRAWDZIWA przyczyna potwierdzona empirycznie przez usera — **Safari NIE stosuje `-webkit-backdrop-filter` podanego przez `var()`** (inspektor pokazuje rozwiniętą wartość `blur(14px)`, ale silnik jej NIE renderuje; wpisany RĘCZNIE literał `blur(14px)` działa). To nie był ani brak `-webkit-` (v0.97.2), ani var-w-blur (v0.97.5). Fix: `.evoke-card` NIE ma już backdrop-filter w `app.css`; dla szkła `surfaceVars` (app.js) wstrzykuje LITERALNĄ regułę `.evoke-card{-webkit-backdrop-filter:blur(14px);backdrop-filter:blur(14px)}` w `surfaceStyleTag` (po linku do app.css → wygrywa; `.evoke-panel-mode .evoke-card` wyższą specyficznością zeruje przy stylu panel). Inne właściwości szkła (bg/border/shadow) zostają zmiennymi — var() działa dla normalnych właściwości, tylko nie dla `-webkit-backdrop-filter`. GOTCHA NA STAŁE: backdrop-filter zawsze literalną wartością, NIGDY przez var(). Cache-busting `app.css?v=assetVer` (v0.97.5) zostaje
- [x] Nawigacja portali klienta (`Settings.layout.portalNav` none|tabs|side-left|side-right, domyślnie none): sekcje /c (projekty/oferty/dokumenty/do zapłaty — pozycje tylko dla niepustych, przy <2 sekcjach stos) i /p (pliki od nas / prześlij+braki z badge) jako zakładki albo menu boczne w karcie; partial `_portal_nav.ejs` (part side|tabs), Alpine `sec` + deep-link `#hash`; domyślna sekcja z `?sent=1`/`?paid=1` liczona SERWEROWO (GOTCHA: doklejanie hasha w JS po uploadzie = nawigacja „tylko-hash" bez przeładowania — drugi upload nie pokazałby plików); wąskie kompozycje (panel/panel-bg/showcase/corner/split) degradują menu boczne do zakładek (`res.locals.portalNavMode` w app.js). **GOTCHA rename**: /c renderuje local `portalClient` (NIE `client`) — widok ma include(). Test `test/portal-nav.test.js`
- [x] Aktualizator z GitHuba (Ustawienia → Zaawansowane, v0.98.0): `update.service` (check = git fetch + rev-list HEAD..origin/main + wersja z `origin/main:package.json`; `redact()` wycina token z URL-i w KAŻDEJ linii logu/błędu) + ODŁĄCZONY proces `jobs/update.job.js` (spawn detached+unref — przeżywa restart Passengera; kroki: backup OBOWIĄZKOWY [reuse backup.service, `prisma.$disconnect()` przed npm — GOTCHA EPERM Windows] → git pull --ff-only → npm install → npm run prisma:deploy → zapis `tmp/restart.txt`); stan `storage/tmp/update-status.json` (merge, stale-lock 30 min) + log `update.log`; endpointy `/admin/update/{check,run,status,notify}` (JSON dla Alpine; status = health-check po restarcie, polling ignoruje błędy sieci); UI z listą commitów i logiem na żywo; cron `reminders.runUpdateCheck` (przed bramką SMTP) → event `update` w NOTIFY_TYPES (dzwonek; klik → `/admin/settings?tab=advanced`, deep-link zakładki przez `tabInit` w settings.controller); powiadomienia wyłączane plikiem-flagą `storage/update-notify-off` (wzór backup). Test `test/update.test.js`
- [x] Nawigacja portali w CHROME strony (v0.99.0): nowe tryby `portalNav` — `header` (menu w nagłówku obok logo; przy logo wyśrodkowanym pod nim; w Banner/Panelach wpina się w ICH nagłówki, w kompozycji `sidebar` nad kartą) i `rail-left/right` (pełnowysoki brandowy pas: logo + menu + stopka; przy kompozycji `sidebar` menu wchodzi w JEJ pas bez dublowania; na desktopie standardowy nagłówek/stopka dostają `md:hidden` — logo/stopka są w pasie; mobile = poziomy pasek page-top z `mobileOnly`). ARCHITEKTURA: sekcje buduje KONTROLER (local `portalNav` = {sections,keys,defaultSec}; portal.controller /p zawsze 2 sekcje, client.controller /c z regułą ≥2 sekcji → null = stos), a x-data ze stanem `sec` siedzi na wrapperze `layouts/public.ejs` — wspólny scope Alpine dla menu w chrome i `x-show` sekcji w widoku. Partial `_portal_nav.ejs`: parts side|tabs (w karcie), page-top|page-side (pływające), chrome-top (tone: bg|white|brand) i rail. Stare pływające warianty zostają („Pasek nad kartą", „Panel obok karty") — razem 10 opcji
- [x] Link powrotny „Wszystkie projekty" na /p → /c: TYLKO dla sesji, która odwiedziła portal klienta (`req.session.cPortal[token]` ustawiane przy wejściu na /c; sam link /p bywa udostępniany osobom trzecim — NIE ujawniamy im adresu /c z rozliczeniami/dokumentami). `backToPortal` w portal.controller, link nad tytułem w portal.ejs
- [x] Poprawki nawigacji portali + „Wiadomości" w menu + szklany panel + lista kopii (v0.99.1):
  - **Kontrast pozycji menu**: usunięta zawodna heurystyka `bgDark` (górna część gradientu bywa jasna → biały tekst na jasnym = niewidoczny). Chipy `_portal_nav.ejs` ZAWSZE jasne półprzezroczyste (`bg-white/70`) + `dark:` warianty (klasa `html.dark`). GOTCHA: aktywna pozycja na brandzie = `bg-white/90`, NIGDY `bg-white` — `html.dark .bg-white` ma `!important` (override na ciemną powierzchnię), więc `bg-white` w dark mode ciemnieje; `/90` jest poza tym overridem.
  - **Pływające ikonki (tryb/koperta)**: `fabCls` w `layouts/public.ejs` — białe (`bg-white/15 text-white`) gdy brandowy pas jest przy PRAWEJ krawędzi (`rail-right` albo `sidebar` z `cardSide:right`); menu w nagłówku dostaje `pr-12` (odsuw od przełącznika `fixed top-4 right-4`).
  - **„Wiadomości" jako pozycja menu**: kontrolery dokładają do `portalNav.sections` pozycję `{action:'messages', dot:msgHasReply}` (NIE w `keys` — nie liczy się do progu ≥2 sekcji na /c). `navClick()` w partialu: `action:'messages'` → `dispatchEvent('open-msgs')` zamiast przełączania `sec`. Layout: `msgInMenu` chowa pływającą kopertę, `@open-msgs.window` na jej `x-data` otwiera popup. Bez nawigacji koperta jak dotąd.
  - **Szklany panel**: przy `card:'glass'` `surfaceVars` (app.js) wstrzykuje `.evoke-panel{background:rgba(255,255,255,0.62);backdrop-filter:blur(14px)}` + `html.dark .evoke-panel{...0.55...!important}`. Blur LITERALNIE (GOTCHA Safari, nie przez var()). Dotyczy kompozycji panel/panel-bg (flush `.evoke-panel`).
  - **Lista kopii zapasowych** (Ustawienia → Zaawansowane): `backup.service.list/filePath/remove` — nazwa twardo po wzorcu `^evoke-backup-[\w-]+\.zip$` + `name===basename(name)` (zero path traversal). Trasy `GET /admin/settings/backups/:name` (download) + `POST .../:name/delete`. Tabela w settings.ejs (pobierz/usuń). Test `test/backups.test.js` (traversal odrzucany)
- [x] Poprawki wyglądu portali + PDF (v0.99.2):
  - **Dedup logo**: przy brandowym pasie z menu (`railOwn`) logo na TLE kompozycji (panel-bg aside, sidebar aside) chowa się automatycznie (`showBgLogo = !railOwn && !hideBgLogo` w layoucie) — koniec podwójnego logo. Ręczny przełącznik `layout.hideBgLogo` („Ukryj logo na tle strony", Ustawienia → Wygląd).
  - **Przełącznik trybu w pasku menu**: przy `portalNav:'header'` przełącznik jasny/ciemny renderuje się WEWNĄTRZ paska menu (`_portal_nav` part `chrome-top`, local `withTheme`) w jednej linii z pozycjami; pływający (`fixed`) jest wtedy ukryty (`!headerNav`). Wiadomości już wcześniej w menu (msgInMenu). Dotyczy wszystkich kompozycji z headerNav (header/banner/panel/panel-bg/sidebar).
  - **PDF `noWrap` na statusie**: „Rozliczono"/„Do zapłaty" w wąskich szablonach (accent) zawijało się — komórka statusu ma `noWrap:true`.
  - **PDF `pdf.hideSeller`**: przełącznik „Ukryj dane sprzedawcy na wydruku" (Ustawienia → Rozliczenia) — `buildDoc` zeruje `seller` → partiesBlock pokazuje sam blok nabywcy, totalsBlock pomija linię „Do zapłaty na konto". Sam wykaz pozycji bez sprzedawcy.
  - Testy: `test/pdf-pagebreak.test.js` (hideSeller + noWrap na strukturze `buildDoc`), `test/portal-nav.test.js` (przełącznik trybu w menu).
- [x] Pas pionowy: logo out + zwijanie hamburgerem (v0.99.3):
  - **Logo znika z PASA** (nie z panelu/tła — odwrotnie niż v0.99.2). `showBgLogo = !hideBgLogo` (bez warunku railOwn) → logo zostaje na tle kompozycji (panel-bg aside, sidebar aside); pas pionowy renderuje się BEZ logo. Awaryjnie w układach klasycznych (bez innego miejsca): standardowy `<header>` z logo pokazywany też na desktopie przy railOwn (usunięty `railOwn ? md:hidden`; stopka dalej `md:hidden` pod railOwn — jest w pasie).
  - **Zwijany pas (hamburger ☰, push)**: x-data wrappera dostał `railOpen: true`; aside `:class="railOpen ? 'md:w-60' : 'md:w-16'"` + `transition-[width] duration-300 overflow-hidden` (treść płynnie się przesuwa — flex sibling). Przycisk ☰ na górze pasa (`icon('menu')`) toggluje `railOpen`. Part `rail` w `_portal_nav`: etykiety/badge `x-show="railOpen"`, przycisk `:class="{ 'justify-center px-0': !railOpen }"` (ikony wyśrodkowane po zwinięciu); active-state połączony w JEDEN obiekt `:class`. Kompozycja `sidebar` (rail wpięty w jej pas) NIE zwija się — `railOpen` zawsze true, brak `transition-[width]`. Nawigacja bez zmian (sekcje/`sec`/okienko wiadomości działają). Mobile: pas chowany, poziomy pasek u góry jak dotąd. Test rozszerzony w `test/portal-nav.test.js`.
- [x] Pas pionowy: dedup stopki + zapamiętywanie stanu (v0.99.4):
  - **Stopka nie dublowana**: przy `railOwn` + panel/panel-bg była stopka W PASIE (163) i na tle (aside) — dwa napisy „…bezpieczna wymiana plików". Stopkę usunięto z pasa (jak logo — zostaje w kompozycji/na tle); standardowa stopka odsłonięta na desktopie przy railOwn (usunięty `railOwn ? md:hidden`) — dla układów klasycznych to jedyne miejsce.
  - **railOpen z pamięcią (localStorage `evoke-rail`)**: x-data czyta zapamiętany stan (pierwszeństwo — pas trzyma zwinięcie/rozwinięcie MIĘDZY ekranami), fallback = domyślny per-strona (/c zwinięty, /p rozwinięty). `x-init="$watch('railOpen', …setItem…)"` persystuje każdą zmianę. Test rozszerzony (getItem/setItem, brak stopki w pasie, fallback true//p, false//c).
- [x] Pas pionowy jako stały nawigator portalu + wiadomości jako podstrona (v0.99.5):
  - **Pas zawsze widoczny** (rail-*): desktop zwijany hamburgerem (push), MOBILE — wąski pasek ikon w potoku, rozwinięcie = pełnoekranowy overlay (`fixed inset-0 z-40 md:static md:inset-auto md:w-60`; GOTCHA: `w-full` na `fixed` liczyło się do szerokości pasa, nie viewportu — użyto `inset-0`), klik pozycji zamyka overlay (`secClick` dokleja `if(!matchMedia(min-width:768).matches) railOpen=false`). Zniknął poziomy pasek mobilny (page-top) — pas przejął mobile.
  - **Ikona trybu jasny/ciemny na dole pasa** (nie pływająca — koniec kolizji z hamburgerem przy pasie z prawej). Pływające ikony (tryb/koperta) tylko gdy NIE headerNav i NIE railOwn.
  - **„← Wszystkie projekty" jako pozycja pasa** na /p (znika z karty gdy rail; `buildNav` w portal.controller). Sekcje/klucze/railOpen z kontrolera jak dotąd.
  - **Wiadomości = PODSTRONA** `.../wiadomosci` (koniec popupu): wspólny `views/public/messages.ejs` (wątek + formularz + powrót). Nowe GET/POST `/{p,c,t,upload,o}/:token/wiadomosci` + `/message`; pozycja „Wiadomości" i pływająca koperta to teraz LINKI (`msgContext.page`); wejście oznacza wątek jako obejrzany (msgSeen). /upload i /o dostały kontekst wiadomości (wątek: transfer przychodzący / klient-właściciel oferty). Pas na stronach treści (/t /upload /o) przez `utils/contentNav.js` (tylko rail: Wiadomości + tryb; inne warianty — pływająca koperta-link).
  - **GOTCHA `_portal_nav`**: pozycja z `href` = LINK (`<a>`), bez `href` = przycisk sekcji (Alpine `sec`); `linkBase` renderuje sekcje jako linki na podstronie. Testy: `portal-nav` (podstrona, mobile overlay, powrót w pasie), `messages`/`attachments` (asercje na podstronę zamiast popupu).
- [x] Aktualizator: opcja pominięcia kopii zapasowej (v0.99.6): checkbox „Zrób kopię zapasową przed aktualizacją" (domyślnie wł.) w sekcji Aktualizacje. `updater()` (settings.ejs) wysyła `skipBackup` JSON-em do `POST /admin/update/run`; `startUpdate({skipBackup})` dokłada arg `--no-backup`; `update.job` czyta `process.argv.includes('--no-backup')` i pomija krok backupu (log „POMINIĘTA"). Test w `update.test.js`.
- [x] Pas pionowy: przyklejony + poprawki mobile (v0.99.7):
  - **Przyklejony (sticky)**: `md:sticky top-0 h-[100dvh] self-start` — pas NIE przewija się z treścią; hamburger (góra) i tryb jasny/ciemny (dół) ZAWSZE widoczne, nawet przy długiej liście plików. Środek (menu) ma własny `overflow-y-auto`. GOTCHA: sticky wymaga miejsca w pojemniku — działa, bo `flex-1 flex` rośnie z treścią, a aside jest `self-start h-[100dvh]` (krótszy).
  - **Wysokość 100dvh, NIE 100vh**: na mobile `100vh` chowało dół (tryb) za paskiem URL. `h-[100dvh]` = dynamiczna wysokość widoku → tryb zawsze na dole widocznego ekranu.
  - **Mobile = animacja szerokości**: pas jest `fixed` (poza potokiem; treść dostaje `pl-14`/`pr-14`), zwinięty `w-14` → rozwija się PŁYNNIE do `w-screen` przez `transition-[width]` (wcześniej snap: width→inset-0 nie animuje). Klik pozycji zwija.
  - **Hamburger zostaje w miejscu**: usunięty `:class mx-auto` (był re-centrowany przy zwijaniu) — zawsze lewo-wyrównany (w-8), pozostałe ikony przesuwają się w miarę pojawiania napisów. Test w `portal-nav`.
- [x] Poprawki pasa pionowego na mobile + czytelność szkła (v0.99.8):
  - **Sticky nagłówek w „Panel na tle 2" (panel-bg)**: logo mobilne w `isPanelBg` NIE miało `sticky` (miał je tylko `isPanel`) → przy przewijaniu uciekało. Teraz `sticky top-0 z-20 bg-white` z pełną szerokością (ujemne marginesy `-mx-6 sm:-mx-9 px-6 sm:px-9` wychodzą poza padding sekcji — pasek na całą szerokość panelu, nie tylko w obrębie px).
  - **Hamburger pasa — wyśrodkowany + przyklejony do krawędzi**: był `w-8` lewo-wyrównany zawsze → przy pasie z prawej „uciekał" z panelem przy rozwijaniu (skok na lewą krawędź). Teraz `w-full flex` + `:class` — zwinięty: `justify-center` (środek pasa), rozwinięty: dosunięty do KRAWĘDZI (`justify-end` dla rail-right, `justify-start` dla rail-left przez EJS `<%= railRight ? … %>` w wyrażeniu Alpine). Ikona zostaje w miejscu przy zwijaniu (środek zwiniętego = krawędź rozwiniętego, różnica ~4px). Ikona w `<span w-8 h-8>` wewnątrz.
  - **Mrożone szkło czytelniejsze**: krycie 0.62 → **0.74** (`--card-bg` glass + `.evoke-panel`) — tekst pomocniczy (slate-400/500) i przerywana ramka dropzone gubiły się na jasnym tle. Ciemny szklany panel 0.55 → 0.62. Ramka dropzone `border-slate-300` → `border-slate-400` (portal.ejs + upload.ejs — nie łamie stanu `dragging` bo to tylko domyślna klasa). GOTCHA: `border-slate-400` NIE było w committowanym buildzie → wymagał `build:css`.
  - **„Tło ucina się z prawej" — ciemny pasek pod pasem (TYLKO tło-obraz)**: pas ma `h-[100dvh]` (żeby tryb/moon był na dole WIDOKU), ale na iOS `100dvh` na elemencie `position:fixed` bywa liczone jak MAŁY ekran (svh) → przy schowanym pasku URL pas jest KRÓTSZY niż realny widok i pod nim prześwituje baza `<body>`. Przy tle-obrazie baza body to ciemne `#0f172a` (`background.js bodyStyle` dla `type:image`) → widać ciemny pasek z prawej (przy gradiencie prześwituje gradient → niezauważalne). Fix: brandowy **PODKŁAD** `md:hidden fixed inset-y-0 z-20 w-14 bg-brand-600 pointer-events-none` (strona zależna od `railRight`) — `inset-y-0` (top:0;bottom:0, BEZ jednostek dvh/vh) pełni całą wysokość viewportu i wypełnia kolumnę pasa kolorem brandu do samego dołu; kryty przez rozwinięty pas (wyższy z-index). GOTCHA: nie odtwarza się w headless/emulacji (tam `dvh`==`vh`) — weryfikowane symulacją skrócenia pasa.
- [x] Poprawki pasa/nagłówka/szkła po testach na iPhonie (v0.99.9):
  - **Przyklejony nagłówek = DESKTOP only**: na mobile w panel-bg logo było `sticky bg-white` z odstępem od górnej krawędzi (brzydki biały pasek). Teraz mobile logo PŁYNIE z treścią (bez tła); toggle „Przyklejony nagłówek" przykleja na DESKTOPIE brandowy pas boczny (`md:sticky md:top-0 md:h-screen md:self-start` na aside panel-bg) — logo/hero/stopka zostają w widoku przy przewijaniu panelu.
  - **Hamburger pasa — stały rozmiar**: był `w-full` (cały górny wiersz klikalny) → hover zajmował całą szerokość, a stuknięcie OBOK ikony w rozwiniętym panelu zamykało pas („po chwili znika" na iOS). Teraz pojemnik `w-full flex` steruje TYLKO pozycją (justify-center zwinięty / justify-end|start rozwinięty), a klikalny jest przycisk `w-10 h-10` — hover kompaktowy, brak przypadkowego zamykania.
  - **Podkład pasa = `absolute`, nie `fixed`**: w Safari `position:fixed` z `inset-y-0`/`100dvh` dalej bywał krótszy niż widok (ciemny pasek pod pasem przy tle-obrazie). Podkład jest teraz `absolute inset-y-0` w `relative flex-1 flex` (pojemnik ≥ viewport przez `min-h-screen`) → sięga realnego dołu dokumentu bez pułapek fixed-viewport Safari.
  - **Szare czcionki na szkle**: dodane przyciemnienie tekstu pomocniczego WEWNĄTRZ szklanych powierzchni (`.evoke-card`/`.evoke-panel`): slate-400→#64748b (slate-500), slate-500→#475569 (slate-600). Dark mode nietknięty (`html.dark .text-slate-*` ma !important).
- [x] Portale klienta bez przeładowania — Etap 1 (Turbo Drive, v0.99.10): nawigacja między stronami portali (`/c`↔`/p`, wejście w Wiadomości `.../wiadomosci`, powrót) przez **Turbo Drive** (Hotwire 8, zvendorowane lokalnie `public/js/turbo.js` — jak Alpine/Sortable, CSP `'self'` pokrywa) — fetch + podmiana `<body>` zamiast pełnego reloadu, zachowany back/scroll/URL. **Alpine radzi sobie sam** z podmianą body (obserwator mutacji re-inicjuje `x-data` — zweryfikowane: sekcje `sec`, `railOpen`, uploader działają po nawigacji; NIE trzeba `Alpine.initTree`). Skrypty zależne od DOM re-inicjowane na `turbo:load`: `bg-rotate.js`/`sticky-header.js` dostały RE-URUCHAMIALNY `init` (`window.evokeBgInit`/`evokeStickyInit`; sticky podpina scroll RAZ do window, bg czyści interwał slideshow) — bez tego wróciłoby migotanie szkła / martwy sticky. Wykluczone z Turbo (`data-turbo="false"`): wszystkie POBIERANIA (`_file_row`, dokumenty `/c`, ZIP `/p`+`/t`) i WSZYSTKIE formularze publiczne (uploader, proofing, „zgłoś wpłatę", oferta, wiadomości, onboarding, hasło) — w Etapie 1 akcje zostają na pełnym submit. Pasek postępu Turbo w kolorze brand (`.turbo-progress-bar` w input.css). **GOTCHA**: nowe pobieranie/formularz w portalach = pamiętać o `data-turbo="false"`, inaczej Turbo przechwyci klik/submit. Zakres Etapu 2 (osobno): podmiana fragmentów po akcjach (upload→lista bez reloadu, proofing/płatność/oferta w miejscu) + `Turbo.visit` w finalize uploadu.
- [x] Poprawki po Etapie 1 + panel admina (v0.99.11):
  - **Błysk tła przy nawigacji Turbo (regresja Etapu 1)**: `<body>` jest podmieniany, więc warstwa tła-obrazu (odsłaniana `opacity 0→1` po onload) re-odsłaniała się przy KAŻDYM przejściu → migotanie. Fix: warstwy tła w kontenerze `<div id="evoke-bg" data-turbo-permanent>` — Turbo przenosi go 1:1 na nową stronę (ten sam węzeł DOM, już odsłonięty), więc brak re-odsłonięcia. Zweryfikowane: po nawigacji `.bg-img-layer` to TEN SAM element (opacity zostaje 1).
  - **Pasek nagłówka panelu w dark mode** (`colors.adminHeader`): trzymał jasny kolor z ustawień w ciemnym trybie. Dodane `--admin-header`/`--admin-header-text` do bloku `html.dark` w input.css (→ `dk-surface`/`dk-text`), jak sidebar/tło.
  - **Sidebar panelu widoczny cały czas (app-shell przez STICKY)**: sidebar i nagłówek `md:sticky md:top-0` (sidebar `md:h-screen md:self-start`), strona przewija się normalnie (body). ŚWIADOMIE bez `overflow-hidden`/`overflow-y-auto` na kolumnie treści — patrz GOTCHA w v0.99.12. `nav` sidebara `min-h-0 overflow-y-auto` (długie menu). Mobile bez zmian (offcanvas, normalny scroll).
  - **Skok wysokości pozycji pasa portalu**: pozycje `part:'rail'` miały `py-2` — po zwinięciu etykieta znika (x-show), więc element był niższy niż po rozwinięciu (ikona+podpis) → hover/aktywny „skakał". Stała wysokość `h-9` (zamiast `py-2`) na pozycjach pasa i przełączniku trybu → brak skoku (zwinięty=rozwinięty=36px).
- [x] Poprawki po testach v0.99.11 (v0.99.12):
  - **Migotanie „szkła" przy nawigacji Turbo**: handler `turbo:load` wołał `evokeBgInit`, który robił `refreshGlass` (puls blur 22→14px) na WSZYSTKICH `.evoke-card`/`.evoke-panel` — przy każdej nawigacji szklane elementy (np. okienko wiadomości) mrugały. Skoro tło jest teraz `data-turbo-permanent` (stabilne), nowe karty malują się od razu na świeżym backdropie i pulsu NIE potrzeba. `turbo:load` re-inicjuje już TYLKO sticky-header. GOTCHA NA STAŁE: nie odświeżać szkła (`refreshGlass`) przy nawigacji Turbo — tylko przy pierwszym odsłonięciu tła-obrazu.
  - **„Szary ekran i część znika" w Ustawieniach → Wygląd**: app-shell z v0.99.11 (`md:overflow-hidden` na pojemniku + `main md:overflow-y-auto`) tworzył kontener przewijania/klipujący WOKÓŁ podglądów z `backdrop-filter` (podgląd szkła stron klienta) → glitch renderowania (szary prostokąt, znikające sąsiednie elementy) w Chrome/Safari. Fix: app-shell przez **STICKY zamiast overflow** — `md:sticky md:top-0` na `aside` (+`md:h-screen md:self-start`) i na `header` (`md:z-20`), strona przewija się normalnie (body). Sidebar/nagłówek dalej zawsze widoczne, a BRAK kontenera overflow = brak glitcha backdrop-filter. GOTCHA NA STAŁE: nie owijać treści admina w `overflow-*`, bo w środku są podglądy `backdrop-filter`.
- [x] Kropka powiadomienia jako badge w rogu ikony (v0.99.13): w pasie pionowym portalu kropka nowej wiadomości (`s.dot`) była osobnym elementem flex OBOK ikony → w zwiniętym pasie spychała ikonę z centrum. Teraz ikona jest owinięta `<span class="relative">` a kropka to `absolute -top-1 -right-1` (ring-brand-600 = „wycięcie" na tle pasa) — nie zajmuje miejsca w wierszu, ikona zostaje wyśrodkowana (zweryfikowane: iconCx==railCx). Dotyczy części `rail` w `_portal_nav.ejs`; pozostałe warianty (z etykietą) mają kropkę na końcu, tam ikona i tak nie skacze.
- [x] Badge nowej wiadomości na kafelku projektu w /c (v0.99.14): wątki wiadomości są rozdzielone per kontekst (`thread({projectId})` vs `thread({clientId, projectId:null})`), więc wiadomość agencji w PROJEKCIE nie pokazywała się na liście projektów `/c` — klient musiał wejść w każdy projekt. Teraz kafelek projektu dostaje badge (ikona `mail` + kropka w rogu), gdy jest oczekująca wiadomość: `client.controller.showClientPortal` liczy `p.hasNewMsg` = ostatnia wiadomość `out` w projekcie nowsza niż `msgSeen[project.clientToken]` (sesyjne, spójnie z resztą) — JEDNO zapytanie `message.findMany({projectId:{in},direction:'out'})` pogrupowane po projekcie (bez N+1). Badge na kafelkach aktywnych i archiwalnych (`client-portal.ejs`). Nagłówkowa „Wiadomości" (wątek kliencki) bez zmian.
- [x] Komunikator agencji (dwupanel) + inicjowanie rozmów + trwałe przeczytanie (v0.99.15): skrzynka `/admin/messages` przerobiona z akordeonu wątków na **dwupanel jak WhatsApp/Messenger** — lewa lista KLIENTÓW (nazwa, podgląd ostatniej wiadomości, czas, badge nieprzeczytanych), prawa = JEDEN strumień rozmowy klienta (bąbelki in/out) z **chipami kontekstu** („· Nazwa projektu"/„Transfer"). Agencja może teraz **ZAGAJAĆ** (wcześniej tylko odpowiadać na `:id/reply`): pole pisania z selektorem kontekstu `<select name="scope">` = Ogólne (`c`) / projekt (`p:<id>`) / transfer obecny w rozmowie (`t:<id>`), default = kontekst ostatniej wiadomości; checkbox „Powiadom mailem" (reuse `mail.sendClientReply`). **Trwałe** przeczytanie: otwarcie rozmowy (GET `?client=<id>`) i wysłanie → `messageService.markClientRead(clientId)` (DB `isRead`, nie sesyjne); badge maleje + `unreadCount` w menu odświeżany. Agregacja PER KLIENT (każda wiadomość ma `clientId`): `conversationList()` (grupowanie + unread), `conversation(clientId)` (strumień + include project/transfer do chipów), `send({clientId,projectId?,transferId?})`, `deleteClientConversation`. Trasy: `GET /messages?client=`, `POST /messages/:clientId/{send,delete}`, zostaje `GET /messages/:id/attachment` (usunięte `:id/{reply,read,delete}`). Widok responsywny (mobile: lista ↔ rozmowa + „← wróć"). Wysokość dwupanelu `md:h-[calc(100vh-8rem)]` z `overflow-y-auto` na panelach (w adminie BEZ backdrop-filter → overflow bezpieczny). Oferty są `clientId`-only (brak `offerId` na Message) → wpadają do „Ogólne". Test `test/messages-admin.test.js` + zaktualizowany `messages.test.js` (send zamiast reply). GOTCHA: `send` NIE dotyka `create` (klient→agencja bez zmian).
- [x] Punkty startu rozmowy z komunikatora (v0.99.16): brakowało wejścia, gdy klient nie miał jeszcze żadnej wiadomości (nie był na liście `conversationList`). Dodane: przycisk „Napisz" na kartotece klienta 360° (`/admin/messages?client=<id>`), „Napisz do klienta" na stronie projektu (`?client=<id>&scope=p:<projectId>` — pole pisania od razu w kontekście projektu), oraz selektor „+ Nowa rozmowa" w skrzynce (`allClients` z `clientService.options()` → wybór DOWOLNEGO klienta, także bez wiadomości). Kontroler `listMessages` czyta `?scope` (podpowiedź kontekstu) i przekazuje `scopeHint` do widoku (wygrywa nad domyślnym „kontekst ostatniej wiadomości"). Otwarcie klienta bez wiadomości = pusty strumień „Napisz pierwszy…" + pole pisania. Zweryfikowane E2E.
- [x] Formularz wiadomości klienta — czytelniejszy „Dołącz plik" + bez zbędnych pól (v0.99.17): w `public/messages.ejs` przycisk „Dołącz plik" z małego linka → wyraźny OBRAMOWANY (dashed) przycisk z ikoną chmury (łatwo było przeoczyć). Pola „Imię"/„E-mail" ukryte gdy nadawca ZNANY — w portalu klienta (/p, /c, /o) wiadomość niesie `clientId`, agencja wie kto pisze i odpisuje na `client.email`. Pola zostają TYLKO dla anonimowych transferów (/t, /upload bez projektu/klienta — tam e-mail bywa jedynym kanałem odpowiedzi). Flaga `msgKnownSender` przekazywana z kontrolerów (portal/client/offer = true; download/receive = `!!(transfer.project && transfer.project.clientId)`). Endpoint `create` bez zmian (senderName/Email dalej opcjonalne).
- [x] Composer wiadomości jak w komunikatorze (v0.99.18): w `public/messages.ejs` **Enter wysyła** wiadomość, **Shift+Enter = nowa linia** (`@keydown.enter` → `$event.target.form.requestSubmit()`; działa bo formularz jest `data-turbo="false"` i `requestSubmit` respektuje `required`); placeholder wprost tłumaczy skrót. Przebudowany dolny rząd: „Dołącz plik" jako kompaktowa OBRAMOWANA etykieta z ikoną `+` po LEWEJ (na mobile sam `+`, `sm:` z podpisem), po wyborze pliku chip z nazwą i „×" czyszczącym input (`$refs.file`), a przycisk „Wyślij" (brand) dosunięty do PRAWEJ (`ml-auto`) — jeden rząd zamiast pionowego stosu. Bez zmian w kontrolerach/endpoincie.
- [x] Enter wysyła w komunikatorze admina (v0.99.19): pole pisania w `admin/messages/index.ejs` wysyła wiadomość Enterem (Shift+Enter = nowa linia) — jak w portalu klienta. Handler w istniejącym bloku `<script>` (admin bez Alpine/Turbo): `textarea[data-enter-send]` łapie `keydown`, `Enter` bez Shift → `preventDefault` + `form.requestSubmit()` (respektuje `required` i `select` kontekstu). Placeholder tłumaczy skrót. CSP-safe (blok inline już był dla auto-scrolla strumienia).
- [x] Nowy wygląd maili (v0.99.20): przeprojektowany centralny szablon `wrap()` + `btn()` w `mail.service.js` (jedna zmiana → WSZYSTKIE maile). Zamiast pełnego brandowego paska u góry: cienki 4px brandowy pasek + logo/wordmark (kolor marki) na BIAŁYM nagłówku, hairline nad stopką, miękki cień, rogi 18px, jaśniejsze tło strony (#f4f4f7). Typografia: systemowy font stack (`-apple-system`…), body slate-700 `line-height 1.6`, nagłówki z ujemnym trackingiem. Przycisk większy (radius 12px). Stopka na `#fafafa` z hairline. NOWE: `preheader` (ukryty podgląd w skrzynce, domyślnie = heading) + `<meta color-scheme:light>` (stabilny wygląd w dark mode klienta pocztowego). Treści/placeholdery/szablony z Ustawień → E-mail bez zmian (tylko „opakowanie").
- [ ] Do zrobienia: portale bez przeładowania — Etap 2 (akcje w miejscu, fragmenty); backup off-site; rytm kontaktu; drugi user/role. UWAGA: white-label per-klient/projekt POZA ZAKRESEM NA STAŁE (decyzja 2026-07-02) — nie proponować.

## Workflow Git
Nie pushować automatycznie. Bump wersji + wpis w changelogu dopiero po potwierdzeniu. W komunikatach/URL-ach redagować token dostępowy.
